---
phase: 04-custody-photos
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - lib/photos.ts
  - db/queries/attachments.ts
  - app/api/devices/[id]/photos/route.ts
  - app/api/attachments/[attachmentId]/route.ts
  - app/(app)/(card)/devices/[id]/photo-grid.tsx
  - app/(app)/(card)/devices/[id]/page.tsx
  - db/queries/devices.ts
  - tests/attachments-queries.test.ts
  - scripts/smoke-custody.mjs
autonomous: false
requirements: [REG-05]
gap_closure: false
must_haves:
  truths:
    - "Фото прикладываются к карточке (input accept=image/* multiple, с телефона тоже), до 8 на устройство (серверный cap-гвард при загрузке и удалении-пересчёте)"
    - "Пайплайн: клиентский ресайз до ~1600px (createImageBitmap+canvas — EXIF ориентация запекается) → POST route handler → sharp rotate+resize inside+withoutEnlargement → re-encode JPEG (EXIF/GPS/ICC стрипаются по умолчанию) → файл на диск UPLOADS_DIR, метаданные в attachments (никогда BLOB)"
    - "Миниатюры: thumb-файл `<key>.thumb.jpg` (400px, деривация по суффиксу — миграций нет); сетка 3/4-колоночная, лайтбокс по клику, удаление с confirm (E-05)"
    - "Раздача ТОЛЬКО авторизованным: route handlers с requireSession()-первой-строкой, Cache-Control private; без cookie → 401 (ACC-02 периметр для бинарников)"
    - "IDOR-гвард: attachmentId в URL обязан ссылаться на deviceId из того же URL (V4); storage paths containment-checked под UPLOADS_DIR; magic-byte gate через sharp metadata (file.type не доверяем); ≤10MB raw; удаление недоступно для disposed (E-03)"
    - "Карточки устройств в списке показывают миниатюру-обложку (первое фото) — batched запрос (REG-05 «thumbnails visible in lists»)"
    - "Probe-грани REG-05: adjacency — attachmentId↔deviceId строгая пара; empty — 0 фото → empty state + CTA «Добавить фото»; ordering — сетка по created_at DESC (новые первыми)"
  artifacts:
    - path: "lib/photos.ts"
      provides: "thumbKeyOf(), UPLOADS_DIR резолвер, MAX_PHOTOS=8, валидация имён/путей (containment)"
    - path: "db/queries/attachments.ts"
      provides: "countByDevice, listByDevice (DESC), insert, getById(deviceId,attachmentId), deleteFile+row (tx-логика на queries-уровне)"
    - path: "app/api/devices/[id]/photos/route.ts + app/api/attachments/[attachmentId]/route.ts"
      provides: "POST upload (multipart), GET serve (original/thumb ?variant=), DELETE — все guard-first"
    - path: "photo-grid.tsx"
      provides: "сетка + лайтбокс + delete-confirm + upload-кнопка (client island)"
  key_links:
    - from: "app/api/devices/[id]/photos/route.ts"
      to: "lib/photos.ts"
      via: "UPLOADS_DIR/containment/лимиты"
      pattern: "photos"
    - from: "photo-grid.tsx"
      to: "app/api/attachments/[attachmentId]/route.ts"
      via: "router.refresh() после upload/delete (client fetch)"
      pattern: "route-refresh"
  prohibitions:
    - statement: "MUST NOT раздавать/принимать фото через public/ или static — только авторизованные route handlers (ACC-02, включая будущие фото)"
      category: boundary
      verification: test
    - statement: "MUST NOT доверять Content-Type клиента — magic-byte gate через sharp metadata"
      category: safety
      verification: test
    - statement: "MUST NOT добавлять/удалять фото у disposed (E-03 view-only)"
      category: boundary
      verification: test
---

<objective>
**User story.** **Как** руководитель, **я хочу** прикладывать фото устройства (в том числе с телефона) и видеть миниатюры, **чтобы** отличать одинаковые единицы и фиксировать состояние (REG-05; E-05, E-06). Объявленная линия отреза фазы — при сдвиге сроков cutting этот план не роняет фазу.
</objective>

<execution_context>
@/Users/aleksey/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-custody-photos/04-CONTEXT.md
@.planning/phases/04-custody-photos/04-RESEARCH.md
@.planning/phases/04-custody-photos/04-PATTERNS.md
@.planning/phases/04-custody-photos/04-UI-SPEC.md
</context>

<tasks>

<task type="tracer">
  <name>Checkpoints + фото-пайплайн: sharp install (human-verify) → lib/queries → routes → grid → smoke</name>
  <files>package.json, lib/photos.ts, db/queries/attachments.ts, app/api/devices/[id]/photos/route.ts, app/api/attachments/[attachmentId]/route.ts, photo-grid.tsx, app/(app)/(card)/devices/[id]/page.tsx, db/queries/devices.ts, tests/attachments-queries.test.ts, scripts/smoke-custody.mjs</files>
  <read_first>.planning/phases/04-custody-photos/04-RESEARCH.md (C4 sharp-пайплайн executed, C5 client resize, C6 route handlers, C7 TZ — дословно); 04-PATTERNS.md (route handler conventions, thumbKeyOf); 04-UI-SPEC.md (photo grid/lightbox/копи, counter «{n} из 8»); app/api/health/route.ts (guard-first route); app/(app)/devices/device-dialog.tsx (echo values); tests/helpers.ts; node_modules/next/dist/docs/ (route-handlers.md)</read_first>
  <action>
0. CHECKPOINT (первый шаг, до npm install): карта пакета sharp@0.35.4 (official repo lovell/sharp, ~94M/wk, no postinstall, SUS=too-new churn) → human-verify gate="blocking-human". После аппрува: `npm install sharp@0.35.4`.
1. `lib/photos.ts` (чистый): UPLOADS_DIR (env, default './data/uploads'), MAX_PHOTOS=8, MAX_RAW_BYTES=10MB, thumbKeyOf(storageKey) → `<base>.thumb.jpg`, assertInsideUploads(path) containment, DEVICE_EXTENSION guard-хелпер (disposed → {code:'DISPOSED'}).
2. `db/queries/attachments.ts` (чистый, better-sqlite3): countByDevice, listByDevice (created_at DESC), insertRow, getAttachment(deviceId, attachmentId) — строгая пара, deleteAttachment (unlink диска + DELETE row; disposed → DISPOSED).
3. `processPhoto(buffer)`: sharp(buffer).metadata() magic-byte gate (jpeg/png/webp/heic → convert) → rotate() → resize(1600, inside, withoutEnlargement) → jpeg({quality 82}) → thumb 400px. Garbage → {code:'BAD_IMAGE'}.
4. Route handlers (guard-first requireSession):
   - POST /api/devices/[id]/photos: multipart formData, cap ≤8 (count+files), ≤10MB/file, processPhoto, mkdir+write original+thumb, insert row; {code:'DISPOSED'|'CAP'|'BAD_IMAGE'} → маппинг русский copy; router.refresh() на клиенте.
   - GET /api/attachments/[attachmentId]?variant=thumb|full&device=<id>: пара id+device обязательна → stream файла, Content-Type по row, Cache-Control: private, immutable; 401 без сессии.
   - DELETE /api/attachments/[attachmentId]?device=<id>: disposed → отказ; unlink+row delete; refresh.
5. `photo-grid.tsx` (client island): сетка по UI-SPEC (aspect-square, counter), empty state + CTA, upload input accept=image/* multiple БЕЗ forced capture, клиентский ресайз (createImageBitmap+canvas ≤1600px) до POST, лайтбокс (full) с «Закрыть» aria-label, delete confirm (нейтральный).
6. `devices.ts` listDevices: +обложка (первое фото) batched-подзапросом; карточный page: placeholder «Фото» → реальный photo-grid; smoke-custody: upload через fetch FormData (node), assert GET thumb 200 c cookie / 401 без, cap на 9-м, удаление.
  </action>
  <behavior>
    attachments-queries (temp-DB + синтетические буферы): cap 8-й → CAP; processPhoto: 5000×2000 → ≤1600×inside, EXIF/GPS отсутствуют (metadata check), без enlargement для меньших, garbage → BAD_IMAGE; delete: файл исчез + row исчез; disposed → DISPOSED; getAttachment чужая пара → undefined.
  </behavior>
  <verify>
    <automated>npx vitest run && npm run build && node scripts/smoke-custody.mjs && node scripts/smoke-devices.mjs && node scripts/smoke-employees.mjs</automated>
  </verify>
  <acceptance_criteria>
    - vitest весь зелёный (attachments-кейсы)
    - smoke-custody: upload 200 → thumb 200 (cookie) / 401 (без), 9-е фото → русский cap-гейт, delete → 200 и файл/row исчезли, disposed → отказ
    - grep: uploads/ НЕ в public/; requireSession в каждом route handler (source-assert)
    - оба старых smoke не регрессировали
  </acceptance_criteria>
  <done>REG-05 закрыт: фото с телефона и десктопа, миниатюры в списках, авторизованная раздача, удаление.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Бинарный POST ↔ route | multipart от клиента — размер/магия/кап |
| GET ↔ файловая система | containment под UPLOADS_DIR, приватный кэш |
| attachmentId ↔ deviceId | IDOR-гвард строгой парой |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Tampering/Info Disclosure | upload/serve routes | high | mitigate | requireSession-first (ASVS V3); magic-byte gate; ≤10MB/≤8 caps; IDOR-пара; containment; private cache; EXIF-strip (приватность гео) |
| T-04-09 | Tampering | путь файла (storageKey) | medium | mitigate | storageKey генерит сервер (uuid), containment-check, клиент не влияет |
| T-04-10 | DoS | бинарный флуд | low | accept | caps 10MB/8шт + LAN один пользователь |
| T-04-11 | Elevation | disposed-мутации | medium | mitigate | DISPOSED guard в queries и роутах (E-03) |
</threat_model>

<verification>
- vitest/build/три smoke green; греп: public/ чист от uploads; requireSession во всех трёх хендлерах
</verification>

<success_criteria>
- REG-05: фото прикладываются (телефон/десктоп), миниатюры в списках, авторизованная раздача
</success_criteria>

<output>
Create `.planning/phases/04-custody-photos/04-03-SUMMARY.md` when done
</output>
