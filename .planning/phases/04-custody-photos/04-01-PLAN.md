---
phase: 04-custody-photos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/movement-schema.ts
  - db/queries/movements.ts
  - app/(app)/devices/actions.ts
  - app/(app)/devices/device-actions.tsx
  - app/(app)/devices/movement-dialogs.tsx
  - app/(app)/(card)/devices/[id]/page.tsx
  - app/(app)/(card)/devices/[id]/timeline.tsx
  - app/(app)/(card)/employees/[id]/page.tsx
  - app/(app)/employees/actions.ts
  - lib/ru.ts
  - tests/movements-queries.test.ts
  - scripts/smoke-custody.mjs
autonomous: true
requirements: [MOVE-01, MOVE-02, MOVE-03, MOVE-04, MOVE-05, EMP-02]
gap_closure: false
must_haves:
  truths:
    - "«Выдать» у in_stock-устройства: диалог (сотрудник → дата → комментарий), одна транзакция пишет movement(assigned) + обновляет devices.currentEmployeeId/status='assigned'; карточка устройства и карточка сотрудника (список выданного, «выдано {дата}») обновляются без F5 (MOVE-01, MOVE-05)"
    - "«Принять» (assigned→in_stock, событие returned from=держатель) и «Передать» (assigned→assigned, событие transferred from=старый to=новый) — те же однотранзакционные действия; «Выдать» скрыта у assigned (E-08) и дубль-guard в action: conditional UPDATE `WHERE id AND status=<ожидание>` решает по .changes — нелегальный переход отбрасывается с нулевыми эффектами (MOVE-02/03, RESEARCH executed)"
    - "Таймлайн: вертикальная лента, события occurredAt DESC + id DESC; пустой timeline рендерит синтетическое «Поступление» из devices.createdAt (миграций нет); имена держателей резолвятся join'ом, в т.ч. архивных сотрудников (MOVE-04)"
    - "Каждое событие: occurredAt — опциональная дата (дефолт сейчас, БУДУЩЕЕ отвергается zod'ом), комментарий ≤500 (E-01)"
    - "Перевод «себе самому»: transfer from=to отвергается zod-refine'ом (adjacency: получатель ≠ текущий держатель)"
    - "Карточка сотрудника: только устройства с текущим assigned на него; пусто → «Пока ничего не выдано» (фазовая копия); «Вернуть всю технику» — одна tx, N событий returned, инжектированный сбой откатывает ВСЁ (E-07, RESEARCH executed)"
    - "employees/actions.ts получает React-19 echo-values паттерн (known issue фазы 3): ошибки валидации больше не стирают ввод"
    - "[backstop/manual UAT] Русский копи-контракт диалогов и таймлайна — по 04-UI-SPEC (интерактив — UAT)"
  artifacts:
    - path: "lib/movement-schema.ts"
      provides: "zod-схемы действий (assign/accept/transfer/returnAll): employeeId, occurredAt (не будущее), comment ≤500, transfer-refine from≠to; чистый модуль"
    - path: "db/queries/movements.ts"
      provides: "assignDevice/acceptDevice/transferDevice/returnAllDevices (tx: guard-UPDATE+INSERT), listTimeline(deviceId), listIssuedByEmployee(employeeId), EventView-типы"
    - path: "app/(app)/devices/{actions,device-actions,movement-dialogs}.tsx + timeline.tsx"
      provides: "статусная матрица кнопок, 3 диалога, таймлайн на карточке"
    - path: "tests/movements-queries.test.ts"
      provides: "guard/tx/timeline/issued/return-all тесты (RESEARCH test map)"
  key_links:
    - from: "app/(app)/devices/actions.ts"
      to: "db/queries/movements.ts"
      via: "запросы действий из формы"
      pattern: "action→queries"
    - from: "db/queries/movements.ts"
      to: "lib/normalize.ts"
      via: "Tx-тип и транзакционный шаблон"
      pattern: "transaction"
  prohibitions:
    - statement: "MUST NOT читать status/currentEmployeeId из payload действий (только из строки БД в tx — guard-UPDATE)"
      category: boundary
      verification: test
    - statement: "MUST NOT существовать UPDATE/DELETE путей к movements (append-only триггеры RAISE(ABORT); исправление — компенсирующее событие)"
      category: safety
      verification: test
    - statement: "MUST NOT быть клиентской пагинации таймлайна v1 (устройств у одного сотрудника — десятки; полная лента по устройству)"
      category: scope
      verification: test
---

<objective>
**User story.** **Как** руководитель, **я хочу** выдавать, принимать и передавать технику одним действием — с атомарной записью в неизменяемую историю и мгновенным обновлением карточек, **чтобы** вопрос «у кого что» всегда имел точный ответ (MOVE-01..05, EMP-02; E-01, E-07, E-08).
</objective>

<execution_context>
@/Users/aleksey/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<artifacts_section>
## Artifacts this phase produces (доля этого плана)
Новые: lib/movement-schema.ts, db/queries/movements.ts, app/(app)/devices/{device-actions,movement-dialogs}.tsx, app/(app)/(card)/devices/[id]/timeline.tsx, tests/movements-queries.test.ts, scripts/smoke-custody.mjs.
Изменённые: app/(app)/devices/actions.ts (custody-действия + echo-паттерн), app/(app)/devices/device-dialog.tsx (кнопки-матрица), app/(app)/(card)/devices/[id]/page.tsx (placeholder «История» → timeline), app/(app)/(card)/employees/[id]/page.tsx (выданный список + «Вернуть всю технику»), app/(app)/employees/actions.ts (echo-values фикс), lib/ru.ts (плюралы событий при необходимости).
Символы: movementSchemas (assign/accept/transfer/returnAll), assignDevice(), acceptDevice(), transferDevice(), returnAllDevices(), listTimeline(), listIssuedByEmployee(), createDeviceDeviceAction-расширение.
</artifacts_section>

<context>
@.planning/phases/04-custody-photos/04-CONTEXT.md
@.planning/phases/04-custody-photos/04-RESEARCH.md
@.planning/phases/04-custody-photos/04-PATTERNS.md
@.planning/phases/04-custody-photos/04-UI-SPEC.md
</context>

<tasks>

<task type="tracer">
  <name>Tracer: movements core (schema+queries) + статусная матрица + диалоги + таймлайн + карточка сотрудника</name>
  <files>lib/movement-schema.ts, db/queries/movements.ts, app/(app)/devices/actions.ts, app/(app)/devices/device-actions.tsx, app/(app)/devices/movement-dialogs.tsx, app/(app)/(card)/devices/[id]/page.tsx, app/(app)/(card)/devices/[id]/timeline.tsx, app/(app)/(card)/employees/[id]/page.tsx, app/(app)/employees/actions.ts, lib/ru.ts, tests/movements-queries.test.ts, scripts/smoke-custody.mjs</files>
  <read_first>.planning/phases/04-custody-photos/04-RESEARCH.md (executed custody patterns C1–C7 — дословные механизмы: guard-UPDATE+INSERT, return-all atomicity, timeline order, batched issued query); db/queries/employees.ts (Tx-тип, транзакционный шаблон resolveDepartmentId); db/queries/devices.ts (getDevice); app/(app)/devices/actions.ts (action shape + echo values 4886f6a); app/(app)/devices/device-dialog.tsx (dialog wrapper/inner WR-01 split); app/(app)/(card)/employees/[id]/page.tsx (placeholder «Пока ничего не выдано»); .planning/phases/04-custody-photos/04-UI-SPEC.md (action matrix, dialog field order сотрудник→дата→комментарий, timeline vocabulary, issued rows); tests/helpers.ts; node_modules/next/dist/docs/ (server-actions, refresh)</read_first>
  <action>
TDD: tests/movements-queries.test.ts (RED) по RESEARCH test map, затем код (GREEN).

1. `lib/movement-schema.ts` (чистый, без next-импортов): zod-объекты assignSchema {employeeId: coerce.int.positive, occurredAt: опц. ISO-дата (regex + не в будущем — compare с началом завтрашнего дня server TZ), comment: строка ≤500 опц.}, transferSchema = assign + refine (employeeId ≠ текущий держатель — передаётся аргументом), acceptSchema {occurredAt, comment}; returnAll — без схемы (id из path-контекста). Экспорт типов.
2. `db/queries/movements.ts`: Tx-экспорт как в employees.ts. Функции (все — одна sync-транзакция каждая):
   - assignDevice(deviceId, employeeId, occurredAt?, comment?): guard-UPDATE devices SET currentEmployeeId=?, status='assigned' WHERE id=? AND status='in_stock' → .changes=0 → throw {code:'ILLEGAL_TRANSITION'}; INSERT movements(assigned, from NULL, to employee). Ожидаемый пост-статус задокументирован в комментариях.
   - acceptDevice(deviceId, ...): guard WHERE status='assigned' → SET NULL,'in_stock'; event returned from=holder.
   - transferDevice(deviceId, toEmployeeId, ...): guard WHERE status='assigned'; event transferred from=текущий to=новый.
   - returnAllDevices(employeeId, occurredAt?, comment?): tx: SELECT устройств по держателю → для каждого guard-UPDATE+INSERT returned; пустой список = no-op ok. Инжектированный сбой (тест) откатывает всё.
   - listTimeline(deviceId): SELECT m.*, имена from/to через LEFT JOIN employees (включая archived — имя нужно всегда); ORDER BY occurredAt DESC, id DESC.
   - listIssuedByEmployee(employeeId): текущие assigned устройства держателя + дата последнего assigned-события (max occurred_at), батчево.
3. `app/(app)/devices/actions.ts` + новый `device-actions.tsx`/`movement-dialogs.tsx`: assignDeviceAction/acceptDeviceAction/transferDeviceAction/returnAllAction — requireSession() первой строкой → movementSchemas.safeParse → queries → {ok:true}+refresh; ЛЮБАЯ ошибка → {fieldErrors|error, values: echoValues(formData)} (React-19 паттерн 4886f6a). Dialogs — wrapper/inner split (WR-01), порядок полей сотрудник→дата→комментарий, date input defaultValue=сегодня.
4. `device-actions.tsx`: статусная матрица кнопок по UI-SPEC (in_stock: Выдать[accent]/В ремонт[заглушка плана 02]/Списать[заглушка]; assigned: Принять/Передать/…; repair: Из ремонта; disposed: строка действий скрыта). «В ремонт»/«Списать» в этом плане — кнопки-заглушки disabled с title «План 02»? НЕТ: план 02 добавит их действиями — здесь просто не рендерить (матрица расширится в 02).
5. `timeline.tsx`: вертикальная лента по UI-SPEC (dot-rail, 14/600 лейблы события, строки маршрута «{от} → {кому}», комментарий, дата ru-RU); пустой список → синтетическое «Поступление» из devices.createdAt.
6. `employees/[id]/page.tsx`: реальный список issued (модель+серийник mono+«выдано {дата}»+count) вместо placeholder; кнопка «Вернуть всю технику» (confirm-диалог, returnAllAction).
7. `employees/actions.ts`: apply echo-values к update-веткам (known issue фазы 3 — минимум: return shape).
8. `scripts/smoke-custody.mjs`: minted cookie → 200 карточка устройства; прямая вставка сотрудников+устройства в temp-БД как в smoke-devices; проверка 307-периметра на /api (когда появится в 03) не требуется здесь — только страницы.
  </action>
  <behavior>
    movements-queries (temp-DB): assign от in_stock пишет event+проекцию; assign от assigned/repair/disposed → ILLEGAL_TRANSITION, 0 изменений; accept/transfer пишут корректные from/to; transfer from==to → zod-refine reject; occurredAt завтра → reject, вчера → ok; returnAll N устройств → N returned-событий + держатель пуст; инжектированный сбой в середине → ROLLBACK всех событий; timeline: backdated-события сортируются по occurredAt, id-tiebreak; имена архивных сотрудников резолвятся; issued: только assigned-на-него, дата = max(assigned occurred); movements UPDATE → RAISE(ABORT).
  </behavior>
  <verify>
    <automated>npx vitest run && npm run build && node scripts/smoke-custody.mjs && node scripts/smoke-devices.mjs && node scripts/smoke-employees.mjs</automated>
  </verify>
  <acceptance_criteria>
    - vitest весь зелёный (102+ новые movements-кейсы)
    - build exit 0; оба существующих smoke не регрессировали; smoke-custody exit 0
    - grep-гейты: `grep -c "status" app/(app)/devices/actions.ts` — status НЕ читается из formData (только из БД-строки в queries); movements UPDATE/DELETE в коде приложения = 0
    - «Выдать» не рендерится при status='assigned' (JSX-условие по статусу — source-assert)
  </acceptance_criteria>
  <done>Выдать/Принять/Передать/Вернуть-всё работают end-to-end, таймлайн живёт, карточки обновляются без F5, guard-и под тестами.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Server Action POST → actions | напрямую POST-абельны — requireSession первой строкой |
| payload → SQL | zod-whitelist; статус/держатель ТОЛЬКО из БД-строки внутри tx (guard-UPDATE) |
| timeline → рендер имён | React-экранирование; имена из LEFT JOIN |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Spoofing/Elevation | custody actions | high | mitigate | requireSession() первой строкой; PUBLIC_PATHS не тронуты |
| T-04-02 | Tampering | payload → переходы статусов | high | mitigate | guard-UPDATE по precondition в tx (RESEARCH executed); системные поля не читаются из payload; inject → strictObject/whitelist |
| T-04-03 | Repudiation | подделка истории | high | mitigate | append-only триггеры RAISE(ABORT) (тест существует); компенсация только новым событием |
| T-04-04 | DoS | return-all на тысячи строк | low | accept | один сотрудник — десятки единиц; одна tx; LAN |
</threat_model>

<verification>
- vitest весь зелёный; build; три smoke green
- Греп-гейты: нет UPDATE/DELETE movements; status/currentEmployeeId не читаются из formData
- Матрица кнопок соответствует статусам (source-assert)
</verification>

<success_criteria>
- SC1/SC2/SC3 (частично: таймлайн), SC4-заготовка — трассируются в задачу
- MOVE-01..05, EMP-02 закрыты этим планом
</success_criteria>

<output>
Create `.planning/phases/04-custody-photos/04-01-SUMMARY.md` when done
</output>
