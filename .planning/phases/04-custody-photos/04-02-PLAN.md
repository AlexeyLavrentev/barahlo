---
phase: 04-custody-photos
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/movement-schema.ts
  - db/queries/movements.ts
  - app/(app)/devices/actions.ts
  - app/(app)/devices/device-actions.tsx
  - app/(app)/devices/movement-dialogs.tsx
  - app/(app)/(card)/devices/[id]/page.tsx
  - tests/movements-queries.test.ts
  - scripts/smoke-custody.mjs
autonomous: true
requirements: [REG-04]
gap_closure: false
must_haves:
  truths:
    - "«В ремонт»: assigned → держатель автоматически принимается (returned-событие) + to_repair-событие, статус repair — одна tx; «Из ремонта»: repair → in_stock, from_repair-событие (E-04)"
    - "«Списать»: красная кнопка (#D70015 — единственный destructive), confirm с ОБЯЗАТЕЛЬНОЙ причиной; disposed-устройство — view-only: строка действий скрыта полностью, таймлайн и фото видны (E-03, UI-SPEC)"
    - "Матрица переходов: из disposed ВСЕ 7 переходов отвергаются guard'ом (RESEARCH executed); недостающие переходы (in_stock→Принять и т.п.) невозможны по построению матрицы кнопок + guard"
    - "События to_repair/from_repair/disposed видны в таймлайне с русскими лейблами словаря UI-SPEC (7 типов)"
    - "Probe-грани REG-04: adjacency — переход только с ожидаемого исходного статуса (guard-UPDATE); empty — disposed существует вечно, ничего не удаляется; ordering — статусы меняются только действиями (нет ручного пути)"
  artifacts:
    - path: "lib/movement-schema.ts (расширение)"
      provides: "repairSchema/disposeSchema (dispose: причина обязательна ≤500)"
    - path: "db/queries/movements.ts (расширение)"
      provides: "sendToRepair/returnFromRepair/disposeDevice — guard-tx; disposed-precondition для uploads (план 03 учтёт)"
    - path: "movement-dialogs.tsx: RepairDialog/DisposeDialog"
      provides: "диалоги ремонта и красного списания с причиной"
  key_links:
    - from: "device-actions.tsx"
      to: "UI-SPEC action matrix"
      via: "полная матрица 4 статусов"
      pattern: "status-matrix"
  prohibitions:
    - statement: "MUST NOT быть пути возврата из disposed (E-03 финальность; ошибка данных = новое устройство)"
      category: safety
      verification: test
    - statement: "MUST NOT красный цвет вне кнопки «Списать» и «Списано»-пилюли (резервация фазы 3)"
      category: contract
      verification: test
---

<objective>
**User story.** **Как** руководитель, **я хочу** отправлять устройство в ремонт и списывать списанную технику с обязательной причиной — действиями с событиями, **чтобы** статусы никогда не врали (REG-04; E-03, E-04).
</objective>

<execution_context>
@/Users/aleksey/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-custody-photos/04-CONTEXT.md
@.planning/phases/04-custody-photos/04-RESEARCH.md
@.planning/phases/04-custody-photos/04-PATTERNS.md
@.planning/phases/04-custody-photos/04-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Ремонт: «В ремонт» / «Из ремонта» (действия + диалоги)</name>
  <files>lib/movement-schema.ts, db/queries/movements.ts, app/(app)/devices/{actions,device-actions,movement-dialogs}.tsx, tests/movements-queries.test.ts</files>
  <read_first>.planning/phases/04-custody-photos/04-01-SUMMARY.md (что построено: guard-tx шаблон, матрица, диалоги); 04-RESEARCH.md (C1 guard-UPDATE, переходы); 04-UI-SPEC.md (ремонт-диалоги, копи); db/queries/movements.ts; tests/movements-queries.test.ts</read_first>
  <action>
RED: тесты sendToRepair/returnFromRepair — repairSchema {occurredAt?, comment? ≤500}; sendToRepair: assigned → tx(returned от держателя + to_repair, status='repair', holder NULL); in_stock → tx(to_repair только, status='repair'); repair → ILLEGAL_TRANSITION; transfer в ремонте — запрет. returnFromRepair: repair → from_repair, status='in_stock'; не-repair → ILLEGAL.
GREEN: реализация по guard-UPDATE шаблону плана 01. device-actions: в матрицу added — assigned: «В ремонт», in_stock: «В ремонт», repair: «Из ремонта» (accent — единственная forward-кнопка статуса). movement-dialogs: RepairDialog (дата+комментарий, echo values).
  </action>
  <verify>
    <automated>npx vitest run && npm run build && node scripts/smoke-custody.mjs</automated>
  </verify>
  <acceptance_criteria>
    - vitest зелёный (новые repair-кейсы: legal/illegal переходы, авто-приём от держателя)
    - матрица кнопок содержит «В ремонт» для in_stock+assigned, «Из ремонта» для repair (source-assert)
  </acceptance_criteria>
  <done>Цикл ремонта замкнут действиями, события целы.</done>
</task>

<task type="auto" tdd="true">
  <name>Списание: финальное действие с причиной + disposed view-only</name>
    <files>lib/movement-schema.ts, db/queries/movements.ts, app/(app)/devices/{actions,device-actions,movement-dialogs}.tsx, app/(app)/(card)/devices/[id]/page.tsx, tests/movements-queries.test.ts, scripts/smoke-custody.mjs</files>
  <read_first>04-UI-SPEC.md (dispose: красная primary, warning-копи, обязательная причина, «Не списывать»); 04-01-SUMMARY.md; db/queries/movements.ts; archive-confirm-dialog.tsx (confirm-паттерн)</read_first>
  <action>
RED: disposeSchema {occurredAt?, причина: string min 1 max 500 (name=comment)}; disposeDevice: из in_stock/assigned/repair → event disposed (from=держатель если был), status='disposed'; из disposed → ILLEGAL; любые другие переходы из disposed → ILLEGAL (7 переходов тестом).
GREEN: реализация. DisposeDialog: красная primary «Списать», причина обязательна (inline-ошибка), warning-текст финальности, dismissal «Не списывать». device-actions: для disposed строка действий НЕ рендерится вовсе; для остальных «Списать» красная последней. Карточка disposed: бейдж «Списано» (tinted red pill по UI-SPEC), таймлайн/фото читаются. smoke-custody: disposed-устройство → карточка 200 без кнопок действий.
  </action>
  <verify>
    <automated>npx vitest run && npm run build && node scripts/smoke-custody.mjs && grep -c "Списать" app/(app)/devices/movement-dialogs.tsx</automated>
  </verify>
  <acceptance_criteria>
    - vitest зелёный: disposed блокирует все 7 переходов; причина обязательна (пустая → inline-ошибка)
    - smoke: disposed view-only (нет action-кнопок в HTML)
    - греп-гейт: красный (#D70015) в JSX — только dispose-кнопка/пилюля (source-assert по файлам devices/)
  </acceptance_criteria>
  <done>REG-04 закрыт: все 4 статуса только действиями, списание финально с причиной.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Server Action POST → repair/dispose actions | requireSession первой строкой |
| payload → переходы | guard-UPDATE по precondition; причина через zod |
| disposed → мутации | нулевая поверхность: guard отвергает всё |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Tampering | ремонт/списание payload | high | mitigate | guard-UPDATE precondition; причина обязательна (E-03); whitelist |
| T-04-06 | Repudiation | возврат из списания | medium | mitigate | финальность + греп-гейт; append-only история |
| T-04-07 | DoS | — | low | accept | один пользователь LAN |
</threat_model>

<verification>
- vitest/build/smoke-custody green; матрица 4 статусов полная; disposed view-only
</verification>

<success_criteria>
- REG-04: все 4 статуса ставятся ТОЛЬКО действиями; списанное остаётся в базе с историей
</success_criteria>

<output>
Create `.planning/phases/04-custody-photos/04-02-SUMMARY.md` when done
</output>
