---
phase: 06-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - "db/queries/devices.ts"
  - "app/(app)/page.tsx"
  - "tests/dashboard-queries.test.ts"
  - "scripts/smoke-dashboard.mjs"
autonomous: true
requirements:
  - DASH-01
  - DASH-02
must_haves:
  truths:
    - "(Спец-край 2, DASH-02) Гарантийная граница инклюзивна по WARRANTY_WARN_DAYS=60 и делит код с фазой 5: гарантия до сегодня → «≤ 30» И «≤ 60», не истекла; до сегодня+60 → «≤ 60»; до сегодня+61 → ни один счётчик (и не истекла); до сегодня-1 → только «Истекла»; счётчики и WarrantyDate делят lib/warranty.ts — попадание счётчика никогда не рендерится зелёным"
    - "(Спец-край 1 — parity-пин, DASH-01/DASH-02) Автоматизированные parity-тесты: каждый счётчик (тайлы типов, тайлы статусов, пресеты w30/w60/expired) равен total соответствующего отфильтрованного списка listDevices при идентичных параметрах — счётчик проверяется против total ФИЛЬТРА через публичный API; отсутствующие бакеты рендерят 0 из keystone-дефолтов"
    - "(Спец-край 3, DASH-02) Ноль в пресете → счётчик показывает «: 0» и остаётся кликабельным (целевой список рендерит своё пустое состояние фазы 5); пустой топ-5 → «Нет техники с истекающей гарантией» — правдиво и при «Истекла: K > 0» (истёкшая ≠ истекает); свежая установка (0 устройств) → все тайлы 0, счётчики 0, честные пустые состояния по копи-таблице UI-SPEC"
    - "(Спец-край 4, DASH-02) Топ-5 ближайших = гарантия до ASC с id-tiebreaker (asc(warrantyUntil), asc(id)), только w60-окно «ещё живых»; счётчики рендерятся в фиксированном порядке 30 → 60 → истекла (D-04/UI-SPEC)"
    - "displayTodayUtc() вычисляется ОДИН раз за рендер страницы и передаётся и в warrantyPresetCounts/nearestExpiringWarranties, и в каждый WarrantyDate (orchestrator-резолюция 6; D-04)"
    - "(DASH-03-пин) Тесты фиксируют ленту: порядок desc(occurredAt), desc(id) устойчив при равных occurredAt (реальный tie), лимит 10 держится на 12 событиях, null-слоты выживают, fromId/toId присутствуют в выборке, movementEventLabel покрывает 7 типов с raw-fallback"
    - "Периметр дашборда доказан на production build: / без cookie → 307 /login; с cookie → 200 с «Дашборд», «Всего:», deep-link href всех трёх семейств (тип, статус, гарантийные пресеты) и пунктом nav «Дашборд»; на свежей temp-БД лента ожидаемо пуста"
    - "(UI Considerations → covered) Топ-5 строка: «гар. до » (14/400 secondary) + WarrantyDate variant=card (наследует 14/400, warn-оранжевый — список состоит только из w60-живых, зелёный внутри окна невозможен по построению); саб-хедер «Ближайшие сроки» 14/600 разделяет счётчики и список внутри одной карточки"
  artifacts:
    - "db/queries/devices.ts: warrantyPresetCounts(today = displayTodayUtc()) → { w30, w60, expired }, nearestExpiringWarranties(limit = 5, today = displayTodayUtc()) → NearestExpiringWarranty[] ({ id, model, warrantyUntil }) — co-located с warrantyPredicate"
    - "app/(app)/page.tsx: блок «Гарантия» — карточка с 3 счётчиками-Link, саб-хедером, топ-5 (модель + «гар. до » + WarrantyDate) и пустым состоянием; блок занимает левую колонку зоны `grid gap-8 sm:grid-cols-2 sm:gap-6`"
    - "tests/dashboard-queries.test.ts (Wave-0 fixture-набор 06-VALIDATION): parity против listDevices, TZ-граница с frozen MSK-полуночью, zero-default, порядок/лимит/null-слоты ленты"
    - "scripts/smoke-dashboard.mjs: периметр + render + deep-link пробы на :31xx-паттерне, пустая лента как норма свежей БД"
  key_links:
    - "warrantyPresetCounts/nearestExpiringWarranties ↔ warrantyPredicate: ТОТ ЖЕ модуль, ТЕ ЖЕ типизированные операторы (isNotNull + gte/lte/lt + addDaysUtc + WARRANTY_WARN_DAYS) — parity-тест прикалывает счётчик к total фильтра"
    - "Счётчики-строки → пресеты фазы 5 через buildDevicesQuery (полный DeviceFilters с warranty-пресетом) — ноль новой выборки (D-03/D-04)"
    - "Топ-5 даты → <WarrantyDate today={today} variant=\"card\">: тот же once-per-render today, что у счётчиков — четвёртый потребитель единственной гарантийной математики"
    - "tests/dashboard-queries.test.ts ↔ db/queries/devices.ts: любой дрейф предиката роняет parity-тест громко (D-04 — структурный, не code-review-hope)"
  prohibitions:
    - requirement_id: DASH-02
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "Гарантийный блок НЕ имеет собственной математики или цветовой логики (вторая реализация/цвет-свитч запрещены — только warrantyState/WarrantyDate/WARRANTY_WARN_DAYS из lib/warranty*), и истёкшие устройства НЕ появляются в списке «Ближайшие сроки» как живые: топ-5 ограничен w60-окном, истёкшие несёт только свой счётчик «Истекла»"
  backstops: []
---

<objective>
Достроить фазу 6: блок «Гарантия» (DASH-02) — три счётчика по готовым пресетам фазы 5 и топ-5 ближайших сроков с переиспользованной WarrantyDate, — затем приколоть инвариант D-04 тестами (parity счётчик↔фильтр, TZ-граница, zero-default, лента) и добавить smoke-dashboard. Это Wave-0 fixture-набор 06-VALIDATION и финальная сборка экрана.

Purpose: счётчик, разошедшийся со своим списком — cardinal bug фазы; parity-тест против total фильтра превращает инвариант D-04 из обзора кода в громкий красный тест.
Output: полный дашборд D-02..D-04, tests/dashboard-queries.test.ts, scripts/smoke-dashboard.mjs.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard/06-PATTERNS.md
@.planning/phases/06-dashboard/06-UI-SPEC.md
@.planning/phases/06-dashboard/06-VALIDATION.md

# Prior plan of this phase (page.tsx shape + aggregates live there):
@.planning/phases/06-dashboard/06-01-SUMMARY.md

# Phase-5 warranty surface this plan composes from:
@lib/warranty.ts
@lib/warranty-date.tsx
@db/queries/devices.ts
@tests/warranty.test.ts
@tests/devices-queries.test.ts
</context>

<artifacts_section>
## Artifacts this phase produces (phase-wide; «этот план» = 06-02)

**Query functions** — этот план добавляет: `warrantyPresetCounts(today?)` → { w30, w60, expired }, `nearestExpiringWarranties(limit?, today?)` → NearestExpiringWarranty[] (deviceCountByType/deviceCountByStatus/totalDeviceCount/listRecentMovements созданы в 06-01).

**Page / nav** — этот план добавляет блок «Гарантия» в page.tsx (голова, тайлы, лента и nav — 06-01).

**Tests / smoke** — этот план: `tests/dashboard-queries.test.ts` (parity, TZ-граница, zero-default, лента — Wave-0 fixtures 06-VALIDATION), `scripts/smoke-dashboard.mjs`; `scripts/smoke-devices.mjs` шаг 8 обновлён в 06-01.
</artifacts_section>

<tasks>

<task type="auto">
  <name>Task 1: Блок «Гарантия» — счётчики-пресеты и топ-5 ближайших сроков (DASH-02, D-04)</name>
  <files>db/queries/devices.ts, app/(app)/page.tsx</files>
  <read_first>
    - .planning/phases/06-dashboard/06-PATTERNS.md — разделы devices.ts (:191-203 warrantyPredicate — ТЕСТО операторной композиции; :1-20 импорты уже достаточны), Shared Pattern 2 (warranty single-source), Shared Pattern 4 (buildDevicesQuery), № 6/8 (сегментные ряды/пустые состояния)
    - .planning/phases/06-dashboard/06-RESEARCH.md — Patterns 2/3 (probe-верифицированные границы и порядок), Pitfall 1 (дрейф счётчика), Pitfall 2 (Date-операнды только типизированными операторами)
    - db/queries/devices.ts — текущее состояние после 06-01 (агрегаты уже рядом; НОВЫХ импортов не требуется: isNotNull/gte/lt/lte/addDaysUtc/displayTodayUtc/WARRANTY_WARN_DAYS на месте)
    - lib/warranty.ts (WARRANTY_WARN_DAYS=60, displayTodayUtc, addDaysUtc), lib/warranty-date.tsx :40-69 (WarrantyDate, вариант card = голая цветная дата; list-вариант с префиксом сюда не подходит)
    - app/(app)/page.tsx после 06-01 — куда встаёт блок: тайлы и лента уже на странице; зона двух колонок получает точные классы из 06-UI-SPEC (`grid gap-8 sm:grid-cols-2 sm:gap-6`), гарантия слева/первая в source order
    - app/(app)/devices/page.tsx :77-80 — контракт today-once фазы 5 (комментарий WAR-01)
    - /Users/aleksey/.zcode/skills/vercel-react-best-practices/SKILL.md — rendering-conditional-render (тернарник пустого состояния)
    - .planning/phases/06-dashboard/06-UI-SPEC.md — Screens (Block «Гарантия», Warranty counter rows, Sub-header, Top-5 nearest rows, Warranty empty), Copywriting (строки счётчиков и «гар. до »), Visual Details (counter row, chevron на счётчиках/топ-5)
  </read_first>
  <action>
    1. **db/queries/devices.ts** — добавь warrantyPresetCounts и nearestExpiringWarranties НЕПОСРЕДСТВЕННО рядом с warrantyPredicate (co-location = D-04; НЕ реимплементить операторы в другом модуле; НЕ экспортировать сам предикат).
       - warrantyPresetCounts(today: Date = displayTodayUtc()) возвращает { w30: number; w60: number; expired: number } (экспортируй тип WarrantyPresetCounts). Каждый из трёх count-запросов (count() с .get()!.value, без join — гарантийные термы employees не трогают) собирает where ИЗ ТЕХ ЖЕ типизированных операторов, что warrantyPredicate, с комментом-перекрёстной ссылкой на :191-203: окно ≤N — and(isNotNull(warrantyUntil), gte(warrantyUntil, today), lte(warrantyUntil, addDaysUtc(today, N))) с N = 30 и N = WARRANTY_WARN_DAYS; истёкшие — and(isNotNull(warrantyUntil), lt(warrantyUntil, today)). Инклюзивная граница фазы 5: до-сегодня живое, до-сегодня+60 внутри окна, до-сегодня+61 вне обоих окон и не истёкшее, до-сегодня-1 только истёкшее (orchestrator-резолюция 1: N маленьких count-ов, НЕ single-pass CASE — raw-шаблон не биндит Date). Default-параметр сохраняет module-internal конвенцию предиката; страница передаёт свой once-per-render today — один Date делится счётчиками и WarrantyDate (orchestrator-резолюция 6).
       - nearestExpiringWarranties(limit: number = 5, today: Date = displayTodayUtc()) возвращает массив { id, model, warrantyUntil } (экспортируй тип NearestExpiringWarranty; warrantyUntil материализуется как Date, timestamp-mode). where = w60-окно (те же операторы, что пресет ≤60); orderBy(asc(warrantyUntil), asc(id)) — id-tiebreaker для одной даты; .limit(limit). Истёкшие и NULL-гарантия НЕ попадают — их несёт счётчик «Истекла» (probe-верифицированный порядок RESEARCH Pattern 3).
    2. **app/(app)/page.tsx** — дострой блок «Гарантия» по 06-UI-SPEC (Screens & Component Inventory), copy-контракт дословно:
       - Зона блоков: `mt-8`, сетка `grid gap-8 sm:grid-cols-2 sm:gap-6`; «Гарантия» — первый ребёнок (левая колонка на sm+, первая на мобиле); существующий блок ленты становится вторым.
       - Заголовок «Гарантия» (20/600) + карточка `mt-3 rounded-2xl bg-white shadow-sm ring-1 ring-hairline divide-y divide-hairline` — ОДНА карточка: счётчики и список читаются как один блок.
       - Три счётчика-строки, ФИКСИРОВАННЫЙ порядок (D-04): «Истекает ≤ 30 дней: {N}», «Истекает ≤ 60 дней: {M}», «Истекла: {K}». Каждая — Link `min-h-11 px-4 py-2 hover:bg-page` + focus-visible ring-accent/30, текст text-sm text-ink, справа ChevronRight size 16 text-[#C7C7CC] aria-hidden (chevron — только на рядах, чья вся поверхность — одна навигация). Href строит buildDevicesQuery с полным DeviceFilters, отличающимся только warranty-пресетом (w30 | w60 | expired); ручная конкатенация filter-URL запрещена (orchestrator-резолюция 7). Счётчики рендерятся ВСЕГДА, включая «: 0» — цель валидный пустой список (UI Considerations).
       - Саб-хедер «Ближайшие сроки» — строка `px-4 py-2` с `text-sm font-semibold text-ink` (разрешённый третий вес — мини-хедер, отделяющий счётчики от списка).
       - Топ-5: nearestExpiringWarranties(5) с today страницы → ряды-Link на карточку устройства, `min-h-11 px-4 py-2 hover:bg-page` + title: строка 1 — модель (text-base text-ink truncate) + ChevronRight; строка 2 (mt-1) — «гар. до » (text-sm text-ink-secondary) + `<WarrantyDate value={warrantyUntil} today={today} variant="card" />` — компонент наследует 14/400 и меняет только цвет (warn-оранжевый внутри окна; зелёный здесь невозможен по построению — инвариант фазы 5). ПРЕФИКС «гар. до » рендерит сама страница (card-вариант — голая дата); list-вариант WarrantyDate сюда не переносить. НИКАКОЙ собственной date-diff/цвет-логики (Don't-Hand-Roll; запрет P2).
       - Пустой топ-5: quiet-строка «Нет техники с истекающей гарантией» (text-sm text-ink-secondary) — правдива всякий раз, когда w30 = w60 = 0, в т.ч. при «Истекла: K > 0» над ней.
       - today страницы (уже вычислен один раз в 06-01) передаётся в warrantyPresetCounts(today), nearestExpiringWarranties(5, today) и в каждый WarrantyDate — три потребителя, один Date.
    3. loading.tsx для / по-прежнему НЕ создавать (orchestrator-резолюция 2; research Pattern 6) — новых границ и островов ноль (D-06).
  </action>
  <verify>
    <automated>npx vitest run && npm run build && node scripts/smoke-devices.mjs</automated>
  </verify>
  <acceptance_criteria>
    <!-- planner-discipline-allow: buildDevicesQuery -->
    - `grep -c "export function warrantyPresetCounts" db/queries/devices.ts` == 1; `grep -c "export function nearestExpiringWarranties" db/queries/devices.ts` == 1
    - `grep -c "WARRANTY_WARN_DAYS" db/queries/devices.ts` >= 2 (предикат + новое окно) и `grep -c "addDaysUtc" db/queries/devices.ts` >= 3 (предикат + w30 + w60-окно)
    - `grep -c "displayTodayUtc" app/(app)/page.tsx` == 1 (один вызов за рендер) и `grep -c "warrantyPresetCounts(today)" app/(app)/page.tsx` >= 1 (today передан в счётчики)
    - `grep -c "WarrantyDate" app/(app)/page.tsx` >= 1 и `grep -c 'variant="card"' app/(app)/page.tsx` >= 1
    - `grep -c "Ближайшие сроки" app/(app)/page.tsx` == 1; `grep -c "Нет техники с истекающей гарантией" app/(app)/page.tsx` == 1
    - `grep -c "buildDevicesQuery" app/(app)/page.tsx` >= 1; `grep -c '"/devices?' app/(app)/page.tsx` == 0
    - `grep -c "warrantyState(" app/(app)/page.tsx` == 0 (собственной математики цвета на странице нет — только WarrantyDate; action литерал не содержит)
    - `npx vitest run` зелёный; `npm run build && node scripts/smoke-devices.mjs` exit 0 (страница по-прежнему рендерится, периметр цел)
  </acceptance_criteria>
  <done>Блок «Гарантия»: три счётчика с числами из операторов, идентичных предикату фильтров (тот же модуль, тот же today, что и даты), ссылки на готовые пресеты; топ-5 ближайших живых гарантий с оранжевой WarrantyDate и ссылками в карточки; пустые состояния по копи-таблице; счётчики рендерятся и в ноль.</done>
</task>

<task type="auto">
  <name>Task 2: Wave-0 тесты — parity против listDevices, TZ-граница, zero-default, лента</name>
  <files>tests/dashboard-queries.test.ts</files>
  <read_first>
    - tests/devices-queries.test.ts :1-26 — бутстрап temp-SQLite (DATABASE_PATH ДО первого импорта @/db, динамические импорты, applyMigrations(db.$client), afterAll-cleanup) — скелет нового файла
    - tests/movements-queries.test.ts — паттерн фикстур через реальные createDevice + custody-действия (assignDevice/transferDevice/acceptDevice/returnFromRepair); уникальные серийники per test; source-гейты
    - tests/devices-perf.test.ts — паттерн raw-insert фикстур (массовая посадка)
    - tests/warranty.test.ts — паттерн frozen-clock (MSK_0030) и DISPLAY_TZ wall-clock; замораживать системное время для границы, не хост-геттеры
    - db/queries/devices.ts — listDevices-сигнатура ({ type, page, pageSize, filters? }) и список экспортируемых агрегатов после 06-01/06-02 Task 1
    - db/queries/movements.ts — listRecentMovements/RecentMovementView после 06-01
    - .planning/phases/06-dashboard/06-VALIDATION.md — Wave 0 Requirements (parity fixtures, feed ordering, TZ boundary)
    - .planning/phases/06-dashboard/06-RESEARCH.md — Validation Architecture (тест-карта DASH-01/02/03, parity-примечание)
    - .planning/phases/06-dashboard/06-PATTERNS.md — тест-аналоги и «strongest assertion» parity-pin
  </read_first>
  <action>
    Создай tests/dashboard-queries.test.ts по бутстрап-паттерну devices-queries.test.ts (temp-каталог, DATABASE_PATH до импорта @/db, applyMigrations, закрытие коннекта в afterAll). Фикстуры — через реальные createDevice и custody-действия (паттерн movements-queries.test.ts; raw-insert допустим для массовых сценариев по паттерну devices-perf.test.ts); серийники уникальны per test. Четыре обязательные группы (Wave-0 06-VALIDATION):
    1. **Parity (D-04 pin)**: для каждого из 4 типов deviceCountByType-значение по ключу равно listDevices({ type: ключ, page: 1, pageSize: 1 }).total; для каждого из 4 статусов — зеркально по status-фильтру; totalDeviceCount() равно listDevices без фильтров total; warrantyPresetCounts().w30 / .w60 / .expired каждое равно listDevices({ type: 'all', page: 1, pageSize: 1, filters: { warranty: пресет } }).total — счётчик ассертится против total ФИЛЬТРА через публичный API (сильнейший guard дрейфа). Плюс: сумма значений по типам равна totalDeviceCount().
    2. **TZ-граница (frozen clock, паттерн warranty.test.ts)**: посади устройство с гарантией до «сегодня» (по MSK) → входит в w30 И w60, не в expired; до «сегодня+60» → в w60, не в w30, не expired; до «сегодня+61» → ни в одном пресете и не expired; до «сегодня-1» → только expired; NULL-гарантия не матчится ни в одном пресете. Здесь же nearestExpiringWarranties: порядок скоро-первым; два устройства с одинаковой датой — по возрастанию id (tiebreaker); истёкшие и NULL исключены; limit усекает (6+ в окне → ровно limit).
    3. **Zero-default**: на пустой базе deviceCountByType() и deviceCountByStatus() возвращают пустой массив, totalDeviceCount() = 0; Map-lookup с дефолтом ноль по DEVICE_TYPES/DEVICE_STATUS_KEYS даёт все восемь нулей (keystone гарантирует полноту тайлов).
    4. **Лента**: два события с одинаковым occurredAt — выше то, у которого больше id (устойчивость к tie); 12 событий → ровно 10, новейшие, порядок сохранён; null-слоты выживают (from_repair через returnFromRepair или raw-insert received — оба имени null); model/serialNumber в строке принадлежат устройству события (join корректен); fromId/toId присутствуют и равны id участников при непустых именах; movementEventLabel покрывает все 7 типов событий с raw-value fallback для неизвестного значения.
    Команда файла — быстрая: только этот файл на этапе задачи, полный suite перед завершением плана.
  </action>
  <verify>
    <automated>npx vitest run tests/dashboard-queries.test.ts && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/dashboard-queries.test.ts` и `grep -c "describe\|it(" tests/dashboard-queries.test.ts` >= 8
    - `npx vitest run tests/dashboard-queries.test.ts` exit 0 — все новые кейсы зелёные
    - `npx vitest run` exit 0 — полный suite зелёный (новые тесты + базовая линия ~278)
    - `grep -c "listDevices" tests/dashboard-queries.test.ts` >= 4 (parity против публичного API фильтра: типы, статусы, пресеты)
    - `grep -c "listRecentMovements" tests/dashboard-queries.test.ts` >= 1; `grep -ci "vi.setSystemTime\|useFakeTimers\|setSystemTime" tests/dashboard-queries.test.ts` >= 1 (frozen clock для границы)
  </acceptance_criteria>
  <done>Parity: любой дрейф предиката счётчика от фильтра роняет тест громко. Граница: инклюзивное w30/w60/expired поведение зафиксировано на frozen MSK-полуночи. Zero-default: пустая база даёт восемь нулей. Лента: tie-порядок, лимит, null-слоты, id в выборке — всё под ассертами.</done>
</task>

<task type="auto">
  <name>Task 3: smoke-dashboard.mjs — периметр, render и deep-link пробы дашборда</name>
  <files>scripts/smoke-dashboard.mjs</files>
  <read_first>
    - scripts/smoke-devices.mjs :1-140 — харнесс для клона: temp DB + применение миграций из SQL-файлов, спавн next start на фиксированном порту с DATABASE_PATH/AUTH_SECRET, mint jose-cookie (шаг 4), периметр-фетч (шаг 5-паттерн 200+probe), cleanup always
    - scripts/smoke-custody.mjs — второй живой пример того же паттерна (проверка структуры/очередности ассертов)
    - scripts/smoke-devices.mjs шаг 8 в редакции 06-01 (что уже ассертится о «/» — не дублировать один-в-один, а расширять гарантийными семействами)
    - .planning/phases/06-dashboard/06-RESEARCH.md — Validation Architecture (smoke-строка D-01/D-03), Pitfall 8 (пустая лента — норма свежей БД)
    - .planning/phases/06-dashboard/06-UI-SPEC.md — Copywriting (строки «Дашборд», «Всего:», счётчиков, пустой ленты)
    - app/(app)/page.tsx — фактические маркеры после 06-02 Task 1 (точные подписи счётчиков и пресетов)
  </read_first>
  <action>
    Создай scripts/smoke-dashboard.mjs — клон харнессы smoke-devices.mjs (шаги 1-4: temp-каталог, миграции из drizzle-SQL, спавн production-сервера на СВОЁМ порту в :31xx-диапазоне, например 3119, minted cookie) с ассертами:
    1. Периметр: GET / БЕЗ cookie → 307 с Location на /login.
    2. Render: GET / с cookie → 200 и HTML содержит: «Дашборд» (заголовок), «Всего:» (некликабельная голова), пункт nav «Дашборд», все три подписи гарантийных счётчиков («Истекает ≤ 30 дней:», «Истекает ≤ 60 дней:», «Истекла:»), саб-хедер «Ближайшие сроки», заголовок блока ленты «Последние перемещения».
    3. Deep links: в HTML присутствуют href-значения всех трёх семейств — тайлы типов (фильтр по type-ключу), тайлы статусов (фильтр по status-ключу), гарантийные пресеты w30/w60/expired — достаточно по одному маркеру на семейство (маркер ищи по query-фрагменту вида «type=», «status=», «warranty=w» + «expired», формируемому buildDevicesQuery, а не по полной строке).
    4. Свежая temp-БД (Pitfall 8): «Всего: 0 устройств» валидно; пустая лента ожидаема — «Перемещений пока нет» присутствует; счётчики рендерятся с нулями (строки счётчиков существуют при «: 0»).
    Любой провал — exit 1 с читаемым сообщением; сервер и temp-каталог чистятся всегда (паттерн оригинала). Скрипт НЕ заменяет обновлённый шаг 8 smoke-devices.mjs (тот уже в 06-01) — это отдельная фазовая проверка полного экрана.
  </action>
  <verify>
    <automated>npm run build && node scripts/smoke-dashboard.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/smoke-dashboard.mjs` и `node scripts/smoke-dashboard.mjs` exit 0 после `npm run build`
    - `grep -c "3119\|31[0-9][0-9]" scripts/smoke-dashboard.mjs` >= 1 (свой порт из :31xx-диапазона)
    - `grep -c "warranty=w" scripts/smoke-dashboard.mjs` >= 1 и `grep -c "expired" scripts/smoke-dashboard.mjs` >= 1 (гарантийные deep-link маркеры присутствуют)
    - `grep -c "Перемещений пока нет" scripts/smoke-dashboard.mjs` >= 1 (пустая лента — ассерт, не сюрприз)
    - повторный запуск `node scripts/smoke-dashboard.mjs` тоже exit 0 (temp-каталоги не конфликтуют)
  </acceptance_criteria>
  <done>Периметр и render-путь дашборда доказаны на production build: без cookie — переадресация на вход, с cookie — полный экран с deep-link href всех трёх семейств фильтров; свежая база даёт нули и честную пустую ленту.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → / (RSC) | Аутентифицированный GET; страница параметров не читает; счётчики — серверные числа |
| «Гарантия» → /devices (counter links) | Hrefs — константы buildDevicesQuery с гарантийным пресетом; целевой список серверно ревалидирует |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | `/` (RSC page) | high | mitigate | Тот же двойной слой: proxy default-deny + requireSession первой строкой; smoke-dashboard ассертит 307 /login без cookie (canon — breadcrumb: /gsd-secure-phase) |
| T-06-02 | Tampering (XSS) | модели/имена/даты топ-5 | medium | mitigate | React auto-escaping; без dangerouslySetInnerHTML; hrefs карточек — шаблонные литералы над числовыми id из БД |
| T-06-03 | Tampering | фальсификация гарантийного состояния | medium | mitigate | Запрет второй математики (P2): только warrantyPresetCounts (операторы предиката) + WarrantyDate(today); parity-тест против listDevices делает дрейф громким красным |
| T-06-04 | Information Disclosure | IDOR-зондаж ссылок топ-5 | low | accept | Вне скоупа: карточные маршруты уже guarding (requireSession + zod + notFound); id устройств уже открыты в списках фаз 3-5 |
| T-06-SC | Tampering | npm-инсталлы | — | n/a | Ноль новых пакетов (Package Legitimacy Audit RESEARCH: none) |
</threat_model>

<verification>
- `npx vitest run tests/dashboard-queries.test.ts` зелёный (parity, граница, zero-default, лента); `npx vitest run` зелёный целиком
- `npm run build && node scripts/smoke-dashboard.mjs && node scripts/smoke-devices.mjs` — оба smoke exit 0
- Счётчик «≤ 60» равен total списка /devices с тем же пресетом при живой проверке на seeded dev-базе (ручная проба verify-work: клик по счётчику открывает отфильтрованный список с тем же числом)
</verification>

<success_criteria>
- DASH-02: блок «Гарантия» — три счётчика в фиксированном порядке 30 → 60 → истекла, каждый ведёт в готовый пресет фазы 5; топ-5 ближайших живых гарантий (ASC + id-tiebreaker, w60-окно) с WarrantyDate-датами; пустые состояния правдивы
- D-04 структурно: счётчики составлены из операторов предиката в том же модуле; parity-тесты (DASH-01/DASH-02) прикалывают счётчик к total фильтра; один today на рендер делится счётчиками и датами
- Wave-0 fixtures 06-VALIDATION закрыты: parity, feed ordering, TZ boundary; smoke-dashboard добавлен, оба smoke зелёные
</success_criteria>

<output>
Create `.planning/phases/06-dashboard/06-02-SUMMARY.md` when done
</output>
