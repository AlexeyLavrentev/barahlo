---
phase: 05-search-filters
plan: 05
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - tests/devices-perf.test.ts
  - scripts/seed.mjs
autonomous: true
requirements: [UI-03]
must_haves:
  truths:
    - "(UI-03, edge 13) the FULL combined filtered query (q + type + status + department + warranty + RAM over the joined tables) at ≥500 seeded rows completes in milliseconds — an automated timing assertion with a generous 200 ms ceiling (research measured 0.9 ms @ 500; the assertion exists to catch accidental N+1s/unindexed regressions, not to benchmark)"
    - "(UI-03) pagination stays server-side (LIMIT/OFFSET inside listDevices) — never client-side; the page payload is ≤ pageSize rows regardless of the total"
    - "(UI-SPEC populated) the seeded dev database carries ~400 devices (laptop 200 / monitor 80 / dock 50 / peripheral 70) so human/UAT paging, filtering and search feel the real scale, with all four warranty states naturally present and homoglyph-friendly Cyrillic models searchable"
    - "(seed discipline) the expanded seed stays deterministic (mulberry32), refuses production and refuses a non-empty database — reseeding the dev DB requires deleting data/app.db first (idempotent-by-refusal guard preserved)"
  artifacts:
    - path: tests/devices-perf.test.ts
      provides: "in-test bulk seed (≥500 rows, deterministic, temp DB) + wall-time assertion of the full combined listDevices call + total/rows consistency at scale"
    - path: scripts/seed.mjs (extended)
      provides: "DEVICE_COUNTS at production-like scale (~400) with warranty-state spread and Cyrillic model coverage for UAT"
  key_links:
    - from: "tests/devices-perf.test.ts"
      to: "listDevices with the full DeviceListFilters (plan 02 predicates)"
      via: "temp SQLite seeded in-test, repeated timed runs"
      why: "UI-03 («сотни устройств — мгновенно») must be an automated regression gate, not a feeling"
    - from: "scripts/seed.mjs DEVICE_COUNTS"
      to: "the /devices registry at human-verified scale"
      via: "DATABASE_PATH-temp run of the seed for automated verification"
      why: "end-of-phase human-check pages/filters the real thing"
  prohibitions:
    - statement: "MUST NOT make the timing assertion flaky-tight — the ceiling stays generous (200 ms vs measured 0.9 ms); a perf test that fails on CI noise trains people to ignore it (RESEARCH A6)"
      category: values
      status: resolved
      verification: judgment
---

<objective>
Prove UI-03 — lists stay instant at hundreds of devices — with an automated perf regression gate over the full combined filter query, and scale the dev seed to ~400 devices so humans verify the phase at real scale.

Purpose: «всё это летает на сотнях устройств» is a success criterion, not a hope; the 0.9 ms measurement becomes a permanent tripwire against N+1 regressions.
Output: tests/devices-perf.test.ts green, seed.mjs at 200/80/50/70, UAT-ready dev fixtures (after the documented reseed).
</objective>

<execution_context>
@/Users/aleksey/.zcode/gsd-core/workflows/execute-plan.md
@/Users/aleksey/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-search-filters/05-RESEARCH.md
@.planning/phases/05-search-filters/05-VALIDATION.md
@.planning/phases/05-search-filters/05-02-SUMMARY.md
@scripts/seed.mjs
@tests/devices-queries.test.ts
@tests/helpers.ts
@db/queries/devices.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Perf regression gate — full combined filter at 500 rows under 200 ms</name>
  <files>tests/devices-perf.test.ts</files>
  <read_first>
    tests/devices-queries.test.ts lines 1-25 (the harness discipline: DATABASE_PATH before @/db import, dynamic imports, applyMigrations(db.$client))
    tests/helpers.ts (insertDevice + applyMigrations — the raw-insert shape to scale up)
    db/queries/devices.ts (listDevices after plan 02 — the full filters object; the covers batch that must not regress)
    05-RESEARCH.md §Validation Architecture (UI-03 measurement approach: 300–500 in-test seed, generous ceiling, catch-N+1 intent) and A6
    05-VALIDATION.md Wave 0 requirements (the perf timing test is one of the three fixtures)
  </read_first>
  <action>
    Create tests/devices-perf.test.ts on the existing temp-SQLite harness: seed ≥500 devices IN-TEST (deterministic loop of raw parameterized inserts mirroring helpers.insertDevice but varied — rotate type_key across all four, status across all four, warrantyUntil across null/past/warn-band/far-future UTC midnights relative to Date.now(), ramUpgraded across null/0/1, ~40 devices assigned to a handful of employees across ≥2 departments, serials like `PERF-{i}-AB`, models mixing Cyrillic and Latin including 'Тестовая модель {i}') — then time listDevices with the FULL combination (filters: { q: 'PERF', type: 'laptop', status: 'assigned', departmentId: <seeded>, warranty: 'w60', ramNoUpgrade: true }) over ~200 runs and assert the AVERAGE wall time < 200 ms (and no single run > 1000 ms as an outlier guard). Also assert correctness at scale so the timing is honest: total > 0, rows.length ≤ pageSize, page/pages math consistent (total equals a full unpaged count via exportDevices with the same filters — plan 04's query; skip if not yet merged, fall back to a raw SQL count with the same WHERE). Warm up once before timing (JIT/pragma cache) so the assertion measures steady state.
  </action>
  <verify>
    <automated>npx vitest run tests/devices-perf.test.ts</automated>
  </verify>
  <done>the full combined filter over ≥500 rows averages well under the 200 ms ceiling (expected single-digit ms), correctness assertions pass, and the test fails loudly if a future change introduces an N+1 or drops an index — the UI-03 automated gate exists</done>
</task>

<task type="auto">
  <name>Task 2: Seed at production-like scale — ~400 devices with full warranty spread</name>
  <files>scripts/seed.mjs</files>
  <read_first>
    scripts/seed.mjs (guards lines 8-24 — production + non-empty-DB refusal; DEVICE_COUNTS line 147; the deterministic mulberry32 core; the warranty spread at line 106 purchaseDate + pick([1,2,3]) * YEAR; the Cyrillic model pool around line 64 'Ugreen 9-в-1')
    05-PATTERNS.md §scripts/seed.mjs (the only edit is the counts line; spread verification expectations)
    05-CONTEXT.md §Claude's Discretion (Seed: расширить до сотен устройств для приёмки UI-03 — сейчас 80)
    05-UI-SPEC.md UI Considerations «populated — hundreds of devices (UI-03)»
  </read_first>
  <action>
    Change DEVICE_COUNTS to { laptop: 200, monitor: 80, dock: 50, peripheral: 70 } (~400 total). Nothing else scales by hand — serials stay unique via the run number, departments/employees/movements keep their existing multipliers. Then verify the spread AUTOMATEDLY against a TEMP database (never touch the dev DB): run DATABASE_PATH=<mktemp dir>/seed.db node scripts/seed.mjs and assert via a quick sqlite query (node -e or a throwaway check inside the run) that devices total ≈ 400, that at least one device falls in each warranty state bucket relative to today (expired: warranty_until < today; warn band: today ≤ warranty_until ≤ today+60; ok: beyond; none: NULL) — if the warn band (or any bucket) lands empty under the current purchase-date distribution, nudge the deterministic distribution (e.g. include some purchase dates ~11–12 months back so 1-year warranties land inside the 60-day window) keeping mulberry32 determinism, and re-run; and that Cyrillic-containing models exist for homoglyph search UAT (e.g. the existing 'Ugreen 9-в-1' pattern). Leave the production/non-empty guards byte-identical.
    Note for the operator (goes in the task's done report, not code): existing dev databases keep 80 devices until data/app.db is deleted and the seed re-run — end-of-phase human-check includes one reseed.
  </action>
  <verify>
    <automated>TMP_SEED="$(mktemp -d)/seed.db" && DATABASE_PATH="$TMP_SEED" node scripts/seed.mjs && sqlite3 "$TMP_SEED" "SELECT count(*) FROM devices;" | grep -E "^3[0-9]{2}$|^(4[0-9]{2})$" && sqlite3 "$TMP_SEED" "SELECT count(*) FROM devices WHERE warranty_until IS NOT NULL AND warranty_until < strftime('%s','now');" | grep -vE "^0$" && sqlite3 "$TMP_SEED" "SELECT count(*) FROM devices WHERE model LIKE '%-в-%' OR model LIKE '%сер%' OR model GLOB '*[А-Яа-яё]*';" | grep -vE "^0$" && echo SEED-SCALE-OK</automated>
  </verify>
  <done>the seed produces ~400 devices (200/80/50/70) into a clean database, expired and ok warranty buckets are provably non-empty (warn band verified non-empty during development — nudge the distribution if not), Cyrillic-searchable models exist, guards untouched; the dev DB itself is not modified by this task</done>
  <reversibility rating="costly">taking effect on the dev machine requires deleting data/app.db and reseeding (the guard refuses non-empty DBs by design) — a deliberate one-time reset of dev fixtures, flagged for the end-of-phase human-check.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | test fixture + dev seed script; no new runtime surface, no new input path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-13 | DoS (self-inflicted) | perf test runtime on CI | low | accept | 500-row temp DB + 200 timed runs is sub-second; the generous ceiling avoids retry storms — accepted with the flakiness prohibition above |
| T-05-14 | Tampering (environment) | seed script running against production | high | mitigate | the existing double guard is preserved byte-identical: NODE_ENV=production refusal AND non-empty-DB refusal → exit 1 (phase 1 D-11); this task verifies the guards still fire |
| T-05-SC | Tampering | npm installs | none | accept | zero new packages this phase — no supply-chain surface |
</threat_model>

<verification>
- npx vitest run tests/devices-perf.test.ts — timing + correctness at scale
- DATABASE_PATH=<temp> node scripts/seed.mjs + sqlite3 counts — scale and warranty spread proven without touching the dev DB
- npx vitest run — whole suite green
- Manual-only (end-of-phase): delete dev data/app.db, reseed, page/filter/search /devices by hand — «мгновенно» at ~400 rows (05-VALIDATION.md manual checks)
</verification>

<success_criteria>
- The full combined filter query is machine-proven instant at ≥500 rows with a permanent regression gate
- The dev seed renders the registry at real scale for human acceptance
- Server-side pagination semantics hold at scale (≤ pageSize rows per page, totals consistent)
</success_criteria>

## Artifacts this phase produces

- `tests/devices-perf.test.ts` (in-test ≥500-row bulk seed + 200 ms-capped timing assertion)
- `scripts/seed.mjs` DEVICE_COUNTS → { laptop: 200, monitor: 80, dock: 50, peripheral: 70 } with verified warranty-state spread

<output>
Create `.planning/phases/05-search-filters/05-05-SUMMARY.md` when done
</output>
