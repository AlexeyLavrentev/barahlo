---
phase: 03-device-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/device-schema.ts
  - db/queries/devices.ts
  - app/(app)/devices/page.tsx
  - app/(app)/devices/actions.ts
  - app/(app)/devices/device-dialog.tsx
  - app/(app)/devices/loading.tsx
  - app/(app)/devices/error.tsx
  - app/layout.tsx
  - app/(app)/page.tsx
  - components/ui/select.tsx
  - components/ui/checkbox.tsx
  - tests/device-schema.test.ts
  - tests/devices-queries.test.ts
  - scripts/smoke-devices.mjs
autonomous: true
requirements: [REG-01, REG-02]
gap_closure: false
must_haves:
  truths:
    - "Список /devices: двухстрочные строки (модель+тип / серийник·инвентарник·держатель·статус), фильтр ?type= валидируется против 4 типов (чужое значение = все), пагинация 20 с clamp и full-query-string links (REG-01, Pattern 1)"
    - "Диалог «Добавить устройство»: select типа рендерит пер-типовые поля из device_schema; смена типа сбрасывает пер-типовые значения (key={typeKey}); серийник обязателен у всех типов, подсказка D-04 у периферии (REG-02, D-01/D-04)"
    - "Создание пишет serialNormalized/inventoryNormalized через lib/normalize; пустой инвентарник → оба NULL; UNIQUE-коллизия → русский copy из UI-SPEC, не generic (D-16/D-17, Pitfall 1/5)"
    - "create-действие: requireSession() первой строкой, zod-whitelist строго по typeFields(typeKey)+общие (server-auth-actions, T-03-01/04), refresh() в конце (Pitfall 1)"
    - "Статус и держатель отсутствуют в zod-whitelist — подменить через payload нельзя (REG-04 граница фазы 4)"
    - "Крайние случаи probe-граней REG-02: adjacency — серийник нормализуется до сравнения (С123=с123 не создаёт дубль); empty — пустой инвентарник → NULL-пара; ordering — device_schema порядок полей = UI-SPEC field table; idempotency — двойной submit невозможен (pending-disable), повторное создание с тем же серийником → UNIQUE copy; concurrency — resolve-в-транзакции не нужен (нет зависимых insert), UNIQUE остаётся арбитром"
  artifacts:
    - path: "lib/device-schema.ts"
      provides: "DEVICE_TYPES (4 типа, ru-имена), typeFields(typeKey), buildZodSchema(typeKey) — единственный источник полей (D-02)"
    - path: "db/queries/devices.ts"
      provides: "listDevices({type,page,pageSize}) → {rows,total,page,pages}; getDevice(id); createDevice; updateDevice — normalized-запись + UNIQUE catch"
    - path: "app/(app)/devices/*"
      provides: "список + диалог + loading/error границы"
    - path: "tests/device-schema.test.ts + tests/devices-queries.test.ts"
      provides: "schema-контракт + queries с UNIQUE/NULL-инвентарником"
  key_links:
    - from: "app/(app)/devices/device-dialog.tsx"
      to: "lib/device-schema.ts"
      via: "typeFields(typeKey) рендер + buildZodSchema-совместная валидация"
      pattern: "device_schema"
    - from: "app/(app)/devices/actions.ts"
      to: "lib/normalize.mjs"
      via: "normalizeSerial/normalizeInventory до записи"
      pattern: "normalize"
  prohibitions:
    - statement: "MUST NOT быть поля статуса/держателя в zod-whitelist или форме (REG-04 — только custody-действия фазы 4)"
      category: boundary
      verification: test
    - statement: "MUST NOT использовать drizzle-kit push (новых миграций нет; push роняет UNIQUE)"
      category: safety
      verification: test
    - statement: "MUST NOT быть клиентской пагинации или клиентского поиска (D-05, серверные списки)"
      category: scope
      verification: test
---

<objective>
**User story.** **Как** руководитель, **я хочу** вносить технику четырёх типов в реестр с полями своего типа и видеть пагинированный список с фильтром по типу, **чтобы** данные устройств попали в систему в структурированном виде — фундамент фаз 4–6 (REG-01, REG-02; D-01..D-06).
</objective>

<execution_context>
@/Users/aleksey/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<artifacts_section>
## Artifacts this phase produces (доля этого плана)
Новые: lib/device-schema.ts, db/queries/devices.ts, app/(app)/devices/{page,actions,device-dialog,loading,error}, components/ui/{select,checkbox} (shadcn-генерация), tests/{device-schema,devices-queries}.test.ts, scripts/smoke-devices.mjs.
Изменённые: app/layout.tsx (nav «Устройства»), app/(app)/page.tsx (redirect /devices).
Символы: DEVICE_TYPES, typeFields(), buildZodSchema(), listDevices(), getDevice(), createDevice(), updateDevice(), createDeviceAction(), updateDeviceAction().
</artifacts_section>

<context>
@.planning/phases/03-device-registry/03-CONTEXT.md
@.planning/phases/03-device-registry/03-RESEARCH.md
@.planning/phases/03-device-registry/03-PATTERNS.md
@.planning/phases/03-device-registry/03-UI-SPEC.md
</context>

<tasks>

<task type="tracer">
  <name>Tracer-срез: device_schema + queries + список + диалог создания → устройство в списке end-to-end</name>
  <files>lib/device-schema.ts, db/queries/devices.ts, app/(app)/devices/*, app/layout.tsx, app/(app)/page.tsx, components/ui/select.tsx, components/ui/checkbox.tsx, tests/*, scripts/smoke-devices.mjs</files>
  <read_first>db/queries/employees.ts (пагинация/clamp/RU-sort/join — дословный шаблон); app/(app)/employees/page.tsx + actions.ts + employee-dialog.tsx (форма действия, guard-first, useActionState, per-open inner component из WR-01); .planning/phases/03-device-registry/03-UI-SPEC.md (two-line row, копи-таблица, per-type field table — канон содержания device_schema); .planning/phases/03-device-registry/03-RESEARCH.md (D1–D6, vercel-правила); lib/normalize.mjs (сигнатуры); tests/helpers.ts; node_modules/next/dist/docs/ (page.md — searchParams Promise; server-actions.md — refresh)</read_first>
  <action>
Сначала tests/device-schema.test.ts + tests/devices-queries.test.ts (RED), затем код (GREEN).

1. `npx shadcn@latest add select checkbox` (официальный реестр, санкция чекпоинта фазы 2; если select уже есть — skip). Проверить отсутствие barrel-импортов в сгенерированном.
2. `lib/device-schema.ts` по 03-RESEARCH D1: DEVICE_TYPES as const (laptop/monitor/dock/peripheral + ru-имена), typeFields() — поля строго из UI-SPEC field table (label ru, type, required, group, options для peripheralKind: мышь/клавиатура/гарнитура/веб-камера/прочее (фиксированный список — D-03, не свободный текст) и panelType: IPS/TN/VA/OLED), buildZodSchema(typeKey): required → строгие min/positive, optional → optional()/nullable(); кастомных трансформаций нет (trim делает action). Иммутабельно, без Next-импортов.
3. `db/queries/devices.ts`: listDevices({type,page,pageSize}) — join employees для держателя, where typeKey (если type ≠ 'all'), orderBy ruSort(model) + id tiebreaker, count+clamp, {rows,total,page,pages}; getDevice(id); createDevice({typeKey, ...общие, ...пер-типовые}) — normalized-запись (lib/normalize), пустой инвентарник → NULL-пара (Pitfall 5), UNIQUE catch SQLITE_CONSTRAINT_UNIQUE → throw {code} для маппинга в action; updateDevice(id, {...}) — тот же normalized-пересчёт (Pitfall 2), existence-check → false.
4. `app/(app)/devices/actions.ts`: createDeviceAction/updateDeviceAction — requireSession() первой строкой; typeKey валидируется против DEVICE_TYPES до полей (js-early-exit); zod = общая схема (модель 1..200, серийник 1..100, инвентарник опц. 1..80, дата/цена/поставщик/гарантия/заметки) + buildZodSchema(typeKey) merge; WHITELIST: статус/держатель/typeKey(в edit) не читаются вообще; маппинг fieldErrors по UI-SPEC копи; UNIQUE-код → копи «Такой серийный номер уже существует»/«Такой инвентарный номер уже существует»; refresh() перед return.
5. `app/(app)/devices/page.tsx`: guard-first RSC, searchParams Promise → type валиден иначе 'all', page clamp; two-line rows (Link на (card)/devices/[id] — маршрут появится в плане 02, строка пока без перехода на несуществующий id — href оставляем, 404 даст не-found страница); dropdown-фильтр типа (client wrapper над select, push в URL через router.replace — или Link-вариант: выбрать по паттерну select из shadcn); пустые состояния по копи-таблице; loading.tsx (h-[60px] скелетоны ×5), error.tsx (retry-проп).
6. `app/(app)/devices/device-dialog.tsx`: client island; useActionState; общие поля + пер-типовые из typeFields(typeKey), контейнер пер-типовых с key={typeKey} (сброс без useEffect — vercel rerender-derived-state-no-effect); create-режим (edit — план 02); hidden departmentName-урок фазы 2 учтён: select управляемый явно; сериализация пропсов — плоские конфиги, без rows (server-serialization).
7. `app/layout.tsx`: nav-ссылка «Устройства» + active-state; `app/(app)/page.tsx` → redirect('/devices').
8. `scripts/smoke-devices.mjs` по образцу smoke-employees: minted cookie → 307 без cookie на /devices; 200 + seeded-модель с cookie; создание через Server Action НЕ эмулируем (интерактив — UAT), но проверяем список/периметр/redirect.
  </action>
  <behavior>
    tests/device-schema.test.ts: typeFields('laptop') содержит ровно ramGb/ramUpgraded/ssdGb; peripheralKind options = 5 фиксированных; buildZodSchema('monitor') отвергает ramGb-ключи (whitelist-проверка через safeParse строгого объекта); required-поля с пустой строкой → fail.
    tests/devices-queries.test.ts: createDevice пишет оба normalized (в т.ч. гомоглиф С123→C123 — дубль-проверка через второй create → UNIQUE throw {code}); пустой инвентарник → NULL/NULL; updateDevice пересчитывает normalized; listDevices фильтр по типу + clamp page + RU-сортировка модели (Ё-тест).
  </behavior>
  <verify>
    <automated>npx vitest run && npm run build && node scripts/smoke-devices.mjs && node scripts/smoke-employees.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` полностью зелёный (весь набор + новые files, базлайн 65+)
    - `npm run build` exit 0; в route table присутствует /devices
    - smoke-devices exit 0: 307 без cookie, 200 + seeded-модель с cookie, redirect `/` → /devices (307 на / с Location /devices)
    - smoke-employees не регрессировал (exit 0)
    - `grep -c "status\|currentEmployeeId" ` по zod-схемам actions = 0 (whitelist-гейт)
    - two-line rows: `grep -c "h-\[60px\]" app/(app)/devices/loading.tsx` ≥ 1
  </acceptance_criteria>
  <done>Устройство создаётся через диалог (проверка UI — UAT), список фильтруется и пагинируется, normalized-инварианты под тестами, периметр цел.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Server Action POST → actions | напрямую POST-абельны, прокси не защищает |
| payload → SQL | typeKey/поля через zod-whitelist по типу |
| normalized-номера → UNIQUE | коллизии как бизнес-ошибка, не 500 |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Spoofing/Elevation | devices actions | high | mitigate | requireSession() первой строкой; PUBLIC_PATHS не расширяются |
| T-03-02 | Tampering | payload → SQL | medium | mitigate | zod-whitelist по typeFields(typeKey); drizzle-параметризация; status/holder не читаются |
| T-03-03 | Tampering | рендер значений | low | mitigate | React-экранирование; без innerHTML |
| T-03-04 | DoS | перебор page/type | low | accept | clamp + enum; один пользователь LAN |
</threat_model>

<verification>
- vitest весь зелёный; build green; оба smoke green
- grep-гейты: статус/держатель вне zod; normalized-поля пишутся в обоих мутациях (grep normalize в queries)
</verification>

<success_criteria>
- REG-01: список по типам с пагинацией работает (smoke + тесты)
- REG-02: создание устройства любого типа с полями типа (диалог рендерит из device_schema; e2e-интерактив — UAT)
- Инварианты normalized/UNIQUE под автоматическими тестами
</success_criteria>

<output>
Create `.planning/phases/03-device-registry/03-01-SUMMARY.md` when done
</output>
