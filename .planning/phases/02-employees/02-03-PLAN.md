---
phase: 02-employees
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - "app/(app)/employees/employee-dialog.tsx"
  - "app/(app)/employees/page.tsx"
  - "app/(app)/employees/[id]/page.tsx"
  - "app/(app)/employees/loading.tsx"
  - "app/(app)/employees/error.tsx"
  - "app/(app)/employees/[id]/loading.tsx"
  - "app/(app)/employees/[id]/error.tsx"
  - app/login/page.tsx
  - app/login/login-form.tsx
autonomous: true
requirements: [EMP-01, UI-01, UI-02]
must_haves:
  truths:
    - "Поле «Отдел» в диалоге — combobox: опции отделов фильтруются по мере ввода, выбранный пункт отмечен акцентной галочкой, триггер стилизован под input h-10 с placeholder «Выберите или введите отдел» (D-03)"
    - "Введено имя без точного совпадения → закреплён первый пункт «Создать „{ввод}“»: Enter/клик выбирает его, фактическое создание отдела делает сервер в транзакции (гонка UNIQUE переиспользует существующий — пользователь никогда не видит ошибки дубля) (D-03)"
    - "Пустой ввод combobox показывает полный список отделов — dropdown никогда не бывает мёртвым (UI-SPEC)"
    - "Переключатель «Активные / Архив» — сегмент-контрол с белой активной пилюлей (не акцентный цвет), по умолчанию «Активные»; переключение сбрасывает страницу на 1 (D-02)"
    - "Пагинация: «Назад»/«Далее» тихие кнопки (граница — приглушение opacity-40) + «Страница N из M»; ссылки всегда несут полный query-string — фильтр не теряется (D-01)"
    - "Пустой «Активные» → «Пока нет сотрудников» + «Добавьте первого сотрудника — понадобится имя и отдел.» (CTA виден); пустой «Архив» → «Архив пуст» + «Уволенные сотрудники появятся здесь.» (UI-SPEC)"
    - "Загрузка списка и карточки показывает 5 скелет-строк h-11 bg-black/5 rounded-lg в белой карточке — без сдвига раскладки (UI-SPEC loading)"
    - "Сбой списка/карточки — error-граница: «Не удалось загрузить список» + кнопка «Попробовать снова», перезапускающая сегмент; внутренние детали ошибки — только в console (V7)"
    - "Экран входа нормализован к контракту: вес 400/600, шкала отступов без полушагов, акцентный фокус-ринговый стиль, primary-кнопка акцентная, копи не менялось (UI-02, UI-01)"
    - statement: "Длинные имя+отдел (100/80 символов) обрезаются с многоточием, не меняя высоту строки и не ломая max-w-md диалога — удерживаемый визуальный тест на UAT"
    verification: backstop
    - statement: "Apple-эстетика (воздух, типографика, пресс-фидбек, дисциплина акцента: только CTA/фокус/галочка combobox) сверяется глазами с UI-SPEC и skill apple-design — субъективное свойство, не авто-ассерт"
    verification: backstop
  artifacts:
    - "employee-dialog.tsx — combobox-режим поля «Отдел» (D-03): фильтрация, галочка, закреплённый «Создать „X“», hidden departmentName"
    - "app/(app)/employees/page.tsx + [id]/page.tsx — передают departments={listDepartments()} в диалог; сегмент-пилюля, пустые состояния, buildQuery-хелпер"
    - "loading.tsx ×2 — скелетоны; error.tsx ×2 — границы с повтором (проп retry этой версии Next)"
    - "app/login/{page,login-form}.tsx — нормализованы к UI-SPEC без изменения копи"
  key_links:
    - "page/[id] → listDepartments() → EmployeeDialog(departments) → combobox → hidden departmentName → create/update action → resolveDepartmentId (уже гонко-безопасен)"
    - "error.tsx → props { error, retry } → onClick retry() (эта версия Next: имя пропа изменилось против до-16-эры)"
    - "Сегмент → Link с buildQuery({filter, page:1}) → listEmployees(filter) — единый строитель query-string"
  prohibitions:
    - "Нигде нет удаления сотрудника (EMP-03); греп-гейты 02-02 продолжают действовать"
    - "Никаких drizzle-kit push; схема не меняется"
    - "Никакой клиентской пагинации и никакого поиска по имени в списке (D-01, D-05 — поиск придёт в Фазе 5)"
---

<objective>
Закрыть остатки контракта фазы: combobox отдела с инлайн-созданием (D-03), сегмент-фильтр «Активные/Архив» с пустыми состояниями (D-02), loading/error-границы и нормализация экрана входа — после чего весь набор экранов фазы выдержан в одном визуальном языке (UI-01, UI-02).

Purpose: Это последний вертикальный слой среза 02-01: интерфейс становится полным по UI-SPEC (все 9 covered-состояний + backstop), а экран входа — первый экран пользователя — приводится к тому же контракту.

Output: Combobox в диалоге, сегмент-контрол с пустыми состояниями, 4 boundary-файла, нормализованный login, финальные греп-гейты дизайна.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-employees/02-01-SUMMARY.md
@.planning/phases/02-employees/02-02-SUMMARY.md
@.planning/phases/02-employees/02-CONTEXT.md
@.planning/phases/02-employees/02-UI-SPEC.md
@.planning/phases/02-employees/02-RESEARCH.md
@.planning/phases/02-employees/02-PATTERNS.md
@/Users/aleksey/.zcode/skills/apple-design/SKILL.md
@app/(app)/employees/employee-dialog.tsx
@app/(app)/employees/page.tsx
@app/login/page.tsx
@app/login/login-form.tsx
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Отдел-combobox с инлайн-созданием «Создать „X“» (D-03)</name>
  <files>"app/(app)/employees/employee-dialog.tsx", "app/(app)/employees/page.tsx", "app/(app)/employees/[id]/page.tsx"</files>
  <read_first>.planning/phases/02-employees/02-01-SUMMARY.md (выбор композиции combobox в Task 2 — нативный или popover+command; API db/queries.listDepartments), .planning/phases/02-employees/02-UI-SPEC.md (строка «Отдел combobox (D-03)», UI Considerations: empty combobox, long-text trigger), .planning/phases/02-employees/02-RESEARCH.md (Pattern 3 — гонко-безопасный резолв; A1 — проверка API примитива), components/ui/combobox.tsx (сгенерирован в 02-01; при фолбэке — popover.tsx/command.tsx), lib/ru.ts (ruCollator), "app/(app)/employees/employee-dialog.tsx", "app/(app)/employees/page.tsx", "app/(app)/employees/[id]/page.tsx"</read_first>
  <action>Заменить текстовый Input отдела в employee-dialog.tsx на combobox-примитив из 02-01 (композиция зафиксирована в 02-01-SUMMARY: нативный Base UI combobox либо popover+command — следовать суммари, не пере-решать). Поведение по UI-SPEC: триггер стилизован как input h-10 rounded-lg с placeholder «Выберите или введите отдел»; опции — проп departments (список {id, name}), отсортированный на клиенте ruCollator из lib/ru; встроенная фильтрация по мере ввода; выбранный пункт — акцентная галочка (check 16px #0071E3) и подсветка строки (акцент здесь — третий разрешённый случай из UI-SPEC); текст триггера с truncate и атрибутом title (consideration long-text); пустой ввод показывает полный список (никогда не мёртвый dropdown). Введённый текст без точного совпадения → закреплённый ПЕРВЫЙ пункт «Создать „{ввод}“» (кавычки-ёлочки с немецкими нижними по копи-таблице): Enter или клик выбирает его; выбор ТОЛЬКО записывает typed-имя в hidden input departmentName и метку триггера — фактическое создание отдела выполняет серверный resolveDepartmentId в существующей транзакции; проигранная гонка UNIQUE молча переиспользует существующий отдел, поэтому отдельной пользовательской ошибки «отдел уже существует» в UI НЕТ (задокументированная подмена строки копи-таблицы на однодорожечный резолв — RESEARCH Pattern 3, discretion D-03). Диалог получает проп departments: обе страницы — "app/(app)/employees/page.tsx" и "app/(app)/employees/[id]/page.tsx" — вызывают listDepartments() и передают его в EmployeeDialog (карточка — для edit-режима). Валидация departmentName не меняется (обрезка 1..80 в zod). Клавиатурная навигация/ARIA — из примитива, своего фокус-менеджмента не писать (Don't Hand-Roll).</action>
  <verify>
    <automated>npm run build && npx vitest run && grep -q 'Создать „' 'app/(app)/employees/employee-dialog.tsx' && grep -q 'listDepartments' 'app/(app)/employees/page.tsx' && grep -q 'listDepartments' 'app/(app)/employees/[id]/page.tsx' && grep -q 'ruCollator' 'app/(app)/employees/employee-dialog.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` зелёный; `npx vitest run` весь набор зелёный
    - employee-dialog.tsx содержит закреплённую опцию «Создать „« (подстрока копи-таблицы) и ruCollator для сортировки опций
    - Обе страницы передают departments из listDepartments() в диалог (grep по обоим файлам)
    - В employee-dialog.tsx нет самописного focus-trap/aria-мешанины — используется API примитива shadcn (код-ревью)
    - smoke-скрипт 02-02 остаётся зелёным (диалог не сломал клиентскую границу)
  </acceptance_criteria>
  <done>Отдел выбирается из списка с фильтром и создаётся инлайн («Создать „X“»), гонко-безопасно, по визуальному контракту UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Сегмент «Активные/Архив», пустые состояния, чистый строитель query-string (D-01, D-02)</name>
  <files>"app/(app)/employees/page.tsx"</files>
  <read_first>.planning/phases/02-employees/02-UI-SPEC.md (строки «/employees — list», «Pagination», Copywriting: пустые состояния; Visual Details: сегмент-анимация), .planning/phases/02-employees/02-RESEARCH.md (Pitfall 3 — потеря фильтра в ссылках; Pitfall 4 — нулевая страница после архивации), "app/(app)/employees/page.tsx" (свои ссылки фильтра/пагинации из 02-01), db/queries/employees.ts (кламп страницы уже в listEmployees)</read_first>
  <action>Довести список до полного контракта UI-SPEC. 1) Извлечь крошечный чистый хелпер buildQuery({filter, page}) в этом же файле (или рядом): единственное место сборки query-string; ВСЕ ссылки фильтра и пагинации идут через него — клик по «Архив» ведёт на ?filter=archive&page=1 (сброс страницы), «Далее»/«Назад» сохраняют текущий filter (Pitfall 3). 2) Сегмент-контрол: контейнер rounded-lg bg-black/5 p-1, два пункта-ссылки text-sm (14/400); активный пункт — белая пилюля bg-white rounded-lg shadow-sm (активное состояние — белая пилюля, НЕ акцентный цвет, UI-SPEC Color); смена — лёгкий переход ≤200ms ease-out, при prefers-reduced-motion — чистый кроссфейд (глобальные правила globals.css уже работают). По умолчанию активен «Активные» (D-02). 3) Пагинация: «Назад»/«Далее» — тихие кнопки-ссылки text-sm; на границе (page=1 / page=pages) — span с opacity-40 без href; подпись «Страница {N} из {M}» (text-sm, ink-secondary). 4) Пустые состояния (копи дословно): ноль строк при filter=active → заголовок «Пока нет сотрудников» + текст «Добавьте первого сотрудника — понадобится имя и отдел.», CTA «Добавить сотрудника» остаётся видимым; ноль при filter=archive → «Архив пуст» + «Уволенные сотрудники появятся здесь.». Нулевая страница после архивации последней строки уже невозможна: listEmployees клампит page (02-01, Pitfall 4). Поиск по имени НЕ добавлять (D-05).</action>
  <verify>
    <automated>npm run build && npx vitest run && grep -q 'Пока нет сотрудников' 'app/(app)/employees/page.tsx' && grep -q 'Архив пуст' 'app/(app)/employees/page.tsx' && grep -q 'buildQuery' 'app/(app)/employees/page.tsx' && grep -q 'Страница' 'app/(app)/employees/page.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` зелёный; `npx vitest run` зелёный
    - page.tsx содержит все четыре строки пустых состояний дословно («Пока нет сотрудников», «Добавьте первого сотрудника — понадобится имя и отдел.», «Архив пуст», «Уволенные сотрудники появятся здесь.»)
    - buildQuery определён и используется во всех ссылках фильтра/пагинации (grep -c 'href={buildQuery' ≥ 2)
    - Ссылка фильтра сбрасывает page=1 (читается в вызовах buildQuery)
    - Активный пункт сегмента — белый фон (класс bg-white), не акцентный токен
  </acceptance_criteria>
  <done>Список ведёт себя по контракту в четырёх состояниях: populated, пустой «Активные», пустой «Архив», пограничная пагинация — и фильтр больше никогда не теряется в ссылках.</done>
</task>

<task type="auto">
  <name>Task 3: Loading/error-границы, нормализация входа, финальный дизайн-гейт</name>
  <files>"app/(app)/employees/loading.tsx", "app/(app)/employees/error.tsx", "app/(app)/employees/[id]/loading.tsx", "app/(app)/employees/[id]/error.tsx", app/login/page.tsx, app/login/login-form.tsx</files>
  <read_first>.planning/phases/02-employees/02-UI-SPEC.md (UI Considerations: loading/error; Login normalization; Copywriting: «Не удалось загрузить список»/«Попробовать снова»), .planning/phases/02-employees/02-RESEARCH.md (Pitfall 2 — проп границы ошибок в этой версии называется retry, старое имя reset), .planning/phases/02-employees/02-PATTERNS.md (разделы про globals.css/login — что менять), /Users/aleksey/.zcode/skills/apple-design/SKILL.md (§14), node_modules/next/dist/docs/ (loading.md, error.md — перед написанием, по AGENTS.md), app/login/page.tsx, app/login/login-form.tsx, app/globals.css</read_first>
  <action>1) loading.tsx ×2 ('app/(app)/employees/' и '[id]/'): белая карточка rounded-2xl shadow-sm ring-1 с пятью скелет-строками h-11 bg-black/5 rounded-lg (animate-pulse допустим как opacity-кроссфейд, уважает reduced-motion) — без сдвига раскладки (UI-SPEC loading). 2) error.tsx ×2: 'use client'; props этой версии Next — { error, retry }; разметка: сообщение «Не удалось загрузить список» (text-base) + кнопка «Попробовать снова» (вторичная) с onClick={() => retry()}; console.error(error) в эффекте/теле — наружу детали не отдаются (V7). НЕ использовать до-16-е имя пропа границы (Pitfall 2 — проверка в критериях). 3) Нормализация входа (app/login/page.tsx + login-form.tsx): копи не трогать («Логин», «Пароль», «Войти», «Вход…», существующие строки ошибок); заменить все классы веса 500 на 600 (шкала весов 400/600); полушаговые отступы (классы с .5 у py/px/mt/mb — 6px/10px) заменить соседними значениями шкалы (8/12/16px); фокус полей — акцентный стиль: ring-2 ring-[#0071E3]/30 + border-color акцент (вместо чёрного фокуса фазы 1); primary-кнопка — акцентная заливка (bg var(--color-accent), hover #0077ED, active:scale-[0.97]) вместо тёмно-нейтральной; фон страницы и карточка уже на токенах/рецепте 02-01 (проверить, при необходимости заменить нейтральные классы на токены). 4) Финальный гейт по всему app/: ни одного класса веса 500, ни одного полушагового отступа (критерии ниже), беглая проверка на англоязычные лейблы в разметке (список частых слов в критериях) — 0 совпадений. 5) human-check (end-of-phase): визуальная сверка списка/карточки/диалогов/входа с UI-SPEC (отступы, типографика, дисциплина акцента — только CTA/фокус/галочка) и apple-design; входит в приёмку фазы.</action>
  <verify>
    <automated>npm run build && npx vitest run && [ "$(grep -rn 'font-medium' app/ | wc -l | tr -d ' ')" = "0" ] && [ "$(grep -rnE '(py|px|mt|mb)-[0-9]+\.5' app/ | wc -l | tr -d ' ')" = "0" ] && grep -q 'retry' 'app/(app)/employees/error.tsx' && grep -q 'Не удалось загрузить список' 'app/(app)/employees/error.tsx' && [ "$(grep -rnE '>(Add|Save|Delete|Cancel|Edit|Search|Loading|Close)<' app/ | wc -l | tr -d ' ')" = "0" ] && node scripts/smoke-employees.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` зелёный; `npx vitest run` весь набор зелёный; smoke-скрипт зелёный
    - grep -rn 'font-medium' app/ = 0; grep полушаговых отступов (py/px/mt/mb с .5) по app/ = 0
    - 'app/(app)/employees/error.tsx' и '[id]/error.tsx' содержат retry, «Не удалось загрузить список», «Попробовать снова»; и НЕ содержат до-16-е имя пропа (grep -c 'reset(' в обоих = 0)
    - Оба loading.tsx содержат ровно 5 скелет-строк (grep -c 'h-11 bg-black/5' = 5 в каждом)
    - Англоязычные лейблы в JSX-тексте: grep '>(Add|Save|Delete|Cancel|Edit|Search|Loading|Close)<' по app/ = 0
    - human-check (end-of-phase UAT): визуальная сверка с UI-SPEC — 9 covered-состояний и backstop long-text подтверждены глазами
  </acceptance_criteria>
  <done>Все состояния UI-SPEC реализованы (загрузка/ошибка/пусто/переполнение), экран входа приведён к контракту, финальные греп-гейты дизайна зелёные — фаза визуально целостна.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Браузер → Server Actions | Диалог с combobox шлёт произвольный departmentName (в т.ч. «создать») |
| URL → список | filter/page из query-string идут в SQL |
| error/loading границы | Внешний вид ошибок не должен утечь внутренностями |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Spoofing/Elevation | create/update actions (combobox-путь) | high | mitigate | requireSession() в действиях (02-01) не регрессирует; proxy PUBLIC_PATHS не расширяется (V14) |
| T-02-02 | Tampering | filter/page → SQL | medium | mitigate | enum-валидация filter, Number+clamp page, drizzle-параметризация (инвариант 02-01 сохраняется) |
| T-02-09 | Information Disclosure | error.tsx рендер деталей исключения | low | mitigate | UI показывает только договорную строку; console.error(error) — серверная консоль; никаких {error.message} в разметке |
| T-02-04 | Tampering | departmentName произвольной длины/содержимого | low | mitigate | zod: обрезка, 1..80; React-экранирование; параметризованный резолв по имени |
| T-02-SC | Tampering | — (установок в этом плане нет) | low | accept | Новых пакетов нет — чекпоинт 02-01 покроил фазу |

</threat_model>

<verification>
- `npx vitest run` — весь набор зелёный (инварианты данных не регрессировали)
- `npm run build` зелёный; smoke-скрипт зелёный
- Дизайн-гейты: 0 веса-500, 0 полушагов, 0 англо-лейблов; retry-границы на месте
- human-check (end-of-phase): визуальная приёмка по UI-SPEC + apple-design
</verification>

<success_criteria>
Контракт UI-SPEC выполнен целиком: combobox с инлайн-созданием, сегмент с пустыми состояниями, скелетоны и границы ошибок, нормализованный вход. UI-01 (русский) и UI-02 (Apple-эстетика) подтверждены гейтами и human-check; D-01..D-06 реализованы полностью.
</success_criteria>

<artifacts_this_phase_produces>
Полная инвентаризация символов фазы 02 — см. блок в 02-01-PLAN.md; этот план добавляет: combobox-режим поля «Отдел» с закреплённой опцией «Создать „X“»; buildQuery-хелпер; загрузочные/ошибочные границы ×2 маршрутов (conventions loading.tsx/error.tsx, проп retry); нормализованный app/login; сегмент-контрол с белой пилюлей и пустыми состояниями.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-employees/02-03-SUMMARY.md` when done
</output>
