---
phase: 02-employees
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components.json
  - components/ui/
  - app/globals.css
  - app/layout.tsx
  - "app/(app)/layout.tsx"
  - "app/(app)/page.tsx"
  - db/queries/employees.ts
  - lib/ru.ts
  - "app/(app)/employees/page.tsx"
  - "app/(app)/employees/actions.ts"
  - "app/(app)/employees/employee-dialog.tsx"
  - tests/employees-queries.test.ts
  - tests/ru.test.ts
  - scripts/smoke-employees.mjs
autonomous: false
requirements: [EMP-01, UI-01, UI-02]
must_haves:
  truths:
    - "Пользователь заполняет диалог «Добавить сотрудника» (имя + отдел) и после отправки видит нового сотрудника в списке /employees без ручной перезагрузки страницы (D-01, EMP-01)"
    - "Список /employees рендерит ровно до 20 строк на страницу; пагинация — SQL limit/offset, переключение — ссылки с полным query-string ?filter=&page= (D-01)"
    - "Сортировка списка русская корректная: «Ёлкин» стоит после «Ежова» (sort-key выражение replace Ё/ё→Е/е в ORDER BY), при равенстве имён — стабильный tiebreaker по id (D-04)"
    - "Заголовок списка показывает «N сотрудник/сотрудника/сотрудников» через Intl.PluralRules('ru') (UI-01)"
    - "Строка списка — одна строка min-h-11 (44px): имя 16/400 ink, « · », отдел 14/400 ink-secondary, chevron-right 16px #C7C7CC, single-line truncate + атрибут title (D-01, UI-02)"
    - "Диалог создаёт/ошибается по контракту UI-SPEC: ошибка поля — под полем 14/400 #D70015, сбой действия — «Не удалось сохранить. Попробуйте ещё раз.» с role=alert (UI-01)"
    - "Повторное «создание» отдела с существующим именем возвращает существующий id: catch SQLITE_CONSTRAINT_UNIQUE → повторный select — дубль отдела невозможен, спец-ошибка UI не нужна (D-03)"
    - "Создание сотрудника с новым отделом — одна синхронная транзакция db.transaction: отдел и сотрудник пишутся атомарно, сбой на середине не оставляет «висячего» отдела"
    - "Имя сотрудника НЕ уникально: два «Иван Иванов» спокойно создаются и различаются отделом и id (D-04)"
    - "Все строки интерфейса трассы — русские из копи-таблицы UI-SPEC; поиска по имени в списке нет (D-05, UI-01)"
    - "Периметр не тронут: /employees без сессии отвечает 307 → /login; ни один новый путь не добавлен в публичные (ACC-02)"
  artifacts:
    - "app/(app)/layout.tsx — шелл: sticky 48px бар «Учёт техники / Сотрудники / Выйти» (bg-white/70 + backdrop-blur-md + hairline)"
    - "components.json + components/ui/{button,input,label,badge,dialog,combobox}.tsx — shadcn, Base UI, пресет none"
    - "app/globals.css — 7 токенов UI-SPEC (--color-page #F5F5F7 … --color-destructive #D70015), системный шрифт, light-only"
    - "db/queries/employees.ts — listEmployees, getEmployee, listDepartments, resolveDepartmentId, createEmployee, updateEmployee, setEmployeeArchived (чистые sync-функции)"
    - "lib/ru.ts — pluralEmployees(n), ruCollator (Intl.Collator('ru'))"
    - "app/(app)/employees/page.tsx — RSC-список: await searchParams, страница 20, строки, пагинация, счётчик"
    - "app/(app)/employees/actions.ts — createEmployeeAction, updateEmployeeAction, setEmployeeArchivedAction ('use server', requireSession + zod + refresh())"
    - "app/(app)/employees/employee-dialog.tsx — клиентский диалог создания (в 02-03 поле отдела станет combobox)"
    - "scripts/smoke-employees.mjs — E2E-скрипт: temp-БД → next start → minted cookie → ассерты периметра и рендера"
    - "tests/employees-queries.test.ts, tests/ru.test.ts — vitest-покрытие слоя данных и плюрализации"
  key_links:
    - "employee-dialog (useActionState) → createEmployeeAction: requireSession → zod safeParse → db.transaction(resolveDepartmentId + insert) → refresh() → {ok}"
    - "/employees?filter&page → await searchParams → listEmployees(limit/offset + RU sort-key + count) → строки/«Страница N из M»"
    - "scripts/smoke-employees.mjs → jose SignJWT({userId}) с AUTH_SECRET → Cookie: session=… → GET /employees → 200 + имя зонда; без cookie → 307 /login"
  prohibitions:
    - "Нигде нет удаления сотрудника: ни кнопки, ни удаляющего запроса — только архив (EMP-03, D-02)"
    - "Никаких drizzle-kit push (запрещён проектом с фазы 1); в этой фазе миграций нет вовсе — employees/departments уже в 0000"
    - "Никакой клиентской пагинации: сортировка, фильтр и лимиты живут в SQL (D-01)"
---

<objective>
**Как** руководитель, **я хочу** добавить сотрудника (имя + отдел) через диалог и сразу найти его в пагинированном списке, **чтобы** справочник был живым с первого экрана, а эти экраны задали визуальный язык всего приложения (русский UI, Apple-эстетика). *(Фрейминг выведен из Goal-строки фазы по MVP-режиму.)*

Purpose: Этот план — tracer-срез фазы: дизайн-система (shadcn + токены UI-SPEC + шелл) и один полный вертикальный путь «диалог → Server Action → SQLite → RSC-список», доказанный тестами и HTTP-скриптом. Всё, что строят планы 02-02 и 02-03, расширяет этот проверенный срез.

Output: Рабочий экран /employees с созданием сотрудника, дизайн-система в репо, полный слой данных `db/queries/employees.ts` с тестами, smoke-скрипт сквозной проверки.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-employees/02-CONTEXT.md
@.planning/phases/02-employees/02-RESEARCH.md
@.planning/phases/02-employees/02-PATTERNS.md
@.planning/phases/02-employees/02-UI-SPEC.md
@/Users/aleksey/.zcode/skills/apple-design/SKILL.md
@db/schema.ts
@db/index.ts
@lib/auth.ts
@lib/session.ts
@app/login/actions.ts
@app/login/login-form.tsx
@tests/helpers.ts
@app/globals.css
@app/layout.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Легитимность пакетов перед установкой (shadcn / lucide-react / @base-ui/react)</name>
  <files></files>
  <what-built>Research-аудит (02-RESEARCH.md §Package Legitimacy Audit) пометил три устанавливаемых пакета вердиктом SUS (too-new): shadcn 4.19.1 (github.com/shadcn-ui/ui), lucide-react 1.39.0 (github.com/lucide-icons/lucide), @base-ui/react 1.7.0 (github.com/mui/base-ui). Все три — канонические пакеты официальных репозиториев, миллионы загрузок в неделю, postinstall: null; флаги — шум свежести релизов. По протоколу легитимности (security_block_on=high, угроза T-02-SC) установка стоит за блокирующейhuman-проверкой; этот чекпоинт НЕ авто-подтверждается.</what-built>
  <how-to-verify>
    1. Открыть npmjs.com/package/shadcn — убедиться: репозиторий github.com/shadcn-ui/ui, weekly downloads ~миллионы, нет deprecation-notice, нет postinstall-скриптов.
    2. Открыть npmjs.com/package/lucide-react — репозиторий github.com/lucide-icons/lucide, тот же чек.
    3. Открыть npmjs.com/package/@base-ui/react — репозиторий github.com/mui/base-ui; убедиться, что устанавливается именно это имя: НЕ скоуп-less пакет base-ui-react (чужой пакет) и НЕ устаревший @base-ui-components/react (deprecated, переименован).
    4. Подтвердить, что shadcn/ui уже одобрен пользователем в STACK.md, а компоненты копируются в репо (не runtime-зависимости) — риск ограничен.
  </how-to-verify>
  <resume-signal>Введите «approved» для разрешения установки (Task 2 выполнит npx shadcn init/add) или опишите возражение.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Дизайн-система — shadcn init, токены UI-SPEC, системный шрифт, шелл (app)/(app)</name>
  <files>components.json, components/ui/, app/globals.css, app/layout.tsx, "app/(app)/layout.tsx", "app/(app)/page.tsx"</files>
  <read_first>.planning/phases/02-employees/02-UI-SPEC.md (разделы Design System, Spacing Scale, Color, Visual Details, App shell — контракт), .planning/phases/02-employees/02-RESEARCH.md (Pitfall 5 про флаги CLI, Standard Stack §Installation), .planning/phases/02-employees/02-PATTERNS.md (раздел про globals.css и app/layout.tsx — что именно менять), app/globals.css, app/layout.tsx, "app/(app)/page.tsx", "app/(app)/actions.ts" (существующий logout — экспорт `logout`), /Users/aleksey/.zcode/skills/apple-design/SKILL.md (§12 материалы, §14 reduced-motion, §15 типографика)</read_first>
  <action>Установить дизайн-систему и собрать шелл защищённой зоны по контракту UI-SPEC (D-06).
  1) Сначала превью: `npx shadcn@latest add combobox --dry-run` — убедиться, что генерируется нативный Base UI combobox (компоненты вида Combobox/ComboboxContent/ComboboxList/ComboboxItem с возможностью вставить свой первый пункт). Если API не позволяет закреплённый пользовательский пункт — взять фолбэк-пару `npx shadcn@latest add -y popover command` и зафиксировать отклонение в SUMMARY (обе композиции одобрены UI-SPEC/RESEARCH).
  2) `npx shadcn@latest init -b base` — Base UI-библиотека, БЕЗ пресета; флаг -d НЕ использовать (он тянет пресет nova, Pitfall 5 RESEARCH). Затем `npx shadcn@latest add -y button input label badge dialog combobox` (lucide-react придёт транзитивно; @base-ui/react ставит сам CLI — руками не ставить, имена-ловушки из Task 1).
  3) app/globals.css: удалить блок тёмной схемы (media-запрос по пользовательской цветовой схеме — light-only по UI-SPEC); объявить семь токенов UI-SPEC на :root: --color-page #F5F5F7, --color-surface #FFFFFF, --color-ink #1D1D1F, --color-ink-secondary #6E6E73, --color-hairline #D2D2D7, --color-accent #0071E3, --color-destructive #D70015; замапить их в блок @theme inline как цвета Tailwind (--color-page и т.д.); --font-sans в @theme = системный стек из UI-SPEC (строка Design System: -apple-system, BlinkMacSystemFont, "SF Pro Text", system-ui, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif); body { background: var(--color-page); color: var(--color-ink); font-family: var(--font-sans) }; заменить Arial-фолбэк в текущем body-правиле этим стеком; добавить два глобальных media-блока доступности из apple-design §14: prefers-reduced-motion → все анимации фазы становятся opacity-кроссфейдами ≤200ms без scale/slide, и prefers-reduced-transparency → бар становится сплошным белым без backdrop-filter.
  4) app/layout.tsx: убрать импорт и переменную Geist Sans; оставить Geist_Mono с переменной --font-mono (понадобится фазам 3+ для табличных цифр); сохранить lang="ru", русскую metadata и типизированные LayoutProps.
  5) app/(app)/layout.tsx (NEW): async-компонент; первой строкой await requireSession() (defense-in-depth, паттерн lib/auth.ts); sticky-бар h-12 (48px) bg-white/70 backdrop-blur-md border-b border-black/5 flex items-center justify-between px-4 md:px-6; слева «Учёт техники» (text-sm font-semibold, цвет ink); рядом ссылка «Сотрудники» на /employees (text-sm, ink-secondary, hover:ink); справа форма с кнопкой «Выйти», отправляющая существующий экспорт logout из "./actions" (паттерн существующей страницы-каркаса). Контент: mx-auto max-w-3xl px-4 md:px-6 py-8. Кнопкам — пресс-фидбек active:scale-[0.97] transition duration-100 ease-out (apple-design §1 / UI-SPEC).
  6) app/(app)/page.tsx: заменить заглушку на redirect('/employees') из next/navigation (маршрут /employees появится в Task 3 этого же плана; до его завершения ссылка «Сотрудники» ведёт на 404 — осознанный промежуток внутри одного плана).
  Не добавлять ничего в proxy.ts и его PUBLIC_PATHS (V14). Новым классам — только шкала UI-SPEC: без полушаговых отступов и без класса веса 500 (это проверит acceptance ниже, в scope app/(app)/; нормализацию входа делает 02-03).</action>
  <verify>
    <automated>npm run build && npx vitest run && grep -q -- '--color-accent: #0071E3' app/globals.css && grep -q -- '--color-page: #F5F5F7' app/globals.css && [ -f components.json ] && [ -f components/ui/dialog.tsx ] && [ -f components/ui/combobox.tsx ] && [ "$(grep -c 'prefers-color-scheme' app/globals.css)" = "0" ] && [ "$(grep -rn 'font-medium' 'app/(app)/' | wc -l | tr -d ' ')" = "0" ] && [ "$(grep -rnE '(py|px|mt|mb)-[0-9]+\.5' 'app/(app)/' | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` зелёный; `npx vitest run` — все существующие тесты зелёные (36 из фазы 1)
    - components.json существует; components/ui/ содержит button.tsx, input.tsx, label.tsx, badge.tsx, dialog.tsx, combobox.tsx (или popover.tsx + command.tsx при задокументированном фолбэке)
    - grep --color-accent и --color-page находят точные значения #0071E3 / #F5F5F7 в app/globals.css
    - grep -c 'prefers-color-scheme' app/globals.css = 0 (light-only)
    - grep -rn 'font-medium' 'app/(app)/' = 0 совпадений; grep полушаговых классов (суффикс .5 у py/px/mt/mb) в 'app/(app)/' = 0 совпадений
    - 'app/(app)/page.tsx' содержит redirect('/employees'); 'app/(app)/layout.tsx' содержит 'bg-white/70', 'backdrop-blur-md', 'h-12', requireSession() и форму с logout
  </acceptance_criteria>
  <done>Дизайн-система установлена без пресета, токены и системный шрифт живут в globals.css, шелл с 48px-баром и выходом собран, существующий тестовый набор зелёный.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 3: Tracer — «диалог → действие → SQLite → список» одним путём, доказан end-to-end</name>
  <files>db/queries/employees.ts, lib/ru.ts, "app/(app)/employees/page.tsx", "app/(app)/employees/actions.ts", "app/(app)/employees/employee-dialog.tsx", tests/employees-queries.test.ts, tests/ru.test.ts, scripts/smoke-employees.mjs</files>
  <read_first>.planning/phases/02-employees/02-RESEARCH.md (Patterns 1–6, Pitfalls 1–7, Code Examples — все проверены запуском), .planning/phases/02-employees/02-PATTERNS.md (аналоги: app/login/actions.ts, login-form.tsx, tests/schema.test.ts), .planning/phases/02-employees/02-UI-SPEC.md (копи-таблица, список, пагинация, диалог), db/schema.ts (employees строки 49+, departments 28+), db/index.ts, lib/auth.ts, lib/session.ts (SESSION_COOKIE='session', payload {userId}, AUTH_SECRET), tests/helpers.ts (createTempDb, applyMigrations), app/login/actions.ts, app/login/login-form.tsx, node_modules/next/dist/docs/ (файл-conventions page.md, error.md — перед написанием страниц, по AGENTS.md)</read_first>
  <behavior>
    - tests/employees-queries.test.ts (temp-SQLite + applyMigrations из tests/helpers): createEmployee с существующим отделом → вставлен сотрудник, id отдела не изменился; с новым отделом → отдел создан и связан; повтор с тем же именем отдела → тот же id (UNIQUE-гонка → re-select); два сотрудника с одинаковым именем существуют одновременно (D-04)
    - updateEmployee меняет имя и отдел; неизвестный id не роняет и не создаёт строк
    - setEmployeeArchived(id, true/false) переключает isActive; архивный исключён из активного списка и его count, включён в архивный; строка продолжает существовать по id (никаких удаляющих запросов в модуле)
    - listEmployees: 25 строк при pageSize 20 → rows 20, total 25, pages 2; страница 2 → 5 строк; page=99 клампится к последней; page=0/отрицательная → 1
    - Русская сортировка: вставка Анна, Борис, Ёлкин, Ежов → порядок Анна, Борис, Ежов, Ёлкин (Ё после Е); при равных именах порядок стабилен по id
    - tests/ru.test.ts (таблица): 1→«1 сотрудник», 2→«2 сотрудника», 5→«5 сотрудников», 21→«21 сотрудник», 22→«22 сотрудника», 111→«111 сотрудников», 0→«0 сотрудников»
  </behavior>
  <action>Провести один путь насквозь: клиентский диалог → Server Action → транзакция в SQLite → перерисованный RSC-список. Продакшн-качество, без прототипных заглушек в архитектуре.
  1) db/queries/employees.ts (NEW, первый queries-модуль): ТОЛЬКО чистые sync-функции над db из @/db — никаких импортов next/headers (Pitfall 7: иначе vitest не импортирует). Функции: listEmployees({filter, page, pageSize}) где filter='active'|'archive' → {rows, total, page, pages}: innerJoin departments, where eq(employees.isActive, 1|0), orderBy sql-выражение replace(replace(name,'Ё','Е'),'ё','е') затем asc по employees.id как стабильный tiebreaker, limit/offset, page клампится в [1, max(1, ceil(total/pageSize))]; count отдельным count().get() (sync, число). getEmployee(id) → {id, name, department, isActive} | undefined. listDepartments() → все {id, name}. resolveDepartmentId(tx, name) — RESEARCH Pattern 3: select по имени → если нет, insert returning → catch кода SQLITE_CONSTRAINT_UNIQUE → повторный select (гонка проиграна — переиспользуем; отдельной ошибки «отдел уже есть» в UI нет, одна кодовая дорожка). createEmployee({name, departmentName}) — одна db.transaction: resolveDepartmentId + insert employees; имя НЕ уникально (D-04). updateEmployee(id, {name, departmentName}) — та же транзакционная схема. setEmployeeArchived(id, archived) — условный update isActive (исслед. Code Example «Archive action»). Экспортировать типы строк (EmployeeListItem и т.п.).
  2) lib/ru.ts (NEW, конвенция lib/normalize.ts — чистые именованные экспорты, только Node built-ins): pluralEmployees(n) через Intl.PluralRules('ru') с картой one/few/many (RESEARCH Pattern 5) и ruCollator = new Intl.Collator('ru') (пригодится combobox в 02-03).
  3) app/(app)/employees/actions.ts (NEW, 'use server'): три действия-обёртки в форме (_prev, formData) из login/actions.ts. Каждое: ПЕРВОЙ строкой await requireSession() (Server Actions доступны прямым POST — lib/auth.ts). zod-схемы на модуле: EmployeeSchema { name: строка с обрезкой пробелов, 1..100; departmentName: строка с обрезкой, 1..80 }, IdSchema { id: coerce int positive }, ArchiveSchema { id, archived: boolean }. Ошибка схемы полей → { fieldErrors: { name: 'Введите имя сотрудника', departmentName: 'Выберите отдел' } }; прочая ошибка → { error: 'Не удалось сохранить. Попробуйте ещё раз.' } (V7: наружу — только эта строка). Успех → функция из db/queries → refresh() из next/cache (Pitfall 1: без refresh маршрут НЕ перерисуется) → { ok: true }. Никаких сырых FormData-спредов в .set() — только поля из схем (anti mass-assignment).
  4) app/(app)/employees/page.tsx (NEW): export const metadata = { title: 'Сотрудники' }; async-компонента: await requireSession(); await searchParams (Promise! Pattern 1); filter валидируется в 'archive'|'active' (дефолт 'active', D-02), page = max(1, Number(sp.page) || 1); listEmployees({filter, page, pageSize: 20}); шапка: h1 «Сотрудники» (text-xl tracking-tight font-semibold) + счётчик pluralEmployees(total) (text-sm, ink-secondary); справа кнопка «Добавить сотрудника» (primary: bg var(--color-accent), белый текст 14/600, hover #0077ED, active:scale-[0.97]) — открывает диалог; переключатель фильтра — две ссылки «Активные»/«Архив», href собирает ПОЛНЫЙ query (?filter=…&page=1 — сброс страницы, Pitfall 3); белый карточный контейнер rounded-2xl bg-white shadow-sm ring-1 ring-[--color-hairline]; строки — Link на /employees/[id]: min-h-11 px-4 flex items-center, имя (text-base, ink) + « · » + отдел (text-sm, ink-secondary), одна строка с truncate и title, ChevronRight 16px #C7C7CC из lucide-react (маршрут карточки появится в 02-02 — осознанный промежуточный 404 внутри фазы, архитектура ссылки не меняется); подвал пагинации: «Назад»/«Далее» как Links (на границе — span с opacity-40) + «Страница N из M» (text-sm, ink-secondary). Пустые состояния — 02-03 (dev-база засеяна 40 сотрудниками).
  5) app/(app)/employees/employee-dialog.tsx (NEW, 'use client', паттерн login-form): useActionState(createEmployeeAction, {}); shadcn Dialog, панель max-w-md p-6; поля: «Имя» — Input h-10 с placeholder «Иван Иванов»; «Отдел» — пока обычный текстовый Input с placeholder «Выберите или введите отдел»: значение — имя отдела, сервер резолвит по имени одинаково для существующего и нового (контракт не меняется; в 02-03 это поле станет combobox — чистая UI-замена без архитектурных изменений); ошибка поля — под полем text-sm цветом #D70015 из state.fieldErrors; сбой действия — блок role="alert" с общим сообщением; футер: вторичная кнопка «Не сохранять» (закрывает диалог) + primary «Добавить сотрудника» с disabled={pending}; useEffect закрывает диалог при state.ok (refresh уже вызван действием).
  6) tests/employees-queries.test.ts + tests/ru.test.ts (NEW): по behavior-блоку выше; структуру копировать из tests/schema.test.ts (ленивый db()-хелпер, afterAll close), для чистого теста — стиль tests/normalize.test.ts (таблица кейсов). Никогда не импортировать файлы actions в vitest.
  7) scripts/smoke-employees.mjs (NEW, ESM): честная end-to-end проверка периметра и рендера. Шаги: создать временный файл БД; открыть better-sqlite3, исполнить drizzle/0000_amusing_talon.sql целиком через exec() (строки --> statement-breakpoint — SQL-комментарии); вставить зонд: отдел «Отдел дыма» и сотрудник «Смок Сотрудник»; секрет: process.env.AUTH_SECRET либо crypto.randomBytes(32).toString('base64') (одноразовый, только для этого запуска); спавн процесса `node ./node_modules/next/dist/bin/next start -p 3117` с env DATABASE_PATH=<temp> и AUTH_SECRET; ждать готовности опросом GET /login (таймаут 30с); минтить cookie тем же алгоритмом, что lib/session.ts (jose SignJWT с payload {userId: 1}, HS256, истечение 30 дней, имя cookie 'session'); ассерты: GET /employees БЕЗ cookie → 307 с location /login (периметр цел); GET /employees с cookie → 200 и HTML содержит «Смок Сотрудник» и «Сотрудники»; завершить процесс, удалить temp-файл, exit 0; любой провал → внятное сообщение в stderr и exit 1.</action>
  <verify>
    <automated>npx vitest run tests/employees-queries.test.ts tests/ru.test.ts && npm run build && node scripts/smoke-employees.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/employees-queries.test.ts tests/ru.test.ts` — все кейсы из behavior зелёные (создание/обновление/архив/пагинация/Ё-сортировка/плюралы)
    - `npm run build` зелёный (RSC/client-границы dialog+page валидны)
    - `node scripts/smoke-employees.mjs` exit 0: без cookie — 307 на /login; с minted cookie — 200 и «Смок Сотрудник» в HTML /employees
    - grep -q 'refresh()' в 'app/(app)/employees/actions.ts' (все три действия заканчивают им)
    - grep -q 'requireSession' в 'app/(app)/employees/actions.ts' и 'app/(app)/employees/page.tsx'
    - grep -q 'Добавить сотрудника' в employee-dialog.tsx и page.tsx (закреплённая опция combobox «Создать „X“» сюда не входит — это 02-03, здесь отдел пока текстовый Input с тем же контрактом departmentName)
    - grep -rnE 'dangerouslySetInnerHTML' 'app/(app)/employees/' = 0 совпадений (XSS-поверхность)
    - в db/queries/employees.ts нет импортов из next/headers и next/cache (чистота модуля, Pitfall 7)
  </acceptance_criteria>
  <done>Один сотрудник создаётся через диалог и виден в пагинированном списке: тесты слоя зелёные, сборка зелёная, smoke-скрипт доказывает и периметр (307), и рендер строки (200 + имя). Срез закоммичен.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Браузер → Server Actions | Прямые POST к actions.ts мимо proxy-периметра — каждая action обязана сама проверить сессию |
| Браузер → /employees, /employees/[id] | searchParams/params — недоверенный ввод (Promise + строки) перед SQL |
| npm registry → репо | Установка shadcn CLI и транзитивных пакетов (lucide-react, @base-ui/react) |
| Form input → SQLite | Имя сотрудника/отдела — произвольные строки пользователя |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Spoofing/Elevation | employees/actions.ts (все 3 действия) | high | mitigate | requireSession() первой строкой каждого действия (прокси не защищает POST к actions — lib/auth.ts); proxy.ts PUBLIC_PATHS не расширяется |
| T-02-SC | Tampering | npm-установки shadcn/lucide-react/@base-ui/react | high | mitigate | Блокирующий human-чекпоинт Task 1 (never auto-approve, security_block_on=high); имена-ловушки base-ui-react/@base-ui-components названы в чекпоинте; компоненты копируются в репо, не runtime-зависимости |
| T-02-02 | Tampering | listEmployees/resolveDepartmentId (SQL) | medium | mitigate | Drizzle-параметризация (eq/sql авто-параметры — проверено исследованием); filter валидируется в enum, page через Number+clamp — никакой интерполяции строк в SQL |
| T-02-03 | Tampering | Рендер имён в строках/диалоге | low | mitigate | React экранирует текстовые ноды; truncate только CSS; опасный innerHTML-рендер запрещён (acceptance-гейт) |
| T-02-04 | Tampering | actions.ts → .set() | low | mitigate | zod-схемы whitelist ровно {name, departmentName} / {id} / {id, archived}; никакого спреда FormData |
| T-02-05 | Spoofing | POST к actions без CSRF-токена | low | accept | Next сам проверяет same-origin (Host/Origin) для Server Actions; сессия-cookie sameSite=lax (фаза 1); single-user LAN |
| T-02-06 | DoS/Repudiation | Потеря данных сотрудника | low | accept | Удаляющего пути не существует ни в UI, ни в модуле запросов; FK restrict в схеме (EMP-03) — archив-семантика |

</threat_model>

<verification>
- `npx vitest run` — весь набор (36 старых + employees-queries + ru) зелёный
- `npm run build` зелёный
- `node scripts/smoke-employees.mjs` — 307 без cookie, 200 + зонд с cookie
- Гейты дизайна: токены в globals.css, нет тёмной схемы, в app/(app)/ нет веса-500 и полушаговых отступов
</verification>

<success_criteria>
User story трассы выполняется: сотрудник создаётся диалогом и виден в списке на 20 строк/страницу с русским счётчиком и корректной Ё-сортировкой. Дизайн-система (shadcn Base UI + токены UI-SPEC + шелл) живёт в репо и наследуется планами 02-02/02-03.
</success_criteria>

<artifacts_this_phase_produces>
Полная инвентаризация символов фазы 02 (межплановая):

**План 02-01 (этот план):**
- Файлы: components.json; components/ui/{button,input,label,badge,dialog,combobox}.tsx; app/globals.css (токены); app/layout.tsx (--font-mono); app/(app)/layout.tsx; app/(app)/employees/{page,actions,employee-dialog}.tsx; db/queries/employees.ts; lib/ru.ts; scripts/smoke-employees.mjs; tests/{employees-queries,ru}.test.ts
- Функции: listEmployees, getEmployee, listDepartments, resolveDepartmentId, createEmployee, updateEmployee, setEmployeeArchived (db/queries/employees.ts); pluralEmployees, ruCollator (lib/ru.ts); createEmployeeAction, updateEmployeeAction, setEmployeeArchivedAction (actions.ts)
- Флаги/маршруты: GET /employees (?filter=active|archive&page=N); CLI: npx shadcn init -b base / add -y …

**План 02-02:**
- Файлы: app/(app)/employees/[id]/page.tsx; app/(app)/employees/archive-confirm-dialog.tsx; расширение scripts/smoke-employees.mjs
- Маршруты: GET /employees/[id]; символы: ArchiveConfirmDialog, режим редактирования EmployeeDialog

**План 02-03:**
- Файлы: app/(app)/employees/{loading,error}.tsx; app/(app)/employees/[id]/{loading,error}.tsx; restyle app/login/{page,login-form}.tsx; combobox-режим employee-dialog.tsx
- Символы: EmployeesSkeleton, EmployeesErrorBoundary (loading/error-конвенции), buildQuery-хелпер query-string, pinned-опция «Создать „X“» в combobox
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-employees/02-01-SUMMARY.md` when done
</output>
