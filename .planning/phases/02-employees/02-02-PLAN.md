---
phase: 02-employees
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - "app/(app)/employees/[id]/page.tsx"
  - "app/(app)/employees/archive-confirm-dialog.tsx"
  - "app/(app)/employees/employee-dialog.tsx"
  - scripts/smoke-employees.mjs
autonomous: true
requirements: [EMP-01, EMP-03, UI-01, UI-02]
must_haves:
  truths:
    - "Клик по строке списка открывает карточку /employees/[id]: имя 28/600 (Display), отдел 14/400 secondary, при архиве — бейдж-пилюля «В архиве» (bg-black/5, px-2 py-1, 14/400)"
    - "«Редактировать» на карточке открывает тот же диалог с предзаполненными именем и отделом; «Сохранить изменения» обновляет данные, карточка и список отражают новое без ручной перезагрузки (EMP-01)"
    - "«Архивировать» открывает подтверждение: заголовок «Архивировать сотрудника?», тело «{Имя} исчезнет из рабочих списков, но останется в базе вместе с историей. Вернуть можно в любой момент.», кнопки «Архивировать» (нейтральный primary, НЕ красный) / «Не архивировать» (D-02, EMP-03)"
    - "После архивации пользователь остаётся на карточке: виден бейдж «В архиве», кнопка переключилась на «Разархивировать»; разархивирование возвращает сотрудника в рабочие списки (D-02)"
    - "Архивный сотрудник исчезает из активного списка и его счётчика, продолжает существовать в базе и открывается по прямой ссылке (EMP-03; проверено тестами 02-01)"
    - "Карточка без выданной техники показывает секцию «Техника» с текстом «Пока ничего не выдано» — список оживёт в Фазе 4 (EMP-02); отдел NOT NULL, карточки без отдела не бывает"
    - "Несуществующий/мусорный id (/employees/99999, /employees/abc) отвечает 404 через notFound(), а не 500 (V4/V5)"
    - "Ошибка показа списка/карточки — состояние-граница с русским сообщением и повтором (UI-SPEC error-контракт; сами границы — файлы 02-03, поведение не меняется)"
    - "Дубликаты имён различимы на карточке видимым отделом (D-04)"
  artifacts:
    - "app/(app)/employees/[id]/page.tsx — RSC-карточка: await params, zod-валидация id, notFound, getEmployee, бейдж, действия, секция «Техника»"
    - "app/(app)/employees/archive-confirm-dialog.tsx — клиентский alert-диалог подтверждения архивации (useActionState на setEmployeeArchivedAction)"
    - "employee-dialog.tsx — режим редактирования (проп employee?: {id, name, department}, предзаполнение, «Сохранить изменения» / «Не сохранять»)"
    - "scripts/smoke-employees.mjs — расширен: ассерты карточки (200 + имя + «Пока ничего не выдано»), 404 на мусорный id, смена бейджа при переключении isActive"
  key_links:
    - "/employees/[id] → await params → z.coerce.int().positive() → notFound() | getEmployee(id) → карточка"
    - "Архив: ArchiveConfirmDialog → useActionState(setEmployeeArchivedAction) → requireSession + zod → setEmployeeArchived → refresh() → бейдж и кнопка на той же карточке"
    - "Редактирование: EmployeeDialog(edit) → updateEmployeeAction → db.transaction(resolveDepartmentId + update) → refresh()"
  prohibitions:
    - "Нигде нет удаления сотрудника: ни кнопки, ни удаляющего запроса — только архив; греп-гейт на латинское слово удаления в db/queries/ и app/(app)/employees/ = 0 (EMP-03)"
    - "Никаких drizzle-kit push; схема не меняется"
    - "Никакой клиентской пагинации; списочные данные — только через db/queries"
---

<objective>
Достроить справочник до полного цикла жизни сотрудника: карточка по клику из списка, редактирование имени/отдела, архивирование с подтверждением и возврат из архива — на дизайн-системе плана 02-01 (EMP-01 полностью, EMP-03 полностью).

Purpose: Карточка — первый detail-экран приложения (паттерн для фаз 3–6), архив — единственная форма «удаления» в продукте, его семантика защищает историю перемещений Фазы 4.

Output: Маршрут /employees/[id], диалог редактирования, архив-цикл, расширенный smoke-скрипт.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-employees/02-01-SUMMARY.md
@.planning/phases/02-employees/02-CONTEXT.md
@.planning/phases/02-employees/02-UI-SPEC.md
@.planning/phases/02-employees/02-RESEARCH.md
@.planning/phases/02-employees/02-PATTERNS.md
@/Users/aleksey/.zcode/skills/apple-design/SKILL.md
@db/queries/employees.ts
@app/(app)/employees/employee-dialog.tsx
@app/(app)/employees/actions.ts
@scripts/smoke-employees.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Карточка сотрудника /employees/[id] — чтение, валидация id, секция «Техника»</name>
  <files>"app/(app)/employees/[id]/page.tsx"</files>
  <read_first>.planning/phases/02-employees/02-01-SUMMARY.md (API db/queries и смоук-скрипта), .planning/phases/02-employees/02-UI-SPEC.md (строка «/employees/[id] — card» инвентаря, Typography: Display 28/600), .planning/phases/02-employees/02-RESEARCH.md (Code Example «Page + card skeleton» — params это Promise), .planning/phases/02-employees/02-PATTERNS.md (guard-first паттерн страницы), node_modules/next/dist/docs/ (dynamic-routes.md — перед написанием), db/queries/employees.ts, "app/(app)/employees/page.tsx" (классы-рецепты строки/карточки)</read_first>
  <action>Создать async-страницу карточки. Контракт: export const metadata не нужен (динамический маршрут); await requireSession() первой строкой; await params (Promise) и достать id; валидировать z.coerce.number().int().positive().safeParse(id) — неудача → notFound() из next/navigation (не 500); getEmployee(id) → undefined → тоже notFound(). Разметка по UI-SPEC: ссылка «← Сотрудники» (text-sm, ink-secondary, href /employees); имя (класс text-[28px] font-semibold tracking-[-0.02em], цвет ink); отдел под именем (text-sm, ink-secondary); при isActive=0 — бейдж-пилюля «В архиве» (rounded-full bg-black/5 px-2 py-1 text-sm, ink-secondary — роль Label); строка действий: «Редактировать» (вторичная кнопка: bg-black/[0.03] hover, text-sm 400, rounded-lg, active:scale-[0.97]) — пока открывает диалог из Task 2 (в этом таске можно оставить кнопку, вызывающую EmployeeDialog в create-режиме как временный hosting — Task 2 подключает edit); секция «Техника»: заголовок (text-xl tracking-tight font-semibold) + белая карточка rounded-2xl shadow-sm ring-1 с текстом «Пока ничего не выдано» (EMP-02 — Фаза 4; заглушка контентная, не архитектурная). Контент-колонка max-w-2xl (уже задана шеллом max-w-3xl — внутренний контейнер карточки max-w-2xl по UI-SPEC App shell). Дубликаты имён различимы видимым отделом (D-04). Никаких кнопок удаления в разметке (EMP-03).</action>
  <verify>
    <automated>npm run build && node scripts/smoke-employees.mjs</automated>
  </verify>
  <acceptance_criteria>
    - scripts/smoke-employees.mjs расширен и зелёный: GET /employees/{id зонда} с cookie → 200, HTML содержит имя зонда и «Пока ничего не выдано»; GET /employees/99999 → 404; GET /employees/abc → 404; GET /employees/ с cookie → 200 (список не сломан)
    - `npm run build` зелёный
    - grep -q 'notFound()' в 'app/(app)/employees/[id]/page.tsx'; grep -q 'requireSession' там же
    - grep -q 'Пока ничего не выдано' и grep -q 'В архиве' в 'app/(app)/employees/[id]/page.tsx'
    - 404-ответ приходит ДО обращения к БД для мусорного id (валидация safeParse до вызова getEmployee — читается в коде страницы)
  </acceptance_criteria>
  <done>Строка списка ведёт на живую карточку: имя/отдел/бейдж/секция «Техника» по UI-SPEC; мусорный и чужой id дают 404; smoke-скрипт это доказывает.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Редактирование сотрудника — тот же диалог в режиме edit (EMP-01)</name>
  <files>"app/(app)/employees/employee-dialog.tsx", "app/(app)/employees/[id]/page.tsx"</files>
  <read_first>.planning/phases/02-employees/02-01-SUMMARY.md (контракт updateEmployeeAction — он уже написан и протестирован в 02-01), .planning/phases/02-employees/02-UI-SPEC.md (строка Create/Edit dialog), db/queries/employees.ts (updateEmployee), "app/(app)/employees/employee-dialog.tsx", "app/(app)/employees/[id]/page.tsx" (свой Task 1), .planning/phases/02-employees/02-RESEARCH.md (Pattern 6 — useActionState)</read_first>
  <behavior>
    - (слой данных уже покрыт тестами 02-01: updateEmployee меняет имя и отдел, неизвестный id — no-op без ошибок)
    - UI-контракт: диалог в режиме edit открыт по «Редактировать» с предзаполненными именем и отделом; primary-кнопка подписана «Сохранить изменения», вторичная «Не сохранять»; скрытое поле id уходит в updateEmployeeAction; при ok диалог закрывается, карточка обновлена (refresh в действии)
  </behavior>
  <action>Расширить employee-dialog.tsx до двух режимов одним компонентом (без дублей): проп employee?: { id: number; name: string; department: string } — есть → режим edit, нет → создание. В edit: значения по умолчанию из пропа (useState-инициализация или defaultValue формы), в formAction передаётся updateEmployeeAction (экспорт из "./actions", написан в 02-01: тот же контракт fieldErrors/error/ok), hidden input с id. Подписи кнопок по UI-SPEC: primary «Сохранить изменения» / при создании «Добавить сотрудника»; вторичная всегда «Не сохранять». Заголовок диалога: «Редактировать сотрудника» / «Новый сотрудник» (текст-роль Heading 20/600). Поле отдела остаётся текстовым Input (combobox — 02-03), контракт departmentName не меняется. Обработка ошибок — та же (fieldErrors под полем, role="alert"). Закрытие по ok — существующий useEffect. На карточке кнопка «Редактировать» из Task 1 теперь рендерит EmployeeDialog с пропом employee (карточка — server-компонента, диалог — client-остров: паттерн «server page renders a small client island» из PATTERNS). После сохранения refresh() в действии обновляет и карточку, и список. Архитектуру действий и слоя данных НЕ менять — они готовы и протестированы.</action>
  <verify>
    <automated>npm run build && npx vitest run && node scripts/smoke-employees.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` зелёный; `npx vitest run` весь набор зелёный (обновления данных покрыты тестами 02-01)
    - grep -q 'Сохранить изменения' и grep -q 'Не сохранять' в employee-dialog.tsx
    - grep -q 'updateEmployeeAction' в employee-dialog.tsx; grep -q 'Редактировать' в 'app/(app)/employees/[id]/page.tsx'
    - smoke-скрипт зелёный (карточка 200 + маркеры; правки не сломали список/периметр)
    - employee-dialog.tsx не дублирует диалог: один компонент с веткой по пропу employee (код-ревью: один Dialog, одна форма)
  </acceptance_criteria>
  <done>Пользователь исправляет имя/отдел сотрудника с карточки; изменения видны сразу; подписи и ошибки — по копи-таблице UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 3: Архив/разархив с подтверждением — EMP-03, D-02</name>
  <files>"app/(app)/employees/archive-confirm-dialog.tsx", "app/(app)/employees/[id]/page.tsx", scripts/smoke-employees.mjs</files>
  <read_first>.planning/phases/02-employees/02-UI-SPEC.md (строка Archive confirmation, Color: destructive НЕ используется в фазе), .planning/phases/02-employees/02-CONTEXT.md (D-02), .planning/phases/02-employees/02-01-SUMMARY.md (контракт setEmployeeArchivedAction), db/queries/employees.ts (setEmployeeArchived), "app/(app)/employees/[id]/page.tsx", app/login/login-form.tsx (паттерн useActionState), scripts/smoke-employees.mjs</read_first>
  <action>Создать archive-confirm-dialog.tsx ('use client'): alert-диалог на shadcn Dialog с пропами { employeeId, employeeName, open, onOpenChange }; useActionState(setEmployeeArchivedAction, {}); скрытые поля id и archived=true. Копи по UI-SPEC дословно: заголовок «Архивировать сотрудника?»; тело «{Имя} исчезнет из рабочих списков, но останется в базе вместе с историей. Вернуть можно в любой момент.» (имя подставляется пропом); кнопки: «Архивировать» — primary с НЕЙТРАЛЬНЫМ акцентом (тёмный ink, не красный: архив обратим, красный зарезервирован для настоящего вреда — apple-design принцип 2), «Не архивировать» — вторичная. Ошибка действия → role="alert" со стандартной строкой; ok → диалог закрывается, refresh уже вызван действием, пользователь остаётся на карточке (без redirect — UI-SPEC Open Question 2 решён: остаёмся). На карточке: при isActive=1 кнопка «Архивировать» (вторичная) открывает этот диалог; при isActive=0 кнопка «Разархивировать» (вторичная) отправляет форму с archived=false НАПРЯМУЮ (без подтверждения — подтверждение нужно только для ухода из рабочих списков). Бейдж «В архиве» уже рендерится по isActive (Task 1). Проверки EMP-03: греп-гейт ниже — в db/queries/ и app/(app)/employees/ ноль совпадений латинского слова удаления; в карточке и диалогах нет кнопки удаления. Расширить scripts/smoke-employees.mjs: после карточных ассертов переключить is_active зонда в 0 прямо в temp-БД, повторный GET карточки → HTML содержит «В архиве» и «Разархивировать»; вернуть is_active=1, GET → бейджа нет.</action>
  <verify>
    <automated>npm run build && npx vitest run && node scripts/smoke-employees.mjs && [ "$(grep -rniE '\bdelete\b' db/queries/ 'app/(app)/employees/' | wc -l | tr -d ' ')" = "0" ] && grep -q 'Архивировать сотрудника?' 'app/(app)/employees/archive-confirm-dialog.tsx' && grep -q 'Разархивировать' 'app/(app)/employees/[id]/page.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/smoke-employees.mjs` зелёный включая новые ассерты: is_active=0 → на карточке «В архиве» + «Разархивировать»; is_active=1 → без бейджа
    - grep -rniE '\bdelete\b' по db/queries/ и 'app/(app)/employees/' = 0 совпадений (EMP-03: пути удаления не существует)
    - archive-confirm-dialog.tsx содержит дословную копию заголовка «Архивировать сотрудника?» и тела подтверждения из UI-SPEC
    - В архивном состоянии кнопка primary диалога не использует --color-destructive (нейтральный ink/тёмный фон) — читается в классах диалога
    - `npm run build` и `npx vitest run` зелёные
  </acceptance_criteria>
  <done>Полный цикл EMP-03: архивация с подтверждением (нейтральные кнопки), исчезновение из рабочих списков, возврат разархивированием, данные и история неприкосновенны — доказано smoke-скриптом и греп-гейтом.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Браузер → Server Actions | setEmployeeArchivedAction/updateEmployeeAction доступны прямым POST |
| URL → карточка | /employees/[id] — id из URL идёт в SQL |
| Form input → SQLite | Имя/отдел при редактировании — произвольные строки |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Spoofing/Elevation | archive-confirm-dialog → setEmployeeArchivedAction | high | mitigate | requireSession() первой строкой действия (написано в 02-01, не регрессирует); proxy-периметр не трогаем |
| T-02-07 | Tampering | /employees/[id] (ID-параметр) | medium | mitigate | z.coerce.number().int().positive().safeParse до any-SQL; мусор → notFound(); параметризованный drizzle-запрос |
| T-02-08 | Repudiation/DoS | Массовая потеря сотрудников через действие | low | accept | Единственное мутация состояния — обратимый флаг isActive; удаляющего пути нет (греп-гейт); подтверждение добавляет трение осознанности (D-02) |
| T-02-03 | Tampering | Рендер имени в подтверждении (проп employeeName) | low | mitigate | React-экранирование текста; имя не вставляется как разметка |
| T-02-05 | Spoofing | CSRF на action-POST | low | accept | Same-origin проверка Next для Server Actions + cookie sameSite=lax (фаза 1) |

</threat_model>

<verification>
- `npx vitest run` — весь набор зелёный
- `npm run build` зелёный
- `node scripts/smoke-employees.mjs` — карточка 200/404-матрица, бейдж переключается с isActive, периметр 307 на месте
- Греп-гейты: no-delete = 0, notFound/requireSession присутствуют, копи-строки UI-SPEC на месте
</verification>

<success_criteria>
EMP-01 и EMP-03 закрыты end-to-end: найти → открыть → исправить → архивировать → вернуть. Удаления не существует. Карточка задаёт detail-паттерн (Display-типографика, бейдж, секции) для фаз 3–6.
</success_criteria>

<artifacts_this_phase_produces>
Полная инвентаризация символов фазы 02 (межплановая) — см. блок в 02-01-PLAN.md; этот план добавляет: маршрут GET /employees/[id]; компоненты ArchiveConfirmDialog и edit-режим EmployeeDialog; ассерты карточки/404/бейджа в scripts/smoke-employees.mjs.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-employees/02-02-SUMMARY.md` when done
</output>
