---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - app/layout.tsx
  - app/page.tsx
  - app/login/page.tsx
  - app/login/login-form.tsx
  - app/login/actions.ts
  - "app/(app)/page.tsx"
  - "app/(app)/actions.ts"
  - app/api/health/route.ts
  - proxy.ts
  - db/schema.ts
  - db/index.ts
  - drizzle.config.ts
  - drizzle/
  - scripts/create-admin.mjs
  - lib/session.ts
  - lib/rate-limit.ts
  - lib/auth.ts
  - vitest.config.ts
  - tests/helpers.ts
  - tests/stubs/server-only.ts
  - tests/schema.test.ts
  - tests/session.test.ts
  - tests/rate-limit.test.ts
  - tests/proxy-matcher.test.ts
  - .env.example
  - .gitignore
  - README.md
autonomous: true
requirements: [ACC-01, ACC-02, ACC-03]
user_setup: []

must_haves:
  truths:
    - "Валидные логин+пароль создают сессию: HttpOnly-cookie `session` с expires на 30 дней (D-01), sameSite=lax, path=/, без Secure-атрибута (D-04); невалидные — ответ «Неверный логин или пароль» без установки куки"
    - "Публичность пути решается точным совпадением строки: `/login` открыт всем, а соседние пути (`/login-fake`, `/login/step`, `/loginX`) закрыты периметром и отвечают редиректом на /login — краевой случай «adjacency» разрешён"
    - "Отсутствующий, пустой или битый session-cookie даёт null из verifySession и редирект на /login — не 500 и не доступ (краевой случай «empty/null input» разрешён)"
    - "Порядок проверок в login-action фиксирован: zod safeParse → checkRateLimit ДО любого обращения к БД и bcrypt → выборка users по login → bcrypt.compare; recordFailure вызывается только при неверных данных, resetFailures только при успехе (краевой случай «ordering» разрешён)"
    - "Одновременность: db.backup() даёт консистентный снимок без остановки записи; host-side drizzle-kit migrate при живом контейнере выжидает через busy_timeout=5000; счётчики rate-limit живут в памяти процесса и сбрасываются при рестарте — осознанное задокументированное допущение в рамках дискреции D-03 (краевой случай «concurrency» разрешён)"
    - "Первая миграция создаёт полную схему SQLite: 7 таблиц (users, departments, device_types, employees, devices, movements, attachments), триггеры movements_no_update/movements_no_delete с RAISE(ABORT), UNIQUE-индексы на serial_normalized и inventory_normalized (D-17), departments как справочник (D-18), инвентарник nullable и только ручной (D-16), 4 строки device_types — краевой случай ACC-03 «unclassified» разрешён"
    - "Учётка создаётся только через CLI: node scripts/create-admin.mjs пишет единственную строку users (D-13); повторный запуск отказывает с сообщением"
  artifacts:
    - path: "proxy.ts"
      provides: "default-deny периметр: export async function proxy, export const config.matcher"
    - path: "lib/session.ts"
      provides: "SESSION_COOKIE, createSession(userId), verifySession(token), destroySession()"
    - path: "lib/rate-limit.ts"
      provides: "checkRateLimit(nowMs?), recordFailure(nowMs?), resetFailures()"
    - path: "lib/auth.ts"
      provides: "requireSession() (React cache-обёртка, редирект на /login)"
    - path: "db/schema.ts"
      provides: "таблицы users, departments, device_types, employees, devices, movements, attachments"
    - path: "db/index.ts"
      provides: "drizzle-клиент с прагмами WAL/foreign_keys/busy_timeout"
    - path: "drizzle/0000_*/migration.sql"
      provides: "первая миграция: DDL 7 таблиц + триггеры + seed device_types"
    - path: "scripts/create-admin.mjs"
      provides: "CLI создания единственного аккаунта (D-13)"
    - path: "app/login/actions.ts"
      provides: "Server Action login(prev, formData) с rate-limit и zod"
    - path: "tests/schema.test.ts"
      provides: "интеграционные проверки схемы: таблицы, UNIQUE, FK, append-only триггеры"
  key_links:
    - from: "proxy.ts"
      to: "lib/session.ts"
      via: "verifySession(req.cookies.get(SESSION_COOKIE)?.value)"
      pattern: "verifySession"
    - from: "app/login/actions.ts"
      to: "lib/rate-limit.ts"
      via: "checkRateLimit() до обращения к БД"
      pattern: "checkRateLimit"
    - from: "app/login/actions.ts"
      to: "db/schema.ts"
      via: "select from users where login = ?"
      pattern: "users"
    - from: "app/login/login-form.tsx"
      to: "app/login/actions.ts"
      via: "useActionState(login, initialState)"
      pattern: "useActionState"
    - from: "db/index.ts"
      to: "process.env.DATABASE_PATH"
      via: "new Database(DATABASE_PATH) + прагмы"
      pattern: "foreign_keys = ON"
  prohibitions:
    - statement: "MUST NOT различать «неверный логин» и «неверный пароль» — ровно один generic-ответ «Неверный логин или пароль» для обоих случаев, без утечки существования пользователя"
      category: transparency
      verification: test
    - statement: "MUST NOT хранить или сравнивать пароль в открытом/обратимом виде — только bcryptjs-хеш (cost 12) в users.password_hash"
      category: privacy
      verification: test
    - statement: "MUST NOT существовать веб-путь создания аккаунта или сброса пароля — ни route, ни action не INSERT-ят в users и не обновляют password_hash; управление аккаунтом только CLI (D-02, D-13)"
      category: safety
      verification: test
    - statement: "MUST NOT расширять публичный allowlist за пределы точного `/login` и ассет-префиксов `_next/static`, `_next/image`, `favicon.ico` — `api`, будущие uploads и любые новые маршруты закрыты по умолчанию (ACC-02)"
      category: safety
      verification: test
    - statement: "MUST NOT полагаться на proxy как на единственную проверку — каждый мутирующий entry-point (Server Action, Route Handler) и защищённая страница вызывает requireSession() (официальное требование Next: Server Action — это POST к своему маршруту)"
      category: safety
      verification: test
    - statement: "MUST NOT переписывать или удалять историю перемещений — ни одного UPDATE/DELETE-пути к movements в коде, и триггеры БД рвут такую попытку с RAISE(ABORT)"
      category: integrity
      verification: test
---

<objective>
**User story.** **Как** руководитель (единственный оператор), **я хочу** открыть приложение на `http://<server>:3000`, один раз войти по логину и паролю и попасть в защищённый каркас, **чтобы** учёт техники с первого дня жил в полной схеме SQLite, куда никто не попадает без входа (ACC-01, ACC-02, ACC-03).

Walking Skeleton (SKELETON.md в этой же папке): один вертикальный срез «вход от стены до стены» — каркас Next.js 16, полная схема v1 первой миграцией (7 таблиц, append-only триггеры, нормализованные UNIQUE-номера), CLI-создание аккаунта, форма входа с rate-limit, jose-сессия на 30 дней, default-deny периметр `proxy.ts`, защищённый каркас-экран и health-route, — всё с автотестами и curl-проверками периметра.

Purpose: это фундамент, который нельзя переделать потом — схема, нормализованные номера и периметр авторизации задают законы для фаз 2–6.
Output: работающий энд-ту-энд срез + тестовый гарнитур (vitest) + README quickstart (D-12).
</objective>

<execution_context>
@/Users/aleksey/.zcode/gsd-core/workflows/execute-plan.md
@/Users/aleksey/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/research/PITFALLS.md
</context>

<schema_push_requirement>
**[BLOCKING] Schema Push Required** (гейт Schema Push Detection: ORM = drizzle, файлы `db/schema.ts`, `drizzle/*.ts` в scope фазы).

Задача 1 содержит обязательный `[BLOCKING]` шаг применения схемы к живой БД ПОСЛЕ создания schema-файлов и ДО любой верификации. Команда по таблице гейта для drizzle — push-вариант `drizzle-kit`, но исследование проекта запрещает его на SQLite (drizzle-kit issue #6060: может тихо ронять UNIQUE-констрейнты при пересоздании таблиц); равнозначный по гарантии «схема реально живёт в БД до верификации» путь, предписанный RESEARCH.md — `npx drizzle-kit generate` + `npx drizzle-kit migrate`. Команда неинтерактивна — план остаётся autonomous.
</schema_push_requirement>

<artifacts_section>
## Artifacts this phase produces (доля этого плана)

Новые файлы: `proxy.ts`; `app/layout.tsx` (модификация скаффолда: `lang="ru"`, метаданные); удаление скаффолдного `app/page.tsx`; `app/login/page.tsx`, `app/login/login-form.tsx`, `app/login/actions.ts`; `app/(app)/page.tsx`, `app/(app)/actions.ts`; `app/api/health/route.ts`; `db/schema.ts`, `db/index.ts`, `drizzle.config.ts`, `drizzle/0000_*/` (migration.sql + snapshot + meta/_journal.json); `scripts/create-admin.mjs`; `lib/session.ts`, `lib/rate-limit.ts`, `lib/auth.ts`; `vitest.config.ts`; `tests/helpers.ts`, `tests/stubs/server-only.ts`, `tests/schema.test.ts`, `tests/session.test.ts`, `tests/rate-limit.test.ts`, `tests/proxy-matcher.test.ts`; `.env.example`; `.gitignore` (дополнение); `README.md` (quickstart).

Символы/экспорты: `proxy`, `config` (matcher); `SESSION_COOKIE`, `createSession(userId)`, `verifySession(token)`, `destroySession()`; `checkRateLimit(nowMs?)`, `recordFailure(nowMs?)`, `resetFailures()`; `requireSession()`; `login(prev, formData)`; `logout()`; `GET` в health-route; drizzle-инстанс `db`; таблицы `users`, `departments`, `device_types`, `employees`, `devices`, `movements`, `attachments`; триггеры `movements_no_update`, `movements_no_delete`; индексы `devices_serial_norm_uq`, `devices_inventory_norm_uq`, `devices_type_status_idx`, `devices_current_employee_idx`, `devices_warranty_until_idx`, `movements_device_occurred_idx`; констрейнты `devices_status_ck`, `users_login_uq`, `departments_name_uq`, `device_types_key_uq`; CLI `scripts/create-admin.mjs` (интерактивные промпты, без флагов).

Env-переменные: `DATABASE_PATH`, `AUTH_SECRET`.
</artifacts_section>

<tasks>

<task type="tracer">
  <name>[BLOCKING-scope] Tracer: каркас → полная схема v1 → живая БД → учётка админа (data spine)</name>
  <reversibility rating="one-way">Первая миграция с append-only movements и нормализованными UNIQUE-номерами не переделывается после появления данных (перестройка таблиц). Дверь уже пройдена пользователем в D-16/D-17/D-18 и RESEARCH — чекпоинт не вставляем, рейтинг для записи.</reversibility>
  <files>package.json, next.config.ts, app/layout.tsx, app/page.tsx, db/schema.ts, db/index.ts, drizzle.config.ts, drizzle/, scripts/create-admin.mjs, vitest.config.ts, tests/helpers.ts, tests/stubs/server-only.ts, tests/schema.test.ts, .env.example, .gitignore, README.md</files>
  <read_first>.planning/phases/01-foundation/01-PATTERNS.md (разделы db/schema.ts, db/index.ts, scripts/*.mjs — код дословный); .planning/phases/01-foundation/01-CONTEXT.md (D-11, D-12, D-13, D-16, D-17, D-18); .planning/research/PITFALLS.md (Pitfall 5, Pitfall 6, Pitfall 7)</read_first>
  <action>
Каркас в корне существующего репо: `create-next-app` отказывается от непустой директории (`.planning/`, `.claude/`), поэтому скаффолдить во временную папку (`npx create-next-app@latest /tmp/barahlo-scaffold --typescript --app --tailwind --eslint --no-src-dir --import-alias "@/*" --use-npm --yes`), затем перенести ВСЕ записи (включая dotfiles, кроме `.git`) в корень `/Users/aleksey/projects/barahlo`, не трогая `.git`, `.planning`, `.claude`. Установить рантайм-зависимости: `drizzle-orm@0.45 better-sqlite3@13 jose bcryptjs zod server-only`; dev-зависимости: `drizzle-kit @types/better-sqlite3 vitest`. В `next.config.ts` добавить `output: "standalone"`. В `app/layout.tsx` выставить `lang="ru"` и `metadata.title` «Учёт техники». Удалить скаффолдный `app/page.tsx` — корневой маршрут появится в задаче 3 как `app/(app)/page.tsx`.

`db/schema.ts` — полная схема v1 (все семь таблиц за один раз, т.к. PITFALLS требует movements в первой миграции; drizzle-паттерн из PATTERNS.md дословно): `users(id INTEGER PK autoincrement, login TEXT NOT NULL, password_hash TEXT NOT NULL, created_at timestamp notNull defaultFn)` + uniqueIndex `users_login_uq` на login. `departments(id PK, name TEXT NOT NULL, created_at)` + uniqueIndex `departments_name_uq` на name (D-18: справочник). `device_types(id PK, key TEXT NOT NULL, name TEXT NOT NULL, created_at)` + uniqueIndex `device_types_key_uq` на key. `employees(id PK, name TEXT NOT NULL, department_id INTEGER REFERENCES departments ON DELETE RESTRICT (NOT NULL), is_active INTEGER NOT NULL DEFAULT 1, created_at)`. `devices` — широкая таблица: `id PK autoincrement, type_key TEXT NOT NULL REFERENCES device_types(key) ON DELETE RESTRICT, model TEXT NOT NULL, serial_number TEXT NOT NULL, serial_normalized TEXT NOT NULL, inventory_number TEXT (nullable — D-16: только ручной ввод из 1С), inventory_normalized TEXT (nullable), status TEXT NOT NULL DEFAULT 'in_stock', current_employee_id INTEGER REFERENCES employees ON DELETE RESTRICT (nullable), purchase_date timestamp (nullable), purchase_price INTEGER (nullable, рубли), supplier TEXT (nullable), warranty_until timestamp (nullable), notes TEXT (nullable), ram_gb INTEGER (nullable), ram_upgraded INTEGER (nullable, 0/1), ssd_gb INTEGER (nullable), screen_diagonal REAL (nullable), panel_type TEXT (nullable), port_count INTEGER (nullable), peripheral_kind TEXT (nullable), created_at, updated_at`; индексы: uniqueIndex `devices_serial_norm_uq` на serial_normalized, uniqueIndex `devices_inventory_norm_uq` на inventory_normalized (в SQLite NULL-ы в UNIQUE различимы — несколько «без инвентарника» валидны; D-17), index `devices_type_status_idx` (type_key, status), index `devices_current_employee_idx` (current_employee_id), частичный index `devices_warranty_until_idx` на warranty_until с where not-null; CHECK `devices_status_ck`: status in ('in_stock','assigned','repair','disposed'). `movements` (append-only): `id PK, device_id INTEGER NOT NULL REFERENCES devices ON DELETE RESTRICT, event_type TEXT NOT NULL (received|assigned|transferred|returned|to_repair|from_repair|disposed), from_employee_id INTEGER REFERENCES employees ON DELETE RESTRICT (nullable), to_employee_id INTEGER REFERENCES employees ON DELETE RESTRICT (nullable), comment TEXT (nullable), occurred_at timestamp NOT NULL, created_at timestamp NOT NULL`; index `movements_device_occurred_idx` (device_id, occurred_at). `attachments`: `id PK, device_id INTEGER NOT NULL REFERENCES devices ON DELETE RESTRICT, file_name TEXT NOT NULL, mime_type TEXT (nullable), byte_size INTEGER (nullable), kind TEXT NOT NULL DEFAULT 'photo', storage_key TEXT NOT NULL (пути, не BLOB), created_at`. Все FK — с явным ON DELETE RESTRICT (ничто в приложении не удаляется).

`drizzle.config.ts`: dialect 'sqlite', schema `./db/schema.ts`, out `./drizzle`, dbCredentials.url из `process.env.DATABASE_PATH ?? './data/app.db'`.

Миграция: `npx drizzle-kit generate` создаст `drizzle/0000_*/migration.sql` + snapshot + `meta/_journal.json`. Затем ВРУЧНУЮ дополнить конец сгенерированного `migration.sql` (не создавать кастомную миграцию — drizzle-kit триггеры не генерирует, а append в 0000 до первого применения сохраняет «всё в первой миграции» и не трогает journal): DDL двух триггеров дословно из PATTERNS.md (movements_no_update и movements_no_delete — BEFORE UPDATE/DELETE с SELECT RAISE(ABORT, 'movements is append-only: ...')), плюс 4 INSERT в device_types: ('laptop','Ноутбук'), ('monitor','Монитор'), ('dock','Док-станция'), ('peripheral','Периферия'). Прочитать весь сгенерированный SQL глазами (Pitfall 5, drizzle issue #3349): CHECK, UNIQUE и частичный where-индекс должны быть в тексте; если частичный индекс сгенерировался без WHERE — поправить SQL вручную и отметить в SUMMARY.

`db/index.ts` — дословно Pattern 4 из PATTERNS.md: better-sqlite3 клиент на `process.env.DATABASE_PATH ?? './data/app.db'`, прагмы `journal_mode = WAL`, `foreign_keys = ON`, `busy_timeout = 5000`, экспорт `db = drizzle(sqlite)`.

CLI `scripts/create-admin.mjs` (D-13; расширенный образец reset-admin из PATTERNS.md, но INSERT вместо UPDATE): `.mjs`, только better-sqlite3 + bcryptjs (никаких next/jose — Pitfall 7), те же три прагмы, readline-промпты «Логин:» и «Пароль:» (минимум 8 символов, иначе stderr и exit 1), bcrypt.hash cost 12, отказ если в users уже есть строка («Пользователь уже существует — сброс пароля: node scripts/reset-admin.mjs») или login занят (unique-конфликт → сообщение), успешный INSERT печатает подтверждение.

Тестовый гарнитур: `vitest.config.ts` (environment 'node', алиасы: '@' → корень, 'server-only' → `./tests/stubs/server-only.ts`, где стаб — файл с пустым экспортом, т.к. настоящий пакет бросает исключение вне React Server). `tests/helpers.ts`: `createTempDb()` — временный sqlite-файл в os.tmpdir + better-sqlite3 с теми же тремя прагмами; `applyMigrations(db)` — читает `drizzle/*/migration.sql` в лексическом порядке и исполняет каждый через db.exec (обходит допущение A1 о drizzle-миграторе в тестах). `tests/schema.test.ts` — behavior ниже.

`.env.example`: `DATABASE_PATH=./data/app.db`, `DATA_DIR=./data`, `AUTH_SECRET=`. `.gitignore` дополнить `data/` и `.env`. `README.md` — quickstart (D-12): установка, `cp .env.example .env`, генерация секрета `openssl rand -base64 32`, `npx drizzle-kit migrate`, `node scripts/create-admin.mjs`, `npm run dev`.

**[BLOCKING]** После этого применить схему к живой БД и только потом верифицировать: `npx drizzle-kit migrate` против `./data/app.db` (generate+migrate — предписанный путь; push-вариант запрещён исследованием, см. schema_push_requirement).
  </action>
  <behavior>
    tests/schema.test.ts (integration, temp-БД через helpers):
    - после applyMigrations в sqlite_master существуют таблицы: users, departments, device_types, employees, devices, movements, attachments
    - device_types содержит ровно 4 строки с key: laptop, monitor, dock, peripheral
    - повторный INSERT устройства с тем же serial_normalized отклоняется (unique); значения, отличающиеся регистром/пробелами («abc-123» vs «ABC  123»), тоже сталкиваются — нормализация пишется вызывающим кодом, так что тест вставляет уже нормализованные значения, а свойство «одинаковый нормализованный вид ⇒ отказ» покрывает кейс опечатки (D-17)
    - два устройства с NULL inventory_normalized вставляются оба; дубликат непустого inventory_normalized отклоняется
    - INSERT devices со status 'bogus' отклоняется CHECK-ом devices_status_ck
    - INSERT movements со ссылкой на несуществующий device_id отклоняется (FK RESTRICT)
    - UPDATE movements завершается ошибкой с текстом про append-only; DELETE movements — тоже; INSERT movements проходит
    - в sqlite_master типа trigger существуют movements_no_update и movements_no_delete
  </behavior>
  <verify>
    <automated>npx drizzle-kit migrate && npx vitest run tests/schema.test.ts && printf 'admin\nPhase1-guard-2026\n' | node scripts/create-admin.mjs && node -e "const D=require('better-sqlite3');const db=new D('./data/app.db');const c=db.prepare('SELECT COUNT(*) AS c FROM users').get().c;if(c!==1){process.exit(1)};console.log('users=1 ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx drizzle-kit migrate` завершается exit 0 и создаёт ./data/app.db (выполнить ДО остальных проверок — [BLOCKING] гейт схемы)
    - `npx vitest run tests/schema.test.ts` зелёный: 7 таблиц, 4 device_types, UNIQUE/ CHECK/ FK/ триггеры подтверждены
    - `node scripts/create-admin.mjs` с пайпом логина и пароля создаёт ровно одну строку users; повторный запуск завершается ненулевым кодом с сообщением о существующем пользователе
    - `grep -c "movements_no_update" drizzle/0000_*/migration.sql` возвращает ≥ 1 (триггеры в первой миграции)
    - `.gitignore` содержит `data/` и `.env`; `next.config.ts` содержит `output: "standalone"`
  </acceptance_criteria>
  <done>Первая миграция применена к ./data/app.db: 7 таблиц, 2 append-only триггера, 4 device_types, UNIQUE на нормализованных номерах; CLI создаёт единственного пользователя; schema-тесты зелёные.</done>
</task>

<task type="auto" tdd="true">
  <name>Вход по логину+паролю: jose-сессия (30 дней, без Secure) + фиксированное окно rate-limit + русская форма на useActionState</name>
  <files>lib/session.ts, lib/rate-limit.ts, lib/auth.ts, app/login/page.tsx, app/login/login-form.tsx, app/login/actions.ts, tests/session.test.ts, tests/rate-limit.test.ts</files>
  <read_first>db/schema.ts и scripts/create-admin.mjs из задачи 1 (таблица users и хеш-формат); .planning/phases/01-foundation/01-PATTERNS.md (разделы lib/session.ts, lib/rate-limit.ts, app/login/actions.ts — код дословный); .planning/phases/01-foundation/01-CONTEXT.md (D-01, D-03, D-04)</read_first>
  <action>
`lib/session.ts` — Pattern 2 из PATTERNS.md с двумя осознанными адаптациями: (1) `import 'server-only'` остаётся, но `next/headers` импортируется лениво внутри createSession/destroySession (динамический import), чтобы модуль импортировался в vitest-среде — сигнатуры и поведение не меняются; (2) добавить `destroySession()` (delete cookie, действие только внутри action/logout). Константы: `SESSION_COOKIE = 'session'`, `SESSION_TTL_DAYS = 30` (D-01). Cookie-опции: httpOnly: true, sameSite: 'lax', expires: +30 дней (персистентная — переживает перезапуск браузера), path: '/'; Secure-атрибут НЕ ставится (D-04: plain HTTP в LAN — браузеры отбрасывают Secure-cookie по http). Подпись: jose SignJWT HS256, payload `{ userId }`, ключ из `process.env.AUTH_SECRET`. `verifySession(token?)` не бросает: отсутствие/битый/просроченный токен → null.

`lib/rate-limit.ts` — дискреция D-03: фиксированное окно в памяти процесса. Реализация для тестируемости: массив таймстампов неудач; `recordFailure(nowMs = Date.now())` добавляет; `checkRateLimit(nowMs = Date.now())` возвращает true, если за последние 15 минут ≥ 5 неудач; `resetFailures()` очищает. Параметры 5/15 мин (A8) вынести в именованные константы `MAX_FAILURES = 5`, `WINDOW_MS = 15 * 60 * 1000`. Комментарий в файле: счётчики сбрасываются при рестарте процесса — осознанное допущение (один процесс, один пользователь).

`lib/auth.ts`: `requireSession()` — обёртка React `cache()`: читает cookie через `cookies()` (await), `verifySession`, при null — `redirect('/login')`. Это второй слой обороны для страниц/действий (proxy недостаточен для Server Actions).

`app/login/actions.ts` — дословный Code Example из PATTERNS.md: 'use server'; zod-схема `{ login: min(1), password: min(1) }`; порядок фиксирован: safeParse → checkRateLimit ДО любых обращений к БД/bcrypt → select users по login → bcrypt.compare cost-независимо; неверные данные → `recordFailure()` + ответ `{ error: 'Неверный логин или пароль' }` (один generic-текст, без утечки существования логина); переполнение окна → `{ error: 'Слишком много попыток. Попробуйте позже.' }` (проверка ДО bcrypt — защита от перебора, D-03); пустые поля → `{ error: 'Введите логин и пароль' }`; успех → `resetFailures()` → `createSession(user.id)` → `redirect('/')`.

`app/login/page.tsx` — серверный компонент: metadata title «Вход», центрированная чистая карточка на Tailwind (воздух, спокойная типографика — полный дизайн-язык заякорится в фазе 2 по ROADMAP, здесь только опрятная страница входа), рендерит `login-form.tsx`. `app/login/login-form.tsx` — 'use client', `useActionState(login, { })`, поля с русскими метками «Логин» и «Пароль», кнопка «Войти», вывод `state.error`.

Сначала тесты (RED), затем реализация (GREEN). `tests/session.test.ts` и `tests/rate-limit.test.ts` — behavior ниже.
  </action>
  <behavior>
    tests/session.test.ts (unit, jose напрямую):
    - sign → verify roundtrip: verifySession(подписанный токен с payload {userId: 1}) возвращает объект с userId === 1
    - подменённый токен (изменить один символ подписи) → verifySession возвращает null
    - токен с exp в прошлом (setExpirationTime на 10 секунд раньше текущего времени) → null
    - verifySession(undefined) и verifySession('') → null, без исключения
    tests/rate-limit.test.ts (unit, явные nowMs):
    - чистое состояние: checkRateLimit() === false
    - 4 неудачи в окне → checkRateLimit() === false; 5-я неудача → true (блокировка, D-03)
    - resetFailures() снимает блокировку → false
    - блокировка в момент T, проверка в T + 16 минут → false (окно истекло); проверка в T + 14 минут → true
  </behavior>
  <verify>
    <automated>npx vitest run tests/session.test.ts tests/rate-limit.test.ts && (npm run dev & sleep 8; curl -s http://localhost:3000/login | grep -c "Войти"; kill %1)</automated>
  </verify>
  <acceptance_criteria>
    <!-- planner-discipline-allow: SESSION_TTL_DAYS = 30 -->
    - `npx vitest run tests/session.test.ts tests/rate-limit.test.ts` зелёные (все 9 кейсов behavior)
    - `grep -c "SESSION_TTL_DAYS = 30" lib/session.ts` = 1 (D-01) и `grep -c "secure" lib/session.ts` = 0 (D-04: атрибут не ставится)
    - `curl -s http://localhost:3000/login` содержит «Войти», «Логин», «Пароль» (страница живая на dev)
    - В app/login/actions.ts вызов checkRateLimit стоит текстуально выше обращения к db.select (порядок: rate-limit до БД/bcrypt)
  </acceptance_criteria>
  <done>Сессия подписывается/проверяется jose (30 дней, HttpOnly, без Secure), rate-limit блокирует 6-ю попытку в 15-минутном окне, форма входа по-русски рендерится и вызывает action; тесты зелёные.</done>
</task>

<task type="auto">
  <name>Периметр: default-deny proxy.ts + защищённый каркас-экран с выходом + health-route + сквозное доказательство периметра</name>
  <files>proxy.ts, "app/(app)/page.tsx", "app/(app)/actions.ts", app/api/health/route.ts, tests/proxy-matcher.test.ts, README.md</files>
  <read_first>lib/session.ts и lib/auth.ts из задачи 2 (verifySession, requireSession, destroySession, SESSION_COOKIE); .planning/phases/01-foundation/01-PATTERNS.md (Pattern 1 proxy.ts и раздел страницы/health); .planning/research/PITFALLS.md (Pitfall 1 — дыры в периметре)</read_first>
  <action>
`proxy.ts` — Pattern 1 из PATTERNS.md: экспорт `async function proxy(req)` и `export const config`. Отличие от дословного образца, разрешающее краевой случай «adjacency»: из matcher-а исключаются ТОЛЬКО ассет-префиксы — matcher: ['/((?!_next/static|_next/image|favicon.ico).*)']; публичность `/login` решается в обработчике точным сравнением `PUBLIC_PATHS = ['/login']` через `===`/includes. Так соседние пути (`/login-fake`, `/login/step`) НЕ выпадают из периметра: proxy их обрабатывает и редиректит на /login — иначе требование «любой адрес без сессии ведёт на вход» нарушалось бы 404-м. Логика обработчика: нет валидной сессии и путь не публичный → `NextResponse.redirect(new URL('/login', req.nextUrl))`; сессия есть и путь `/login` → редирект на `/`; иначе `NextResponse.next()`. Экспорт `runtime` НЕ объявлять (Next 16: proxy только Node, конфиг бросает ошибку).

`app/(app)/page.tsx` — защищённый корень: вызывает `requireSession()` (второй слой обороны), показывает название приложения «Учёт техники» и текст «Экраны появятся в фазах 2–6» (это ровно граница фазы из 01-CONTEXT/ROADMAP Open Question 5, не урезание объёма) и кнопку «Выйти» — форма с server action. `app/(app)/actions.ts`: 'use server', `logout()` — `destroySession()` + `redirect('/login')`.

`app/api/health/route.ts`: `export async function GET()` — вызывает `requireSession()` и возвращает `NextResponse.json({ ok: true })`; маршрут существует именно как цель curl-проверки периметра (PITFALLS Pitfall 1), поэтому он остаётся ЗА авторизацией.

`tests/proxy-matcher.test.ts` (unit, допущение A10): импортирует `config` из '@/proxy' и через `unstable_doesProxyMatch` из 'next/experimental/testing/server' проверяет matcher: '/_next/static/chunk.abc.js' — НЕ матчится (ассет), '/_next/image?url=x' — НЕ матчится, '/favicon.ico' — НЕ матчится; '/login', '/login-fake', '/api/health', '/' — матчатся (попадают под периметр; публичность /login решает обработчик). Если API недоступно в установленной версии next — заменить тест на bash-курсор из verify (задокументированный фолбэк A10), сохранив те же утверждения через curl.

`README.md` — дополнить раздел «Проверка периметра» точными curl-командами с ожидаемыми кодами (без cookie: `/` → 307 на /login; `/api/health` → 307; `/login-fake` → 307; `/login` → 200; с валидной сессией: `/` → 200) — это же чек-лист приёмки фазы.

Сквозное доказательство (в verify): собрать prod (`npm run build`), поднять (`npm start`), проверить периметр curl-ом без cookie и С кукой — валидный токен сгенерировать node-скриптом тем же алгоритмом, что lib/session.ts (jose SignJWT HS256, AUTH_SECRET из .env, payload {userId:1}, exp 30d), и передать как `Cookie: session=<token>`.
  </action>
  <verify>
    <automated>npx vitest run && npm run build && (npm start & sleep 8; TOKEN=$(node --input-type=module -e "import { SignJWT } from 'jose'; import { readFileSync } from 'node:fs'; const secret = readFileSync('.env','utf8').match(/^AUTH_SECRET=(.*)$/m)[1]; const t = await new SignJWT({userId:1}).setProtectedHeader({alg:'HS256'}).setIssuedAt().setExpirationTime('30d').sign(new TextEncoder().encode(secret)); console.log(t)"); echo "root-no-cookie:$(curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/)"; echo "health-no-cookie:$(curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/api/health)"; echo "loginfake:$(curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/login-fake)"; echo "login:$(curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/login)"; echo "root-with-cookie:$(curl -s -o /dev/null -w '%{http_code}' -H "Cookie: session=$TOKEN" http://localhost:3000/)"; kill %1)</automated>
    <human-check>В браузере: открыть http://localhost:3000 → редирект на /login; войти учёткой из задачи 1 → каркас-экран с «Выйти»; перезапустить браузер → сессия жива (критерий 2 дорожной карты). Фиксируется на приёмке фазы (human_verify_mode: end-of-phase).</human-check>
  </verify>
  <acceptance_criteria>
    - вывод verify: root-no-cookie = 307 (редирект на /login), health-no-cookie = 307, loginfake = 307, login = 200, root-with-cookie = 200
    - `npx vitest run` — весь набор зелёный (schema, session, rate-limit, proxy-matcher)
    - `npm run build` зелёный (Turbopack + standalone)
    - proxy.ts не содержит слова `middleware` и экспорта `runtime`; matcher не исключает `api` и `login` (только `_next/static`, `_next/image`, `favicon.ico`)
    - app/(app)/page.tsx содержит вызов requireSession(); app/api/health/route.ts содержит вызов requireSession()
  </acceptance_criteria>
  <done>Default-deny периметр доказан сквозной curl-проверкой (307/307/307/200/200); защищённый каркас-экран с выходом отдаётся только с валидной сессией; весь тестовый набор и prod-сборка зелёные — Walking Skeleton работает от стены до стены.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → proxy.ts | весь трафик офисной LAN без сессии; untrusted до проверки подписи cookie |
| browser → login Server Action | untrusted креды/формы; zod + rate-limit до обращения к БД |
| app → SQLite | доверенный код, но инварианты движению истории дублируются триггерами БД |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Spoofing | app/login/actions.ts | high | mitigate | фиксированное окно rate-limit (5 неудач / 15 мин) ДО bcrypt и БД (D-03); generic-ошибка без утечки существования логина |
| T-01-02 | Information Disclosure / Elevation of Privilege | proxy.ts matcher + requireSession | high | mitigate | default-deny matcher (исключены только ассеты), точное равенство для /login, requireSession в странице/действии/роуте; curl-проверки в verify |
| T-01-03 | Spoofing | lib/session.ts cookie | medium | mitigate | HttpOnly + SameSite=Lax + HS256-подпись jose; React auto-escaping, без dangerouslySetInnerHTML |
| T-01-04 | Tampering | login Server Action (CSRF) | medium | mitigate | SameSite=Lax + встроенная same-origin проверка Origin у Server Actions (A4 — подтверждается на code review по /docs/app/guides/data-security) |
| T-01-05 | Tampering | db-запросы | medium | mitigate | только параметризованные запросы Drizzle; db.exec — исключительно доверенный версионированный SQL миграций |
| T-01-06 | Repudiation | movements | high | mitigate | триггеры RAISE(ABORT) на UPDATE/DELETE (задача 1) + отсутствие UPDATE/DELETE-путей в коде; тест schema.test.ts |
| T-01-07 | Denial of Service | rate-limit in-memory | low | accept | один пользователь в доверенной LAN; сброс счётчиков рестартом задокументирован (дискреция D-03) |
| T-01-SC | Tampering | npm-установки (next, better-sqlite3, jose, bcryptjs, zod, drizzle, server-only) | medium | mitigate | Package Legitimacy Audit в 01-RESEARCH.md: все пакеты VERIFIED/Approved (SUS только на «too-new» у канонических имён, зафиксировано исследователем); версии pinned в package.json, lockfile в git |
</threat_model>

<verification>
- Полный набор: `npx vitest run` зелёный; `npm run build` зелёный
- Периметр: curl-матрица из задачи 3 (307/307/307/200/200)
- Схема: миграция 0000 содержит 7 таблиц, триггеры, UNIQUE; schema-тесты проверяют инварианты на temp-БД
- Сессия: cookie HttpOnly, 30 дней, без Secure; подпись jose HS256; tamper/expiry → null
</verification>

<success_criteria>
- Без сессии любой адрес ведёт на /login (включая /api/health и несуществующие соседние пути)
- Вход по логину+паролю работает; перебор ограничен; сессия персистентна (30 дней)
- Учётка существует только по CLI (create-admin), повторное создание невозможно
- Первая миграция создаёт полную схему v1 с append-only гарантиями на уровне БД
- Walking Skeleton закрывает сценарий SKELETON.md end-to-end
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
