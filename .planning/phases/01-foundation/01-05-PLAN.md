---
phase: 01-foundation
plan: 05
type: execute
wave: 4
depends_on: ["01-02", "01-04"]
files_modified:
  - README.md
autonomous: true
requirements: [ACC-02, ACC-03]
user_setup:
  - service: corporate GitLab
    why: "Канонический remote для кода + .planning/ (D-15); деплой тянет git pull оттуда"
    env_vars: []
    dashboard_config:
      - task: "Создать пустой приватный проект в корпоративном GitLab и сообщить его URL (или настроить remote и push самостоятельно)"
        location: "GitLab → New project → Create blank project (private)"
  - service: internal server
    why: "Серверный деплой и приёмка выполняются на внутреннем сервере (D-08); у Claude нет SSH-доступа"
    env_vars: []
    dashboard_config:
      - task: "Доступ к серверу по SSH (или выполнение шагов README руками) — docker и Node ≥ 20.9 установлены"
        location: "сервер по D-08"

must_haves:
  truths:
    - "Восстановление из бэкапа отрепетировано локально end-to-end по README-процедуре: поднятая контейнерная стопка с данными → backup через `docker compose exec -T app node scripts/backup.mjs` → замена тома копией из backups/<день> → контейнер поднялся → `PRAGMA integrity_check` = ok → учётка и контрольная запись на месте (успех-критерий 4, D-14)"
    - "Контейнерная стопка переживает `down` + `up -d` с сохранением данных на томе (полная локальная форма успеха-критерия 5)"
    - "Код и `.planning/` запушены в корпоративный GitLab как канонический remote (D-15); README содержит финальный чек-лист приёмки на сервере: деплой, создание учётки на сервере, вход из браузера рабочей станции, curl-матрица периметра на сервере, проверка cron-строки, серверная репетиция восстановления"
  artifacts:
    - path: "README.md (раздел «Приёмка фазы 1»)"
      provides: "финальный чек-лист приёмки: GitLab push, серверный деплой, проверка периметра, cron, restore"
    - path: "README.md (отметки о локальной репетиции восстановления)"
      provides: "зафиксированное доказательство выполнения процедуры (дата, шаги отмечены)"
  key_links:
    - from: "локальная репетиция"
      to: "README (runbook плана 03)"
      via: "исполнение процедуры восстановления шаг за шагом из README"
      pattern: "docker compose down"
    - from: "git push"
      to: "корпоративный GitLab (origin)"
      via: "канонический remote с кодом и .planning/ (D-15)"
      pattern: "origin"
  prohibitions:
    - statement: "MUST NOT выполняться репетиция восстановления на живом прод-томе под работающим контейнером — локально или только при остановленной стопке, строго по README-процедуре"
      category: safety
      verification: judgment
---

<objective>
**User story.** **Как** руководитель, **я хочу** заранее знать, что восстановление из бэкапа реально работает и приложение на сервере переживёт перезапуск, **чтобы** доверять системе данные с первого дня — и принять фазу по явному чек-листу (успех-критерии 4 и 5; D-14, D-15).

Purpose: «отрепетировано хотя бы раз» — проверяемое требование фазы, а не намерение; локальная репетиция автоматизирует максимум, серверная часть фиксируется чек-листом и закрывается на приёмке (human_verify_mode: end-of-phase).
Output: исполненная и отмеченная в README локальная репетиция восстановления и переживания рестарта; финальный чек-лист приёмки; push в GitLab.
</objective>

<execution_context>
@/Users/aleksey/.zcode/gsd-core/workflows/execute-plan.md
@/Users/aleksey/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@README.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
</context>

<artifacts_section>
## Artifacts this phase produces (доля этого плана)

Новых исходников нет — этот план исполняет и фиксирует. Изменяется только `README.md`: отметки о выполненной локальной репетиции восстановления (дата, отмеченные шаги) и новый раздел «Приёмка фазы 1» (серверный чек-лист).
Рантайм-эффекты: локальные docker-том и каталог data/ проходят цикл backup → replace → verify; git-remote origin добавлен и запушен (если GitLab URL предоставлен).
</artifacts_section>

<tasks>

<task type="auto">
  <name>Локальная репетиция восстановления из бэкапа на контейнерной стопке (критерий 4)</name>
  <files>README.md</files>
  <read_first>README.md (runbook «Бэкапы и восстановление» из плана 03 — исполнять ровно его шаги); scripts/backup.mjs из плана 03 (структура backups/<день>/app.db + uploads/); compose.yml из плана 04 (том ./data:/app/data); .planning/phases/01-foundation/01-CONTEXT.md (D-14, успех-критерий 4)</read_first>
  <action>
Исполнить README-процедуру восстановления локально, от начала до конца, фиксируя каждый шаг. Подготовка (если локальная стопка из плана 04 не поднята): `npx drizzle-kit migrate`, `.env` с AUTH_SECRET, `docker compose up -d --build`, создать учётку `docker compose exec -T app node scripts/create-admin.mjs` (пайпом), добавить контрольную запись (одно устройство через разовый `docker compose exec -T app node -e` с INSERT в devices). Репетиция: (1) снять бэкап `docker compose exec -T app node scripts/backup.mjs` — убедиться в `./data/backups/<день>/app.db`; (2) остановить стопку `docker compose down`; (3) `mv data data.broken-<дата>`; (4) `mkdir data` и `cp -R data.broken-<дата>/backups/<день>/. data/`; (5) `docker compose up -d`; (6) `http://localhost:3000/login` отвечает 200; (7) integrity_check поднятой базы = ok (readline-скрипт node с better-sqlite3 против ./data/app.db); (8) контрольная запись и учётка на месте (COUNT-запросы); (9) контроль переживания рестарта: `docker compose down && docker compose up -d` → `http://localhost:3000/login` = 200, данные на месте (критерий 5). По завершении: `docker compose down`; отметить в README-разделе «Бэкапы и восстановление» выполненную репетицию (дата + строка «репетиция выполнена, шаги 1–9 пройдены»); вспомогательный каталог data.broken-<дата> удалить после проверки. Любое расхождение шагов с README-процедурой — чинить README в том же коммите (процедура и реальность обязаны совпадать).
  </action>
  <verify>
    <automated>grep -c "репетиция" README.md && docker compose ps | grep -c app; test -f ./data/app.db && node -e "const D=require('better-sqlite3');const db=new D('./data/app.db',{readonly:true});const ok=db.pragma('integrity_check',{simple:true});if(ok!=='ok'){process.exit(1)}console.log('integrity ok after rehearsal')"</automated>
  </verify>
  <acceptance_criteria>
    - README-раздел «Бэкапы и восстановление» содержит отметку о выполненной репетиции с датой
    - в ходе репетиции backup создал ./data/backups/<день>/app.db, и после замены тома копией контейнер поднялся, /login отвечает 200
    - integrity_check восстановленной базы = ok; контрольная запись devices и строка users присутствуют после восстановления
    - после полного `down` + `up -d` данные и учётка на месте (переживание рестарта, критерий 5)
    - шаги, исполненные руками, совпадают с текстом README один-в-один (расхождения внесены в README)
  </acceptance_criteria>
  <done>Восстановление из бэкапа выполнено и задокументировано локально: снимок → замена тома → поднятая стопка с целой базой и данными; переживание рестарта доказано (критерии 4 и 5 локально).</done>
</task>

<task type="auto">
  <name>Push в корпоративный GitLab (D-15) + финальный чек-лист приёмки фазы в README</name>
  <precondition>URL приватного проекта корпоративного GitLab известен (user_setup «corporate GitLab» выполнен); если не выполнен — остановиться и вернуть checkpoint с запросом URL, не пушить в сторонние ремоуты.</precondition>
  <files>README.md</files>
  <read_first>README.md (текущие разделы — дополнить); scripts/deploy.sh из плана 04 (шаги серверного деплоя, на которые ссылается чек-лист); .planning/phases/01-foundation/01-CONTEXT.md (D-10, D-15)</read_first>
  <action>
Чек-лист приёмки — новый раздел README «Приёмка фазы 1» с нумерованными шагами на сервере: (1) настроить remote и запушить (выполняется в этой задаче, см. ниже); (2) на сервере: клонировать/обновить из GitLab, `cp .env.example .env`, сгенерировать AUTH_SECRET (`openssl rand -base64 32`), `mkdir -p data && chown -R 1000:1000 data`; (3) `bash scripts/deploy.sh` (первый прогон ставит и cron-строку); (4) на сервере создать учётку: `docker compose exec -T app node scripts/create-admin.mjs`; (5) из браузера рабочей станции: открыть `http://<server>:3000` → редирект на /login → войти → каркас-экран; перезапустить браузер — сессия жива; (6) curl-матрица периметра на сервере (без сессии `/` → 307, `/api/health` → 307, `/login` → 200 — те же команды из README-раздела «Проверка периметра»); (7) `crontab -l` содержит одну строку с scripts/backup.mjs; утром проверить `./data/backups/<вчерашний день>/`; (8) серверная репетиция восстановления по README-процедуре (с остановленной стопкой) — хотя бы один раз в первый месяц эксплуатации; (9) отмечать выполненные шаги прямо в README.

Пуш: `git remote add origin <GitLab URL>` (если ещё нет), `git push -u origin main` (ветка main, включая `.planning/` — D-15 разрешает и требует). Если push отклонён по авторизации — вернуть checkpoint с текстом ошибки, не переключаться на другие ремоуты.
  </action>
  <verify>
    <automated>git remote get-url origin && git ls-remote --heads origin main && grep -c "Приёмка фазы 1" README.md</automated>
    <human-check>Серверные шаги чек-листа (2)–(8) выполняет оператор на внутреннем сервере; результат фиксируется на приёмке фазы (human_verify_mode: end-of-phase): вход из браузера рабочей станции, вчерашний бэкап в ./data/backups/, curl-матрица с сервера.</human-check>
  </verify>
  <acceptance_criteria>
    - `git remote get-url origin` возвращает URL корпоративного GitLab; `git ls-remote --heads origin main` видит ветку main (push состоялся)
    - README содержит раздел «Приёмка фазы 1» с шагами серверного деплоя, создания учётки, входа из браузера, curl-матрицы, проверки cron и серверной репетиции восстановления
    - ни один шаг чек-листа не превращает новый маршрут в публичный (периметр не ослабляется ради проверки)
  </acceptance_criteria>
  <done>Код и .planning/ живут в корпоративном GitLab как канонический remote; приёмка фазы расписана проверяемым чек-листом, серверная часть которого закрывается оператором на end-of-phase верификации.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| рабочая станция → GitLab | пуш кода и planning-материалов в приватный корпоративный ремоут (D-15) |
| оператор → прод-том | репетиция восстановления касается живого механизма данных |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Destruction (Availability) | репетиция на проде | high | mitigate | запрет репетиции на живом томе под работающим контейнером (must_haves); локальная репетиция проходит до серверной; серверная — с остановленной стопкой по README |
| T-05-02 | Information Disclosure | код в GitLab | medium | mitigate | приватный корпоративный проект (D-15); публичное зеркало — отдельное будущее решение, .planning/ и данные в него не попадают; .env и data/ в .gitignore и .dockerignore |
| T-05-03 | Repudiation | расхождение README-процедуры с реальностью | medium | mitigate | расхождения, найденные в репетиции, чинятся в README тем же коммитом; чек-лист приёмки опирается только на актуальный текст |
| T-05-SC | Tampering | push не в тот remote | low | accept | precondition задачи 2 требует явного URL GitLab; в сторонние ремоуты не пушим, ошибка авторизации возвращается человеку |
</threat_model>

<verification>
- Локальная репетиция: backup → замена тома → поднятая стопка → integrity ok → данные на месте → переживание down+up (критерии 4 и 5 локально)
- README содержит отметку репетиции и полный чек-лист приёмки
- origin указывает на корпоративный GitLab, main запушен
</verification>

<success_criteria>
- «Восстановление успешно отрепетировано хотя бы один раз» — выполнено локально и задокументировано (критерий 4)
- Контейнер + том переживают перезапуск — доказано локально, серверная проверка — в чек-листе приёмки (критерий 5)
- Канонический remote — корпоративный GitLab (D-15)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-05-SUMMARY.md` when done
</output>
