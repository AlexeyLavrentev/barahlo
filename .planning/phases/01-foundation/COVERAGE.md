# API Coverage — Phase 1: Foundation

No external API integration: фаза — локальный каркас (Next.js routes, Server Actions, локальный SQLite, CLI); `app/api/health` — внутренний route, не внешняя интеграция.
