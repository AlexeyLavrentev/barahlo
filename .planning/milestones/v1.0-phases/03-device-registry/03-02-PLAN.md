---
phase: 03-device-registry
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/(app)/(card)/devices/[id]/page.tsx
  - app/(app)/(card)/devices/[id]/error.tsx
  - app/(app)/devices/device-dialog.tsx
  - app/(app)/devices/actions.ts
  - scripts/smoke-devices.mjs
autonomous: true
requirements: [REG-03]
gap_closure: false
must_haves:
  truths:
    - "Карточка /devices/[id]: группы полей (Основное / Характеристики типа / Закупка), пер-типовые только своего типа, пустые опциональные — прочерк; секции «История»/«Фото» — placeholder (фаза 4) (D-06)"
    - "Мусорный id (не число / нет строки) → русская not-found (404-инвариант, (card) group без loading.tsx)"
    - "Edit-режим того же диалога: тип read-only, все поля своего типа редактируются (REG-03); normalized пересчитывается (Pitfall 2)"
    - "Держатель и статус показываются в карточке, но не редактируются нигде (REG-04 граница)"
    - "Probe-грани REG-03: adjacency — edit не меняет typeKey (zod не принимает); empty — update несуществующего id → не создаёт строк, action возвращает generic-ошибку; ordering — группы карточки в фиксированном порядке device_schema"
  artifacts:
    - path: "app/(app)/(card)/devices/[id]/page.tsx"
      provides: "карточка устройства группами + notFound"
    - path: "app/(app)/(card)/devices/[id]/error.tsx"
      provides: "русская error-граница карточки («Не удалось загрузить карточку устройства», retry)
    - path: "app/(app)/devices/device-dialog.tsx (edit-режим)"
      provides: "edit через тот же диалог, type read-only"
  key_links:
    - from: "(card)/devices/[id]/page.tsx"
      to: "lib/device-schema.ts"
      via: "typeFields(typeKey) для группировки"
      pattern: "device_schema"
  prohibitions:
    - statement: "MUST NOT быть удаляющего пути устройства (архив-семантика — статус через фазу 4)"
      category: boundary
      verification: test
    - statement: "MUST NOT быть loading.tsx внутри (card) group (404-инвариант d0ceff8)"
      category: safety
      verification: test
---

<objective>
**User story.** **Как** руководитель, **я хочу** открыть карточку устройства и исправить любые его поля (кроме типа/статуса/держателя), **чтобы** реестр отражал актуальные данные (REG-03; D-06).
</objective>

<execution_context>
@/Users/aleksey/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-device-registry/03-CONTEXT.md
@.planning/phases/03-device-registry/03-RESEARCH.md
@.planning/phases/03-device-registry/03-PATTERNS.md
@.planning/phases/03-device-registry/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Карточка устройства + 404-инвариант</name>
  <files>app/(app)/(card)/devices/[id]/page.tsx, app/(app)/(card)/devices/[id]/error.tsx</files>
  <read_first>app/(app)/(card)/employees/[id]/page.tsx (id zod→notFound, guard-first, группы — дословный шаблон); db/queries/devices.ts (getDevice); lib/device-schema.ts (typeFields); .planning/phases/03-device-registry/03-UI-SPEC.md (карточка: группы, placeholder секций, статус-пилюли нейтральные); node_modules/next/dist/docs/ (params Promise)</read_first>
  <action>params Promise → id через z.coerce.number().int().positive().safeParse → fail → notFound(); getDevice(id) → undefined → notFound(). Рендер группами из typeFields: Основное (модель/тип/серийник mono/инвентарник mono/держатель/статус-пилюля нейтральная), Характеристики типа (только поля своего типа, пустые опциональные — прочерк), Закупка (дата ru-RU, цена ₽ Intl.NumberFormat, поставщик, гарантия до + подсветка semantics появится в фазе 5). Секции «История»/«Фото» — placeholder-строка копи из UI-SPEC. Кнопка «Редактировать» — открывает edit-диалог (задача 2). Ссылка «← Устройства». Плюс error.tsx рядом: копи «Не удалось загрузить карточку устройства» + «Попробовать снова» (retry-проп), по аналогу employees.</action>
  <verify>
    <automated>npm run build && node scripts/smoke-devices.mjs</automated>
  </verify>
  <acceptance_criteria>
    - smoke-devices расширен: карточка seeded-устройства 200 + модель; /devices/99999 и /devices/abc → русская «Страница не найдена» (404-инвариант)
    - группы рендерятся: grep typeFields в page.tsx ≥ 1
    - статус-пилюля без красного (UI-SPEC: destructive не используется)
  </acceptance_criteria>
  <done>Карточка показывает все поля своего типа группами; мусорный id — русская 404.</done>
</task>

<task type="auto" tdd="true">
  <name>Edit-режим диалога + updateAction</name>
  <files>app/(app)/devices/device-dialog.tsx, app/(app)/devices/actions.ts</files>
  <read_first>app/(app)/employees/employee-dialog.tsx (edit-режим: hidden id, первичная копи «Сохранить изменения», per-open inner state); app/(app)/employees/actions.ts (updateAction shape); db/queries/devices.ts (updateDevice — normalized-пересчёт)</read_first>
  <action>device-dialog: проп device (values) → defaultValue полей, typeKey read-only (select disabled / текст), primary «Сохранить изменения», dismissal «Не сохранять»; zod UpdateSchema = {id} + общие + buildZodSchema(typeKey из существующей строки — typeKey из БД, не из payload). actions: updateDeviceAction — requireSession, whitelist без typeKey/status/currentEmployeeId; normalized-пересчёт уже в updateDevice; ok → refresh(). Тест (RED): updateDevice несуществующего id → false без вставок; edit-action с подменённым typeKey/status в payload — ключи отброшены (safeParse строгого объекта).</action>
  <verify>
    <automated>npx vitest run && npm run build && node scripts/smoke-devices.mjs</automated>
  </verify>
  <acceptance_criteria>
    - vitest зелёный (весь набор)
    - smoke: data-атрибут edit-острова на карточке; изменение модели через прямой action-вызов не эмулируем (UI — UAT), но updateDevice-тест покрывает normalized-пересчёт
    - grep typeKey в UpdateSchema = 0 (тип не из payload)
  </acceptance_criteria>
  <done>Все поля типа редактируются через тот же диалог; тип/статус/держатель неизменяемы.</done>
</task>

<task type="auto">
  <name>No-delete гейт + финальные гейты плана</name>
  <files>scripts/smoke-devices.mjs</files>
  <read_first>.planning/phases/03-device-registry/03-01-SUMMARY.md (что уже в smoke); 02-02-PLAN.md задача 3 (греп-гейт образец)</read_first>
  <action>Греп-гейт: `grep -rniE "\\bdelete\\b" db/queries/devices.ts app/\(app\)/devices/ | grep -v "^Binary"` = 0 (нет удаляющего пути, EMP-03-стиль). Гейт loading: `ls app/(app)/(card)/devices/[id]/loading.tsx` = отсутствие (404-инвариант). Smoke финал: полный матричный прогон.</action>
  <verify>
    <automated>grep -rniE "\bdelete\b" db/queries/devices.ts "app/(app)/devices" | wc -l | command grep -qx 0 && test ! -f "app/(app)/(card)/devices/[id]/loading.tsx" && npm run build && npx vitest run && node scripts/smoke-devices.mjs && node scripts/smoke-employees.mjs</automated>
  </verify>
  <acceptance_criteria>
    - no-delete grep = 0
    - (card)/loading.tsx отсутствует
    - весь набор green (vitest, build, оба smoke)
  </acceptance_criteria>
  <done>REG-03 закрыт: карточка + edit + гейты целостности.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| URL id → SQL | валидация до запроса |
| edit payload → SQL | whitelist без typeKey/status/holder |
| карточка → рендер | React-экранирование значений |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Spoofing/Elevation | update action | high | mitigate | requireSession() первой строкой (наследуется) |
| T-03-02 | Tampering | id/payload → SQL | medium | mitigate | zod-coerce id; whitelist без системных полей; параметризация |
| T-03-03 | Repudiation/DoS | удаление данных | low | accept | удаляющего пути нет (греп-гейт) |
</threat_model>

<verification>
- vitest/build/smoke green; no-delete + no-card-loading гейты
- 404-матрица в smoke; normalized-пересчёт под тестом
</verification>

<success_criteria>
- REG-03: редактирование всех полей с пер-типовым enforced; тип/статус/держатель неизменяемы
</success_criteria>

<output>
Create `.planning/phases/03-device-registry/03-02-SUMMARY.md` when done
</output>
