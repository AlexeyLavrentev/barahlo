---
phase: 05-search-filters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/index.ts
  - db/queries/devices.ts
  - lib/device-schema.ts
  - lib/normalize.mjs
  - lib/normalize.ts
  - "app/(app)/devices/query-params.ts"
  - "app/(app)/devices/search-box.tsx"
  - "app/(app)/devices/page.tsx"
  - tests/homoglyphs-fixture.ts
  - tests/device-search.test.ts
autonomous: true
requirements: [FIND-01, FIND-04]
must_haves:
  truths:
    - "(FIND-01, edge 1) q is trimmed, internal whitespace collapsed, uppercased and homoglyph-folded server-side; folded q = \"\" produces NO search predicate (full list); page clamps into [1, pages] — never an out-of-range OFFSET"
    - "(FIND-01, edge 2) zero matches renders «Ничего не найдено» + hint «Проверьте раскладку: набранное кириллицей „С123“ найдёт „C123“.» + «Сбросить фильтры» Link to /devices; subtitle reads «Найдено: 0 устройств»"
    - "(FIND-01, edge 3) search folds are byte-identical to write-side folds — both come from lib/normalize.mjs; serial/inventory compare their stored *_normalized columns, model folds via the norm() UDF which IS normalizeNumber"
    - "(FIND-01, edge 4) search results keep the canonical RU-sort (Ё→Е replace recipe) with devices.id as stable tiebreaker — no relevance ranking anywhere in the search path"
    - "(FIND-04, edge 7) the homoglyph typing fixture proves ФИКСТУРА-completeness: all 11 HOMOGLYPHS pairs typed in EITHER direction find the counterpart («С123» finds «C123» and vice versa); extra internal whitespace collapses; leading/trailing whitespace trims; the fixture provably covers every key of the HOMOGLYPHS map"
    - "(D-01) live search from the first character: ~300 ms debounce, then router.replace with the FULL query string and scroll:false inside startTransition; Enter commits immediately; re-typing cancels the pending timer; the value === q guard skips no-op pushes (Pitfall 6 race with «Сбросить фильтры»)"
    - "(D-05) with q active the subtitle reads «Найдено: {pluralDevices(total)}»; without it the existing pluralDevices counter is unchanged"
    - "(UI-SPEC loading) a debounced update keeps the current list mounted through the server swap (startTransition, no skeleton flash per keystroke); the input keeps focus and its value; loading.tsx and error.tsx boundaries are untouched"
    - "(UI-SPEC long-text) model names in searched rows keep the existing truncate + full-title rules"
  artifacts:
    - path: db/index.ts
      provides: "norm() UDF registration in openDb() — sqlite.function('norm', { deterministic: true }, normalizeNumber)"
    - path: db/queries/devices.ts
      provides: "DeviceListFilters type, searchPredicate(q), listDevices({ filters }) consuming filters.q (optional param — existing callers unaffected)"
    - path: "app/(app)/devices/query-params.ts"
      provides: "the ONE pure params module: DeviceFilters, WARRANTY_ITEMS, parseDevicesSearchParams(sp), buildDevicesQuery(f, page) — shared later by islands, page and the CSV route"
    - path: lib/device-schema.ts
      provides: "DEVICE_STATUS_KEYS + isDeviceStatusKey exported from the keystone (status filter reads it in plan 02)"
    - path: lib/normalize.mjs
      provides: "exported HOMOGLYPHS map (fixture completeness guard) + typed re-export in lib/normalize.ts"
    - path: "app/(app)/devices/search-box.tsx"
      provides: "DeviceSearchBox debounced island (useState + useEffect timer + useTransition + router.replace)"
    - path: tests/homoglyphs-fixture.ts
      provides: "the typing-test fixture table (11 pairs) + completeness guard — closes the STATE.md blocker"
    - path: tests/device-search.test.ts
      provides: "FIND-01/FIND-04 search matrix against temp SQLite through listDevices"
  key_links:
    - from: "DeviceSearchBox (300 ms debounce)"
      to: "buildDevicesQuery → URL ?q= → parseDevicesSearchParams → listDevices(filters.q)"
      via: "router.replace(href, { scroll: false }) inside startTransition"
      why: "the URL is the only search state (phase-3 pattern); the server re-validates every param"
    - from: "searchPredicate fold"
      to: "lib/normalize.mjs normalizeNumber (the same function the write path uses at createDevice/updateDevice lines 218/256)"
      via: "norm() UDF registered in openDb(); serialNormalized/inventoryNormalized compared directly"
      why: "query-side and write-side folds cannot diverge; SQLite LIKE/upper are ASCII-only — the UDF is the only correct fold point"
    - from: "DeviceFilters (URL layer — 'all'/null/false sentinels)"
      to: "DeviceListFilters (db layer — undefined-for-inactive)"
      via: "the page strips inactive sentinels when building the listDevices filters arg (Task 1 step 7)"
      why: "plan-02 Task 2 presence guards assume undefined — a leaked 'all'/false would corrupt every composed filter"
  prohibitions:
    - statement: "MUST NOT widen device search beyond serial number / inventory number / model — in particular MUST NOT match holder or employee names («вся техника Иванова» lives in the employee card) (FIND-01/D-02)"
      category: values
      status: resolved
      verification: test
    - statement: "MUST NOT build a relevance ranking or any opaque ordering for search results — order stays the deterministic RU-sort + id tiebreaker (D-03)"
      category: values
      status: resolved
      verification: test
---

<objective>
Live, homoglyph-proof substring search over devices — the heart of FIND-01/FIND-04 — working end-to-end: keystroke → 300 ms debounce → URL ?q= → server-validated parse → norm()-UDF SQL predicate → rendered list with «Найдено: N», the «Ничего не найдено» empty state, and the full query-params module every later plan imports.

Purpose: Core Value moment one — «С123» typed on a Cyrillic keyboard finds the Latin serial «C123»; this plan also lays the shared interface (query-params.ts, keystone status exports, exported HOMOGLYPHS) that plans 02–04 build on.
Output: searchable /devices (URL-driven, live), tests/homoglyphs-fixture.ts + tests/device-search.test.ts green, UDF registered in db/index.ts.
</objective>

<execution_context>
@/Users/aleksey/.zcode/gsd-core/workflows/execute-plan.md
@/Users/aleksey/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-search-filters/05-CONTEXT.md
@.planning/phases/05-search-filters/05-RESEARCH.md
@.planning/phases/05-search-filters/05-PATTERNS.md
@.planning/phases/05-search-filters/05-UI-SPEC.md
@db/index.ts
@db/queries/devices.ts
@lib/device-schema.ts
@lib/normalize.mjs
@app/(app)/devices/page.tsx
@app/(app)/devices/type-filter.tsx
@tests/devices-queries.test.ts

AGENTS.md: this Next.js version has breaking changes — verify any Next.js API (router.replace/push options, searchParams Promise prop) against node_modules/next/dist/docs/ guides (RESEARCH already verified use-router.md and use-search-params.md for the installed 16.3.3).
Vercel React best practices: server-serialization (islands receive flat props only, never functions), server-cache-react (list rendered by RSC; the island is the only client state), rerender discipline (single controlled input state — cite server-serialization in code comments where the pattern applies).
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end live search — Cyrillic «с123» finds Latin serial «C123»</name>
  <files>db/index.ts, db/queries/devices.ts, app/(app)/devices/query-params.ts, app/(app)/devices/search-box.tsx, app/(app)/devices/page.tsx, lib/device-schema.ts, lib/normalize.mjs, lib/normalize.ts, tests/device-search.test.ts</files>
  <read_first>
    db/index.ts (openDb() pragmas block, lines 10-19 — the UDF registration site; the Proxy singleton at lines 29-34 exposes db.$client to tests)
    db/queries/devices.ts (module contract lines 7-9; DeviceListItem/DeviceRow types; listDevices lines 104-179 — the where-once/use-twice property and the covers batch that must survive untouched; write-site normalization lines 216-219, 256)
    lib/normalize.mjs + lib/normalize.ts (normalizeNumber trim→collapse→upper→HOMOGLYPHS; typed re-export shape)
    lib/device-schema.ts (pure-module header discipline lines 9-12; isDeviceTypeKey lines 91-96 as the enum-guard template; DEVICE_STATUS_LABELS lines 108-113 — the keystone the new exports derive from)
    app/(app)/devices/page.tsx (buildQuery lines 26-33, searchParams validation lines 40-53, subtitle line 62, filter wrapper lines 70-74, empty states lines 76-100)
    app/(app)/devices/type-filter.tsx (the island shell: 'use client', useRouter, full-query-string push lines 25-38)
    05-RESEARCH.md Pattern 1 (UDF + searchPredicate — probe-verified) and Pattern 3 (debounced island mechanics)
    05-PATTERNS.md sections: query-params.ts, search-box.tsx, db/index.ts UDF registration
    05-UI-SPEC.md Screens table rows: SearchBox island; Empty states precedence; Subtitle
  </read_first>
  <action>
    1. db/index.ts — inside openDb(), after the three pragmas and before `return drizzle(sqlite)`, register the fold UDF per RESEARCH Pattern 1: sqlite.function('norm', { deterministic: true }, normalizeNumber) with normalizeNumber imported from '@/lib/normalize'. Deterministic flag per better-sqlite3 13 API (RESEARCH verified in the installed lib/methods/function.js).
    2. lib/normalize.mjs — export the HOMOGLYPHS map (add `export` to the existing const; no behavior change). lib/normalize.ts — re-export it typed: `export const HOMOGLYPHS: Record<string, string> = _HOMOGLYPHS` following the existing re-export shape (fixture completeness guard needs the map; additive, safe).
    3. lib/device-schema.ts — export `DEVICE_STATUS_KEYS` (readonly array derived from the DEVICE_STATUS_LABELS keys: 'in_stock','assigned','repair','disposed') and `isDeviceStatusKey(value: unknown): value is DeviceStatusKey` cloned from the isDeviceTypeKey guard shape, plus the exported `DeviceStatusKey` type. Status filters (plan 02) must read the keystone — no parallel list anywhere (orchestrator resolution 4; D-02 discipline of phase 3).
    4. db/queries/devices.ts — add `export type DeviceListFilters = { q?: string; status?: DeviceStatusKey; departmentId?: number; warranty?: 'w30' | 'w60' | 'expired'; ramNoUpgrade?: boolean }` (full interface now; this task implements only the q consumer — plans 02 fill the rest). Add private `searchPredicate(rawQ: string | undefined)` exactly per RESEARCH Pattern 1: fold with normalizeNumber, return undefined when the folded query is empty (edge: empty/whitespace q → NO predicate), slice the folded query to 100 chars (server-side cap, A4), escape `\`, `%`, `_` and bind with `escape '\\'`, then `or()` the three LIKEs: devices.serialNormalized, devices.inventoryNormalized, and sql`norm(${devices.model}) like ${pattern} escape '\\'`. D-02: exactly these three fields — the employees join stays out of the predicate. Extend listDevices' options object with optional `filters?: DeviceListFilters` and start the where composition as `and(type === 'all' ? undefined : eq(devices.typeKey, type), filters?.q ? searchPredicate(filters.q) : undefined)` — one where shared by count and rows (keep the Pitfall 5 property; leave the covers batch untouched).
    5. app/(app)/devices/query-params.ts — NEW pure module, header comment copied from the lib/device-schema.ts discipline (no framework imports — imported by RSC, islands and vitest alike). Exports: `DeviceFilters` type { q: string; type: DeviceListType; status: DeviceStatusKey | 'all'; departmentId: number | null; warranty: 'w30' | 'w60' | 'expired' | 'all'; ramNoUpgrade: boolean }; `parseDevicesSearchParams(sp)` validating every param server-side — q: typeof string → trim → slice 100 (invalid/absent → ''), type via isDeviceTypeKey else 'all', status via isDeviceStatusKey else 'all', departmentId via z.coerce.number().int().positive() (zod, per the card-page IdSchema pattern) else null, warranty via the WARRANTY_ITEMS value set else 'all', ramNoUpgrade via presence of the ram param value '1' (any other value → false; the server predicate self-limits to laptops regardless — plan 02 wires it); `buildDevicesQuery(f: DeviceFilters, page = 1): string` building the FULL URLSearchParams (omit 'all'/null/false params, omit page when 1, and omit the ram param when f.type !== 'laptop' — the D-08 coupling lives HERE in the one builder: switching type to a non-laptop drops the ram param so it can never silently reactivate later) — the single builder pagination links, islands and the CSV link all call; `WARRANTY_ITEMS` as const per UI-SPEC Default 12 order: { all, 'Вся гарантия' }, { w60, 'Истекает ≤ 60 дней' }, { w30, 'Истекает ≤ 30 дней' }, { expired, 'Истекла' }. Import param names as a single convention: q, type, status, dept, warranty, ram, page.
    6. app/(app)/devices/search-box.tsx — NEW client island per RESEARCH Pattern 3: 'use client'; props { q: string; current: DeviceFilters } (flat serializable values only — server-serialization); controlled input type="search" with maxLength 100, placeholder «Серийник, инвентарник или модель», aria-label «Поиск по устройствам», per UI-SPEC: h-10 rounded-lg px-3 pl-9 text-base, hairline border, white fill, accent focus ring, leading lucide `Search` icon 16px ink-secondary absolutely positioned left-3; local useState(q), useEffect with a 300 ms setTimeout that calls startTransition(() => router.replace(buildDevicesQuery({ ...current, q: value }), { scroll: false })) — cleanup clearTimeout on every value/q change; skip when value === q; a mounted-ref skips the initial mount; Enter (form onSubmit prevented or onKeyDown Enter) clears the timer and navigates immediately. NOT a <form action> (React 19 controlled-reset landmine, 4886f6a) and no server action — cite this in a comment.
    7. app/(app)/devices/page.tsx — rewire: parse via parseDevicesSearchParams(await searchParams) (delete the local type/page ad-hoc parsing; keep requireSession() as the first statement); call listDevices({ type, page, pageSize: PAGE_SIZE, filters }) passing a DeviceListFilters built from the parsed DeviceFilters by stripping the URL-layer sentinels into the db-layer contract — q '' → omitted, status/type 'all' → omitted, departmentId null → omitted, warranty 'all' → omitted, ramNoUpgrade false → omitted (undefined-for-inactive; plan-02 Task 2 presence guards assume undefined); replace buildQuery with buildDevicesQuery for both pagination links (Назад/Далее — full query string + page, Pitfall 10 backstop stays the listDevices clamp); subtitle (D-05): when q is non-empty render «Найдено: {pluralDevices(total)}» else the existing pluralDevices(total); add DeviceSearchBox into a new filter row container `mt-6 flex flex-wrap items-center gap-2` (plan 02 grows it into FilterBar — keep the existing DeviceTypeFilter beside it for now); empty states (D-04): when rows are empty AND q is non-empty render the third state — heading «Ничего не найдено» (text-xl font-semibold tracking-tight text-ink), hint «Проверьте раскладку: набранное кириллицей „С123“ найдёт „C123“.» (text-sm text-ink-secondary mt-1), and `<Link href="/devices" className="...">Сбросить фильтры</Link>` (mt-4, secondary button styling) — the plain server Link clears everything and the island's value === q guard clears the input on arrival; the two phase-3 states keep their exact existing copy and precedence when q is empty.
    8. tests/device-search.test.ts — NEW, reusing the devices-queries.test.ts harness discipline verbatim (DATABASE_PATH before @/db import, dynamic imports, applyMigrations(db.$client)). Tracer cases: insert a device with serial 'C123' (via createDevice so the write-side normalization runs), listDevices({ type: 'all', page: 1, pageSize: 20, filters: { q: 'с123' } }) returns that row (Cyrillic с → Latin C); the same with '  c123  ' (whitespace trim); filters.q '' returns the full unfiltered set with an unchanged total.
  </action>
  <verify>
    <automated>npx vitest run tests/device-search.test.ts && grep -n "function('norm'" db/index.ts && grep -n "export const DEVICE_STATUS_KEYS" lib/device-schema.ts && grep -n "parseDevicesSearchParams" "app/(app)/devices/page.tsx"</automated>
  </verify>
  <done>listDevices with filters.q finds 'C123' from the Cyrillic query 'с123' and from padded input; empty q is a no-predicate full list; the UDF is registered in openDb; /devices renders the live search island, «Найдено: N» under active q, and the D-04 empty state with the раскладка hint and reset Link; query-params.ts, DEVICE_STATUS_KEYS/isDeviceStatusKey and HOMOGLYPHS exports exist; the existing devices-queries suite still passes (optional filters param is backward compatible)</done>
  <reversibility rating="reversible">UDF registration and the optional filters param are additive one-file changes; the replace-chain fallback is documented in RESEARCH if UDFs are ever unwanted.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Homoglyph typing fixture + FIND-04 matrix — prove fold completeness, whitespace tolerance, cap and wildcard safety</name>
  <files>tests/homoglyphs-fixture.ts, tests/device-search.test.ts</files>
  <read_first>
    lib/normalize.mjs (the 11 HOMOGLYPHS entries and normalizeNumber)
    tests/devices-queries.test.ts (harness lines 1-25; the homoglyph-duplicate precedent lines 67-76 for С↔Х-style assertions)
    tests/helpers.ts (insertDevice and applyMigrations)
    05-RESEARCH.md §Validation Architecture (FIND-04 fixture spec: per-pair both directions + completeness guard over every HOMOGLYPHS key)
    05-RESEARCH.md Pitfall 3 (LIKE wildcards) and Common Pitfalls cap A4
  </read_first>
  <behavior>
    - For each of the 11 pairs (А↔A, В↔B, С↔C, Е↔E, Н↔H, К↔K, М↔M, О↔O, Р↔P, Т↔T, Х↔X): a device whose serial contains the Latin letter is found by searching the Cyrillic-typed twin — and for С, О, Е also the reverse direction (Latin query finds Cyrillic-stored twin)
    - normalizeNumber round-trip: '  с123  ' → 'C123'; internal double space collapses ('AB  12' → 'AB 12'); lowercase folds
    - q = '' and q = '   ' produce the full list (no predicate) with total unchanged
    - a query longer than 100 chars does not throw and behaves as its capped form
    - queries '%', '_', 'a%b' match literal characters only — they never act as SQL wildcards (escaped + ESCAPE clause)
    - equal model names resolve stably by id across repeated searches (RU-sort + id tiebreaker, D-03); searched results stay in canonical RU-sort order (Ё→Е)
    - fixture completeness: every key of the exported HOMOGLYPHS map appears in the fixture table — the test FAILS if the map grows without the fixture following
  </behavior>
  <action>
    Write the failing tests first (RED), then confirm they pass against the Task 1 implementation (GREEN — the implementation already exists; this task is the proof layer, so a genuinely failing first run is expected only for cases like the cap or wildcards if Task 1 missed them — fix in db/queries/devices.ts, never by weakening the test).
    tests/homoglyphs-fixture.ts — export a typed table: one entry per pair { cyr: 'А', lat: 'A' } … { cyr: 'Х', lat: 'X' } plus the reverse-direction subset ['С','О','Е']; import HOMOGLYPHS from '@/lib/normalize' and export a completeness assertion helper that checks every Object.keys(HOMOGLYPHS) value appears as a `lat` in the table.
    tests/device-search.test.ts — extend with describe blocks: (1) per-pair matrix seeding via createDevice (serials like `SN-{lat}-77`) and asserting listDevices({ filters: { q: cyr-typed twin } }) hits exactly that device; (2) whitespace/case round-trips through the same query path (not just normalizeNumber — assert the SQL path honors them); (3) empty/whitespace q returns the full set; (4) over-100-char query (e.g. 'x'.repeat(150)) returns the capped query's result set without throwing; (5) wildcard literals: seed 'AB-100' and 'AxxB-100'-style rows and assert q 'a%b' / 'a_b' / '%' return only literal matches (for '%' assert it does NOT return the full registry); (6) sort stability: two devices with identical models inserted in known id order appear in that order under q active, and a Ё-model sorts per the replace recipe.
  </action>
  <verify>
    <automated>npx vitest run tests/device-search.test.ts tests/normalize.test.ts</automated>
  </verify>
  <done>the full 11-pair matrix passes in both required directions; the fixture-completeness guard fails when the table is missing a pair (verified once by temporarily removing an entry during development, then restored); whitespace, cap, wildcard-escape and sort-stability cases are green; the STATE.md homoglyph blocker is closed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → URL searchParams (?q=) | untrusted string crosses into SQL via the search predicate |
| keystrokes → island → router.replace | client input echoed back into a controlled input value |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | searchPredicate SQL | high | mitigate | drizzle bound params only — user text is never interpolated into SQL; the sql`norm(...)` template interpolates a column reference, the pattern rides a bound parameter |
| T-05-02 | DoS (marginal) / correctness | LIKE wildcards in q | medium | mitigate | escape `\`, `%`, `_` + `ESCAPE '\'` on every LIKE; server-side 100-char cap on the folded q (RESEARCH Pitfall 3, A4) |
| T-05-03 | Tampering (reflected XSS) | q echoed into the controlled input / subtitle | low | accept | React escapes text nodes and input values by construction; q is never rendered as raw HTML and the empty-state hint is static copy — no additional mitigation available or needed |
| T-05-SC | Tampering | npm installs | none | accept | zero new packages this phase (Package Legitimacy Audit: none) — no supply-chain surface |
</threat_model>

<verification>
- npx vitest run tests/device-search.test.ts — full FIND-01/FIND-04 matrix green
- npx vitest run — whole suite stays green (backward-compatible listDevices signature)
- grep proves the wiring: norm UDF in db/index.ts, DEVICE_STATUS_KEYS export in lib/device-schema.ts, parseDevicesSearchParams in page.tsx
- Cyrillic «с123» finding Latin «C123» is proven at the queries level (the live-feel debounce is a documented manual-only check for end-of-phase per 05-VALIDATION.md)
</verification>

<success_criteria>
- A user typing a fragment of a serial / inventory number / model sees live filtered results from the first character, case- and homoglyph-insensitive, whitespace-tolerant
- «С123» (Cyrillic) finds «C123» (Latin) and the fixture proves this for ALL 11 pairs
- Zero matches shows «Ничего не найдено» + раскладка hint + working «Сбросить фильтры»
- The URL is the only search state; pagination and filter links preserve q
</success_criteria>

## Artifacts this phase produces

- `norm()` UDF registration in `db/index.ts` (openDb)
- `DeviceListFilters`, `searchPredicate()`, extended `listDevices({ filters })` in `db/queries/devices.ts`
- `app/(app)/devices/query-params.ts`: `DeviceFilters`, `WARRANTY_ITEMS`, `parseDevicesSearchParams()`, `buildDevicesQuery()`
- `DEVICE_STATUS_KEYS`, `isDeviceStatusKey`, `DeviceStatusKey` exports in `lib/device-schema.ts`
- exported `HOMOGLYPHS` in `lib/normalize.mjs` + typed re-export in `lib/normalize.ts`
- `DeviceSearchBox` in `app/(app)/devices/search-box.tsx`
- extended `/devices` page: live search, «Найдено: N» subtitle (D-05), D-04 empty state, buildDevicesQuery pagination
- `tests/homoglyphs-fixture.ts`, `tests/device-search.test.ts`

<output>
Create `.planning/phases/05-search-filters/05-01-SUMMARY.md` when done
</output>
