---
phase: 05-search-filters
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - lib/warranty.ts
  - "app/(app)/devices/warranty-filter.tsx"
  - "app/(app)/devices/status-filter.tsx"
  - "app/(app)/devices/department-filter.tsx"
  - "app/(app)/devices/ram-chip.tsx"
  - "app/(app)/devices/filter-bar.tsx"
  - "app/(app)/devices/type-filter.tsx"
  - "app/(app)/devices/page.tsx"
  - db/queries/devices.ts
  - tests/warranty.test.ts
  - tests/devices-queries.test.ts
autonomous: true
requirements: [FIND-02, FIND-03]
must_haves:
  truths:
    - "(FIND-02, edge 5) the RAM predicate is exactly (type_key = 'laptop') AND (ram_upgraded IS NULL OR ram_upgraded != 1) — a NULL-ramUpgraded laptop is INCLUDED, ramUpgraded = 1 is EXCLUDED, non-laptops never match even with a hand-crafted ?ram=1&type=monitor URL"
    - "(FIND-03, edge 6) all filters (type/status/department/warranty/RAM) and q compose into ONE and() where shared by the count query and the rows query; invalid param values degrade to «all» equivalents (never a 500); every filter combines with every other and with q"
    - "(WAR-01, edge 8) warranty boundaries are inclusive and unified: wu == today → warn; wu == today + 60 → warn; wu == today + 61 → ok; wu == today - 1 → expired; ONE WARRANTY_WARN_DAYS = 60 constant serves BOTH the «Истекает ≤ 60 дней» filter AND the color state (a filter hit can never render green) — orchestrator resolution 1, closing RESEARCH Open Question 1 / UI-SPEC Defaults #3"
    - "(WAR-01, edge 9 — filter side) warrantyUntil NULL never matches any warranty filter; «Истекает ≤ N» presets are active-only windows (today ≤ wu ≤ today + N); «Истекла» is wu < today with wu non-null"
    - "(WAR-01/D-09, edge 10) list ordering stays the canonical RU-sort + devices.id tiebreaker in every filtered/searched view — equal keys resolve stably across pages"
    - "(UI-03, edge 11) pagination survives filtering: ?page beyond pages clamps to pages, ?page=0/negative/fractional clamps to 1 — no empty page, no SQL error; every filter change builds the query with page = 1"
    - "(UI-03/D-04, edge 12) empty-state precedence: zero devices overall with nothing active → «Пока нет устройств» (phase 3 verbatim); zero rows with ONLY the type filter → «Нет устройств этого типа» (phase 3 verbatim); zero rows with any of q/статус/отдел/гарантия/RAM active → «Ничего не найдено» + hint + «Сбросить фильтры»"
    - "(D-08) the RAM chip is a pill toggle beside the search field: activate writes type=laptop + the ram param with page=1; deactivate drops the ram param and keeps type=laptop; changing the type Select to a non-laptop drops the ram param (chip renders inactive) — chip active ⇔ ram param present AND type=laptop"
    - "(D-09) the department filter matches devices whose CURRENT holder belongs to the department: in-stock devices (no holder) never appear; devices held by an ARCHIVED employee DO appear in that employee's department"
    - "(D-10/D-11) status is a single Select with «Все статусы»; disposed devices remain visible by default and a serial search finds them with no status filter active"
    - "(D-12/D-13/D-14) all filters sit in ONE visible bar (flex-wrap allowed) in the order поиск → тип → статус → отдел → гарантия → RAM-чип; department choice is a Select (NOT the Base UI combobox — burned twice: bb5674e, 7e400c9); no separate active-filter chips — selects show their own values, reset via «Все …» options and the empty-state button"
    - "(D-15) warranty presets are exactly «Истекает ≤ 30 дней», «Истекает ≤ 60 дней» (listed first per UI-SPEC Default 12), «Истекла»; no arbitrary N-days input"
    - "(UI-SPEC error) invalid URL params normalize (unknown type/status/warranty → «all», non-integer page → 1) — never a 500; triggers show the normalized choice"
    - "(UI-SPEC partial) in-stock devices are correctly absent from department results (D-09) — склад remains visible via «Все статусы»/«На складе»; the existing «—» rendering for empty inventory numbers stands untouched in filtered rows"
    - "(UI-SPEC zero-one-many) «Найдено: …» fires when ANY dimension is active including type-only, and pluralDevices renders «Найдено: 0 устройств» above the empty state"
    - statement: "(UI-SPEC backstop, long-text) a long department name truncates with ellipsis in the sm:w-40 trigger and a 100-char query renders in the flex-1 field without breaking the one-bar wrap behavior — held-out visual check at end-of-phase"
      verification: backstop
  artifacts:
    - path: lib/warranty.ts
      provides: "WARRANTY_WARN_DAYS = 60, displayTodayUtc() (CR-01 wall-clock recipe), addDaysUtc(), WarrantyState, warrantyState() — pure, injectable clock"
    - path: db/queries/devices.ts
      provides: "warrantyPredicate() + status/department/ramNoUpgrade predicates composed into the single listDevices where; DeviceListFilters fully consumed"
    - path: "app/(app)/devices/warranty-filter.tsx"
      provides: "warranty Select island (WARRANTY_ITEMS, sm:w-48, aria-label «Фильтр по гарантии»)"
    - path: "app/(app)/devices/status-filter.tsx"
      provides: "status Select island built from DEVICE_STATUS_KEYS + deviceStatusLabel"
    - path: "app/(app)/devices/department-filter.tsx"
      provides: "department Select island fed by listDepartments() (flat items prop)"
    - path: "app/(app)/devices/ram-chip.tsx"
      provides: "RamChip toggle island with aria-pressed and D-08 query coupling"
    - path: "app/(app)/devices/filter-bar.tsx"
      provides: "FilterBar server composition: mt-6 flex flex-wrap items-center gap-2, one bar per D-12"
    - path: tests/warranty.test.ts
      provides: "frozen-clock boundary tests (MSK 00:30 vs UTC prior day; day 0/59/60/61; expired; null)"
    - path: tests/devices-queries.test.ts (extended)
      provides: "the filter matrix: RAM NULL semantics, department join semantics, composition, degrade-to-all, count/rows parity"
  key_links:
    - from: "lib/warranty.ts displayTodayUtc()"
      to: "warrantyPredicate SQL boundaries (gte/lte/lt bound Date operands)"
      via: "Intl.DateTimeFormat en-CA wall-clock parts in DISPLAY_TZ → Date.UTC midnight"
      why: "date('now')/server-clock math is the CR-01 bug class; boundaries are computed in JS and bound as values"
    - from: "RamChip / type + status + department + warranty Selects (all filter islands — incl. the rewired DeviceTypeFilter)"
      to: "buildDevicesQuery (query-params.ts from plan 01)"
      via: "router.push with the FULL query string, page=1"
      why: "URL is the only filter state; a bare param would drop the other filters (Pitfall 10)"
    - from: "department predicate"
      to: "the existing leftJoin(employees) in listDevices"
      via: "eq(employees.departmentId, filters.departmentId)"
      why: "in-stock NULL-holder rows drop out of the LEFT JOIN for free (D-09); archived employees join normally"
  prohibitions:
    - statement: "MUST NOT implement «без апгрейда RAM» as a comparison against a nominal/base RAM per model and MUST NOT introduce a model-nominal reference table or any schema change — the ONLY semantics is the explicit ramUpgraded flag, with NULL treated as not upgraded (FIND-02/D-06/D-07)"
      category: values
      status: resolved
      verification: test
    - statement: "MUST NOT introduce the Base UI combobox for the department (or any) filter — Select only (D-13; combobox burned the project twice: bb5674e, 7e400c9)"
      category: values
      status: resolved
      verification: judgment
---

<objective>
Combinable filters — «ноуты без апгрейда RAM» in one click (FIND-02), type/status/department/warranty-window filters that compose with each other and with search (FIND-03) — over the single where-builder from plan 01, with the one-bar FilterBar UI (D-12) and the warranty math foundation (lib/warranty.ts) that plan 03 reuses for coloring.

Purpose: «отфильтровал одним кликом» is the second Core Value moment; the unified inclusive 60-day boundary (orchestrator resolution 1) guarantees a filter hit can never render green.
Output: fully wired filter bar (5 islands), lib/warranty.ts + tests/warranty.test.ts, the complete filter matrix in tests/devices-queries.test.ts.
</objective>

<execution_context>
@/Users/aleksey/.zcode/gsd-core/workflows/execute-plan.md
@/Users/aleksey/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-search-filters/05-CONTEXT.md
@.planning/phases/05-search-filters/05-RESEARCH.md
@.planning/phases/05-search-filters/05-PATTERNS.md
@.planning/phases/05-search-filters/05-UI-SPEC.md
@.planning/phases/05-search-filters/05-01-SUMMARY.md
@lib/movement-schema.ts
@app/(app)/devices/query-params.ts
@app/(app)/devices/type-filter.tsx
@db/queries/employees.ts
@components/ui/select.tsx
@app/(app)/devices/page.tsx

AGENTS.md: verify Next.js APIs against node_modules/next/dist/docs/ before use (router.push options already verified for 16.3.3 in 05-RESEARCH).
Vercel React best practices: server-serialization — islands receive flat props (current value, plain filters object, items array), never functions; FilterBar is a server component composing islands, no client wrapper.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end warranty window — «Истекает ≤ 60 дней» filters the registry through one bar</name>
  <files>lib/warranty.ts, app/(app)/devices/warranty-filter.tsx, app/(app)/devices/filter-bar.tsx, app/(app)/devices/page.tsx, db/queries/devices.ts, tests/warranty.test.ts, tests/devices-queries.test.ts</files>
  <read_first>
    lib/movement-schema.ts lines 44-66 (the CR-01 zonedParts wall-clock recipe — h23 hourCycle, formatToParts) and lines 74-81 (the injectable-now discipline that makes frozen-clock tests possible)
    lib/ru.ts line 47 (DISPLAY_TZ — import it, never re-declare)
    05-RESEARCH.md Pattern 4 (displayTodayUtc, addDaysUtc, warrantyState, warrantyPredicate — probe-verified boundary semantics) and Pitfall 4
    db/queries/devices.ts (current where composition after plan 01)
    app/(app)/devices/query-params.ts (WARRANTY_ITEMS, parseDevicesSearchParams, buildDevicesQuery from plan 01)
    app/(app)/devices/type-filter.tsx (the Select island clone template)
    05-PATTERNS.md sections: lib/warranty.ts, warranty-filter.tsx, filter-bar.tsx
    05-UI-SPEC.md Screens table rows: Гарантия Select; filter bar; Subtitle
  </read_first>
  <action>
    1. lib/warranty.ts — NEW pure module (header discipline of lib/ru.ts: no framework imports, injectable clock). Exports exactly per RESEARCH Pattern 4: `WARRANTY_WARN_DAYS = 60` (the ONE constant — orchestrator resolution 1: unified INCLUSIVE boundary, filter and color alike; cite «resolved RESEARCH Open Question 1 / UI-SPEC Defaults #3» in the comment); `displayTodayUtc(now: Date = new Date()): Date` building the Europe/Moscow wall-clock calendar date via Intl.DateTimeFormat('en-CA', { timeZone: DISPLAY_TZ, ... }) parts → Date.UTC midnight (CR-01 recipe; en-CA gives yyyy-mm-dd directly); `addDaysUtc(day, days)`; `export type WarrantyState = 'ok' | 'warn' | 'expired' | 'none'`; `warrantyState(warrantyUntil: Date | null, today: Date): WarrantyState` — null → 'none'; wu < today → 'expired'; else days = Math.round((wu - today)/86_400_000), days <= WARRANTY_WARN_DAYS → 'warn', else 'ok'.
    2. db/queries/devices.ts — add private `warrantyPredicate(w)` per Pattern 4: for 'w30'/'w60' → and(isNotNull(devices.warrantyUntil), gte(devices.warrantyUntil, displayTodayUtc()), lte(devices.warrantyUntil, addDaysUtc(today, days))) (active-only — D-15 separates «истекает» from «истекла»); for 'expired' → and(isNotNull(devices.warrantyUntil), lt(devices.warrantyUntil, today)). Extend the listDevices where composition with the warranty term (the and() stays ONE object shared by count and rows — Pitfall 5). Import WARRANTY_WARN_DAYS/displayTodayUtc/addDaysUtc from '@/lib/warranty'. Date operands bind as unix seconds on the timestamp-mode column (probe-verified).
    3. app/(app)/devices/warranty-filter.tsx — NEW island cloned from type-filter.tsx: items = WARRANTY_ITEMS, value = current warranty ('all' default), onValueChange → router.push(buildDevicesQuery({ ...filters, warranty: value }), page reset handled by the builder) — FULL query string; trigger `h-10 w-full px-3 text-sm text-ink-secondary sm:w-48` (the wider width so «Истекает ≤ 60 дней» never truncates), aria-label «Фильтр по гарантии».
    4. app/(app)/devices/filter-bar.tsx — NEW server component (no client wrapper): props { filters: DeviceFilters } and it renders the bar `mt-6 flex flex-wrap items-center gap-2` containing DeviceSearchBox (from plan 01), DeviceTypeFilter (STILL on its phase-3 bare push at this point — Task 3 step 6 rewires it to buildDevicesQuery with a filters prop), and DeviceWarrantyFilter. The bar grows in Task 3; the composition replaces the page's ad-hoc filter row div.
    5. app/(app)/devices/page.tsx — swap the filter row for <FilterBar filters={filters} />; extend the D-05 subtitle trigger set with warranty ('warranty !== 'all'' counts as active → «Найдено: …»).
    6. tests/warranty.test.ts — NEW pure-lib tests with frozen clocks (pass `now` explicitly; pattern from tests/movement-schema.test.ts lines 17-22): today = displayTodayUtc(new Date('2026-09-03T21:30:00.000Z')) must equal Date.UTC(2026,8,4) — 2026-09-04 MSK wall clock (the RESEARCH MSK-00:30 pair: an instant whose UTC date is the prior day yields the next day's MSK «today»); boundaries against warrantyState: wu == today → 'warn'; wu == today+59 → 'warn'; wu == today+60 → 'warn'; wu == today+61 → 'ok'; wu == today-1 → 'expired'; null → 'none' (edge 8 + edge 9 render semantics live here).
    7. tests/devices-queries.test.ts — extend: seed devices with warrantyUntil at today/today+60/today+61/today-1/null (compute via the same lib/warranty helpers against a frozen expectation — the queries module calls displayTodayUtc() internally, so seed relative to the real current date using Date.now()-derived UTC midnights) and assert the w60 filter returns exactly the warn-band rows, w30 its subset, expired only the past ones, and null-warranty rows match NO warranty filter (edge 9 filter side).
  </action>
  <verify>
    <automated>npx vitest run tests/warranty.test.ts tests/devices-queries.test.ts && grep -n "WARRANTY_WARN_DAYS = 60" lib/warranty.ts && grep -n "sm:w-48" "app/(app)/devices/warranty-filter.tsx"</automated>
  </verify>
  <done>«Истекает ≤ 60 дней» in the one-bar FilterBar filters the registry end-to-end; the inclusive boundary (day 0 and day 60 warn, day 61 ok, yesterday expired) is proven by frozen-clock tests; the same WARRANTY_WARN_DAYS constant backs filter and future color; null warranty matches no warranty filter; the whole existing suite stays green</done>
  <reversibility rating="reversible">the unified inclusive boundary is one constant; switching to exclusive day-60 semantics later is a one-line change plus test updates.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Status + department + RAM predicates — the full filter matrix with NULL-safe D-07 SQL</name>
  <files>db/queries/devices.ts, tests/devices-queries.test.ts</files>
  <read_first>
    db/queries/devices.ts (the where composition as of Task 1; the leftJoin(employees) line; the covers batch that must remain untouched)
    05-RESEARCH.md Pattern 2 (the full and() builder) and Pitfall 1 (NULL is not ≠ 1 — probe-verified)
    05-CONTEXT.md D-06, D-07, D-09, D-10, D-11 (semantics) and orchestrator resolution 5 (the exact SQL shape)
    db/schema.ts (devices.ramUpgraded column, status CHECK) — READ ONLY, no schema edits allowed this phase
    app/(app)/devices/query-params.ts (the parsed filters this task consumes)
  </read_first>
  <behavior>
    - RAM chip semantics (edge 5): laptop with ramUpgraded NULL → INCLUDED in ramNoUpgrade; laptop with ramUpgraded 0 → included; laptop with ramUpgraded 1 → excluded; monitor/dock/peripheral with ramUpgraded NULL or 0 → NEVER included
    - department (edge 6/D-09): device assigned to an employee of dept X → matches deptId X; in-stock device (currentEmployeeId NULL) → never matches ANY department filter; device held by an ARCHIVED employee → still matches their department
    - status (D-10/D-11): status filter 'in_stock'/'assigned'/'repair'/'disposed' matches exactly; with no status filter, disposed devices appear in default and search results
    - composition (edge 6): q + type + status + department + warranty + ram combine — the total returned by listDevices equals the count of rows satisfying ALL predicates; count and rows share one where (a paged walk over pages yields exactly total rows, no dupes/gaps)
    - degrade (edge 6): parseDevicesSearchParams maps unknown status/warranty values to 'all', junk dept to null, junk ram to false — listDevices never sees an invalid enum and never 500s
    - ordering (edge 10): every filtered/searched result keeps the RU-sort + id tiebreaker
    - clamp (edge 11): page 0/-3/2.5 → page 1; page beyond pages → last page renders its rows
  </behavior>
  <action>
    Extend the listDevices where (db/queries/devices.ts) with the three remaining terms, each guarded by the presence of its parsed filter so inactive filters contribute nothing: (a) status → eq(devices.status, filters.status); (b) departmentId → eq(employees.departmentId, filters.departmentId) riding the EXISTING leftJoin — do not add a second join, do not filter employees.isActive (archived holders stay per D-09); (c) ramNoUpgrade → and(eq(devices.typeKey, 'laptop'), or(isNull(devices.ramUpgraded), ne(devices.ramUpgraded, 1))) — the exact orchestrator-resolution-5 SQL shape; plain != 1 silently drops NULL rows (probe-verified). The RAM term is self-limiting to laptops server-side, so a hand-crafted ?type=monitor&ram=1 is inert (never amplify the client coupling — RESEARCH Pattern 6). Keep ONE and() object shared by the count and rows queries.
    Write the failing matrix tests first in tests/devices-queries.test.ts (RED), then implement (GREEN): seed fixtures via createDevice + employee/department helpers (or raw inserts mirroring tests/helpers.ts) covering: the four RAM cases; the three department cases (assigned/in-stock/archived-holder — createEmployee then a movement-style update of devices.currentEmployeeId via db.update or an existing phase-4 query helper); disposed-visible-by-default (D-11: listDevices with q matching a disposed device's serial returns it); a full-combination case asserting count/rows parity by walking all pages; degrade cases driving parseDevicesSearchParams directly with junk URLSearchParams; ordering stability with duplicate models under an active filter; the clamp matrix via listDevices page args.
  </action>
  <verify>
    <automated>npx vitest run tests/devices-queries.test.ts</automated>
  </verify>
  <done>the full filter matrix is green: NULL-ramUpgraded laptops included, upgraded excluded, non-laptops never match; department = holder's dept with in-stock excluded and archived holders included; disposed visible by default; all filters + q compose with count/rows parity; junk params degrade; ordering and clamps hold under filters; no schema file was touched</done>
</task>

<task type="auto">
  <name>Task 3: Filter bar completion — status/department Selects, RAM chip coupling, empty-state precedence</name>
  <files>app/(app)/devices/status-filter.tsx, app/(app)/devices/department-filter.tsx, app/(app)/devices/ram-chip.tsx, app/(app)/devices/filter-bar.tsx, app/(app)/devices/page.tsx, app/(app)/devices/type-filter.tsx</files>
  <read_first>
    app/(app)/devices/type-filter.tsx (the island template — 'use client', Select usage, full-query push)
    app/(app)/devices/query-params.ts (buildDevicesQuery — every island routes through it)
    lib/device-schema.ts (DEVICE_STATUS_KEYS, deviceStatusLabel from plan 01)
    db/queries/employees.ts listDepartments lines 86-97 (already RU-sorted; returns { id, name })
    05-RESEARCH.md Pattern 6 (bar composition, chip coupling, subtitle anyActive, empty-state copy)
    05-UI-SPEC.md Screens rows: Тип/Статус/Отдел/Гарантия Selects; RAM chip; Empty states precedence; Defaults #4/#5/#6/#7/#13/#15
    components/ui/select.tsx, components/ui/button.tsx (chip press rule active:scale-[0.97])
    app/(app)/devices/page.tsx (current empty-state block from plan 01)
  </read_first>
  <action>
    1. status-filter.tsx — island: items = [{ value: 'all', label: 'Все статусы' }, ...DEVICE_STATUS_KEYS.map(k => ({ value: k, label: deviceStatusLabel(k) }))] (read the keystone — D-10; option order in_stock/assigned/repair/disposed per the keystone); trigger `h-10 w-full px-3 text-sm text-ink-secondary sm:w-40`, aria-label «Фильтр по статусу»; onValueChange → router.push(buildDevicesQuery({ ...filters, status: value })).
    2. department-filter.tsx — island receiving `items: { id: number; name: string }[]` as a flat prop (FilterBar, a server component, calls listDepartments() once and passes them down — server-serialization); internal option list = [{ value: 'all', label: 'Все отделы' }, ...items.map(d => ({ value: String(d.id), label: d.name }))]; zero departments → the single «Все отделы» option (never a dead dropdown — UI-SPEC Default 15); trigger sm:w-40, aria-label «Фильтр по отделу», Select NOT combobox (D-13); long names truncate in the trigger (truncate class); onValueChange → buildDevicesQuery({ ...filters, departmentId: value === 'all' ? null : Number(value) }).
    3. ram-chip.tsx — island button «Без апгрейда RAM», pill `h-10 px-3 rounded-full`, inactive `bg-black/5 text-ink-secondary hover:text-ink`, active `bg-ink text-white` (neutral-strong — NOT accent, UI-SPEC Default 4), global press rule active:scale-[0.97] duration-100 ease-out, aria-pressed carrying the state (D-08 + UI-SPEC). Click behavior via buildDevicesQuery: when inactive → { ...filters, type: 'laptop', ramNoUpgrade: true }; when active → { ...filters, ramNoUpgrade: false } keeping type=laptop; the chip renders active ⇔ filters.ramNoUpgrade AND filters.type === 'laptop' (a non-laptop type drops the ram param in the builder — UI-SPEC Default 5).
    4. filter-bar.tsx — complete the composition in the D-12 order: DeviceSearchBox → DeviceTypeFilter (now passed the same flat filters prop as the other islands) → DeviceStatusFilter → DeviceDepartmentFilter → DeviceWarrantyFilter → RamChip (the CSV link joins in plan 04 at the end with ml-auto). No popover, no active-filter chips (D-12/D-14); flex-wrap to a second line at max-w-3xl is correct behavior (UI-SPEC Default 1).
    5. page.tsx — extend the empty-state block to the three-state precedence (edge 12/D-04/UI-SPEC Default 7): compute anySearchFilter = q non-empty OR status !== 'all' OR departmentId non-null OR warranty !== 'all' OR ramNoUpgrade; when rows are empty AND type === 'all' AND NOT anySearchFilter → existing «Пока нет устройств» verbatim; when rows empty AND type !== 'all' AND NOT anySearchFilter → existing «Нет устройств этого типа» verbatim; when rows empty AND anySearchFilter → the plan-01 «Ничего не найдено» state (hint + «Сбросить фильтры» Link to bare /devices). Extend the subtitle trigger (D-05/Default 6): «Найдено: …» when any of q/type/status/department/warranty/RAM is active (type-only counts); else the plain counter. Pass departments into FilterBar via listDepartments().
    6. type-filter.tsx — rewire to the shared builder (FIND-03 edge 6; closes the last bare push in the app): add a flat `filters: DeviceFilters` prop (server-serialization — FilterBar passes it), and replace the phase-3 bare push (`?type=${value}&page=1`, which wipes q/status/dept/warranty/ram on every type change) with `onValueChange → router.push(buildDevicesQuery({ ...filters, type: value }))` — page reset stays the builder's job, and the builder drops the ram param when value is a non-laptop (D-08 coupling, per the plan-01 Task 1 step 5 builder rule). Also narrow the trigger to sm:w-40 for row consistency (UI-SPEC §Spacing).
  </action>
  <verify>
    <automated>npx vitest run && grep -n "aria-label=\"Фильтр по статусу\"" "app/(app)/devices/status-filter.tsx" && grep -n "aria-label=\"Фильтр по отделу\"" "app/(app)/devices/department-filter.tsx" && grep -n "aria-pressed" "app/(app)/devices/ram-chip.tsx" && grep -cn "Combobox\|combobox" "app/(app)/devices/department-filter.tsx" | grep -qx "0" && echo ISLANDS-OK</automated>
  </verify>
  <done>the one-bar FilterBar renders поиск → тип → статус → отдел → гарантия → RAM-чип with D-12/D-13/D-14 discipline; the chip writes/drops the ram+type params per D-08 while the server predicate stays self-limiting; a type-Select change pushes buildDevicesQuery({ ...filters, type: value }) so q/status/dept/warranty/ram survive (no bare push remains anywhere in the bar); the three-state empty-state precedence matches UI-SPEC copy verbatim; «Найдено: …» fires for any active dimension including type-only; full suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → URL searchParams (status/dept/warranty/ram) | untrusted values cross into SQL predicates and enum switches |
| hand-crafted URLs (e.g. ?type=monitor&ram=1) | hostile param combinations probing filter amplification |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Tampering | filter params → SQL | high | mitigate | every enum via keystone guards (isDeviceTypeKey/isDeviceStatusKey/WARRANTY_ITEMS set), dept via zod positive-int coerce; all values bind as drizzle parameters — no user string reaches SQL text (RESEARCH V5) |
| T-05-05 | DoS / Information Disclosure (V7) | invalid params causing errors or internals leakage | medium | mitigate | degrade-to-«all» parsing — unknown values normalize, never 500, no internals echoed (matches the established invalid-type→all behavior) |
| T-05-06 | Elevation via client trust | ram chip coupling assumed enforced client-side | low | accept | the server predicate self-limits to laptops, making hostile combos inert; the client coupling is UX only — accepted because the server guard is the real boundary |
| T-05-SC | Tampering | npm installs | none | accept | zero new packages this phase — no supply-chain surface |
</threat_model>

<verification>
- npx vitest run tests/warranty.test.ts — inclusive boundary matrix (day 0/59/60/61, expired, null, MSK-00:30 frozen clock)
- npx vitest run tests/devices-queries.test.ts — full filter matrix + composition + degrade + clamp
- npx vitest run — whole suite green
- grep proves island aria-labels, aria-pressed, sm:w-48 warranty trigger; no combobox import in the new filter islands
- Manual-only (end-of-phase human-check per 05-VALIDATION.md): live filter feel, one-bar wrap on narrow viewport
</verification>

<success_criteria>
- «Без апгрейда RAM» is one click and returns the complete correct list (0 and NULL both included, non-laptops excluded)
- Type, status, department, warranty-window filters and search combine freely; the URL round-trips every filter; invalid URLs degrade gracefully
- The unified 60-day inclusive warranty window is proven at the SQL boundary and backed by one shared constant
</success_criteria>

## Artifacts this phase produces

- `lib/warranty.ts`: `WARRANTY_WARN_DAYS`, `displayTodayUtc()`, `addDaysUtc()`, `WarrantyState`, `warrantyState()`
- `warrantyPredicate()` + status/department/RAM predicates composing into the single `listDevices` where in `db/queries/devices.ts`
- `DeviceWarrantyFilter` (warranty-filter.tsx), `DeviceStatusFilter` (status-filter.tsx), `DeviceDepartmentFilter` (department-filter.tsx), `RamChip` (ram-chip.tsx), `FilterBar` (filter-bar.tsx)
- extended `tests/warranty.test.ts` (NEW) and `tests/devices-queries.test.ts` (filter matrix)

<output>
Create `.planning/phases/05-search-filters/05-02-SUMMARY.md` when done
</output>
