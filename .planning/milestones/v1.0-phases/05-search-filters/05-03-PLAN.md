---
phase: 05-search-filters
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - app/globals.css
  - lib/warranty-date.tsx
  - db/queries/devices.ts
  - db/queries/movements.ts
  - "app/(app)/devices/page.tsx"
  - "app/(app)/(card)/devices/[id]/page.tsx"
  - "app/(app)/(card)/employees/[id]/page.tsx"
  - tests/movements-queries.test.ts
autonomous: true
requirements: [WAR-01]
must_haves:
  truths:
    - "(WAR-01, edge 9 — render side) warrantyUntil NULL renders as «no state»: no color anywhere, the «гар. до …» segment is OMITTED in the registry rows and the employee-card issued list, and the device card shows «—» under «Гарантия до»; null devices never render a warranty color"
    - "(WAR-01/D-16) the warranty state is colored at exactly THREE render sites — registry row line 2 (« · гар. до dd.mm.yyyy»), device card «Гарантия до» value, employee-card issued list line 2 — all through ONE WarrantyDate component and ONE warrantyState() calculation"
    - "(WAR-01/D-17 + resolution 2) colors: ok = #248A3D (--color-warranty-ok — the checker D3 rec, NOT #34C759), warn = #FF9500 (--color-warranty-warn), expired = #D70015 (--color-destructive — the same one red as «Списать»); text color only — never bold, never a pill, never a background, never an icon"
    - "(edge 8 continuity) the color state uses the SAME warrantyState()/WARRANTY_WARN_DAYS = 60 inclusive constant as the plan-02 filter — a device found by «Истекает ≤ 60 дней» can never render green; expiring today renders warn"
    - "(UI-SPEC overflow) row line 2 stays truncate with its title attribute extended to include «гар. до …»; the appended segment never wraps the row"
    - "(D-16 typography) WarrantyDate inherits the local text role and changes ONLY the color — 14/400 in lists, 16/400 on the card; the warranty segment is NOT mono (a date, not an identifier)"
  artifacts:
    - path: app/globals.css
      provides: "--color-warranty-ok: #248A3D and --color-warranty-warn: #FF9500 in the @theme block (the only CSS edit of the phase; expired reuses --color-destructive)"
    - path: lib/warranty-date.tsx
      provides: "WarrantyDate server component — list variant (whole «гар. до {дата}» segment colored) and card variant (date value colored, «—» when null)"
    - path: db/queries/devices.ts
      provides: "warrantyUntil added to the listDevices row select (type DeviceRow already carries it)"
    - path: db/queries/movements.ts
      provides: "IssuedDeviceView widened with warrantyUntil: Date | null + the field added to the listIssuedByEmployee select (~line 389; type at lines 34-39 — orchestrator resolution 3)"
    - path: tests/movements-queries.test.ts (extended)
      provides: "listIssuedByEmployee returns warrantyUntil (Date | null) per issued row"
  key_links:
    - from: "WarrantyDate (all three sites)"
      to: "warrantyState() + WARRANTY_WARN_DAYS in lib/warranty.ts (plan 02)"
      via: "today = displayTodayUtc() computed once per page render and passed down"
      why: "one calculation, three render sites (D-16 literal); boundaries identical to the filter's"
    - from: "employee-card issued rows"
      to: "devices.warrantyUntil via listIssuedByEmployee"
      via: "one added select field — no migration, the column exists"
      why: "Pitfall 9: the component cannot color what the query does not select"
  prohibitions:
    - statement: "MUST NOT introduce a second red or additional colored warranty surfaces — no backgrounds, pills, dots, icons or bold on warranty state; #D70015 stays the one red in the system (shared with the «Списать» action) and the warranty date is text-color only (WAR-01/D-17, UI-SPEC Color table)"
      category: safety
      status: resolved
      verification: test
---

<objective>
Warranty state made visible everywhere the user looks (WAR-01): colored warranty dates in the registry rows, the device card, and the employee card's issued list — one WarrantyDate component, one inclusive 60-day calculation, tokens #248A3D / #FF9500 / #D70015.

Purpose: «увидел истекающую гарантию» — the third Core Value moment; WAR-01 says «card and lists» and the plan takes it literally (D-16's three sites).
Output: two new @theme tokens, WarrantyDate, three wired render sites, listIssuedByEmployee widened, movements tests extended.
</objective>

<execution_context>
@/Users/aleksey/.zcode/gsd-core/workflows/execute-plan.md
@/Users/aleksey/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-search-filters/05-CONTEXT.md
@.planning/phases/05-search-filters/05-RESEARCH.md
@.planning/phases/05-search-filters/05-PATTERNS.md
@.planning/phases/05-search-filters/05-UI-SPEC.md
@.planning/phases/05-search-filters/05-02-SUMMARY.md
@lib/warranty.ts
@app/globals.css
@app/(app)/devices/page.tsx
@app/(app)/(card)/devices/[id]/page.tsx
@app/(app)/(card)/employees/[id]/page.tsx
@db/queries/movements.ts

Vercel React best practices: WarrantyDate is a server component (no client JS); displayTodayUtc() is computed once per page render and passed as a prop (per-render, not per-row — the research server-hoist note).
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end warranty color — registry rows show «гар. до 12.03.2027» in state color</name>
  <files>app/globals.css, lib/warranty-date.tsx, db/queries/devices.ts, app/(app)/devices/page.tsx</files>
  <read_first>
    app/globals.css @theme block lines 54-68 (literal-hex tokens with inline UI-SPEC rationale comments — copy the comment discipline)
    05-UI-SPEC.md §Color (new tokens + the four-state table: ok/warn/expired/none rules) and §Visual Details (WarrantyDate is a colored text span; the whole segment colored in lists, the date value only on the card)
    05-PATTERNS.md sections: app/globals.css, WarrantyDate component (list-site segment shape from the employee card lines 74-82; missing-value rule from the device card Value lines 147-153; card-site swap at lines 298-301)
    lib/warranty.ts (warrantyState, displayTodayUtc — from plan 02)
    db/queries/devices.ts (listDevices select lines 128-136; DeviceRow already has warrantyUntil at line 29)
    app/(app)/devices/page.tsx (row lines 104-162 — line 2 span and the title array)
  </read_first>
  <action>
    1. app/globals.css — add exactly two tokens to the existing @theme block, each with its rationale comment per the established discipline: `--color-warranty-ok: #248A3D;` (derived — contrast-safe Apple green, AA on white, checker D3 rec 2026-09-04; resolution 2 — NOT #34C759) and `--color-warranty-warn: #FF9500;` (locked D-17). Expired deliberately gets NO token — it reuses --color-destructive (#D70015, D-17: один красный). Touch nothing else in the file.
    2. lib/warranty-date.tsx — NEW server component (no 'use client'): `WarrantyDate({ value, today, variant }: { value: Date | null; today: Date; variant: 'list' | 'card' })`. Compute state = warrantyState(value, today) from '@/lib/warranty'. Map: ok → text-warranty-ok, warn → text-warranty-warn, expired → text-destructive, none → no color. Format the date as dd.mm.yyyy in UTC (the device-card dateFormat convention — UTC-midnight stored dates; replicate the existing Intl formatter shape from the device card page, module-level instance). variant 'list': render null when state is 'none' (segment omitted); otherwise the whole segment « · гар. до {дата}» (leading separator included, matching the row's ' · ' join idiom) inside a span carrying the state color. variant 'card': render «—» (text-ink-secondary, the existing missing-value rule) when value is null; otherwise the date value in the state color, 16/400 — label untouched.
    3. db/queries/devices.ts — add `warrantyUntil: devices.warrantyUntil` to the listDevices row select (the DeviceRow type already carries it — no type change needed; keep the covers batch untouched).
    4. app/(app)/devices/page.tsx — compute `const today = displayTodayUtc()` ONCE per render; append <WarrantyDate value={row.warrantyUntil} today={today} variant="list" /> as the final segment of the row's line-2 span (after the holder segment); extend the Link title array with the formatted «гар. до …» string only when warrantyUntil is non-null (UI-SPEC overflow row). Existing segments (тип · серийник mono · инвентарник mono · держатель) unchanged.
    UI-SPEC covered considerations this implements: «partial — без гарантии devices» (segments omitted, card «—», never colored) and «overflow — row line 2 with appended warranty segment».
  </action>
  <verify>
    <automated>npx vitest run tests/devices-queries.test.ts && grep -n "color-warranty-ok: #248A3D" app/globals.css && grep -n "color-warranty-warn: #FF9500" app/globals.css && grep -n "warrantyUntil: devices.warrantyUntil" db/queries/devices.ts && grep -n "variant=\"list\"" "app/(app)/devices/page.tsx" && ! grep -q "34C759" app/globals.css && echo TOKENS-OK</automated>
  </verify>
  <done>registry rows render the colored «гар. до …» segment via one WarrantyDate component; listDevices rows carry warrantyUntil; the two new tokens exist with #248A3D (not #34C759); «без гарантии» rows render line 2 unchanged (no segment, no color); the row title includes the warranty segment when present</done>
</task>

<task type="auto">
  <name>Task 2: Card sites — device card «Гарантия до» colored value + employee-card issued list segment</name>
  <files>db/queries/movements.ts, app/(app)/(card)/devices/[id]/page.tsx, app/(app)/(card)/employees/[id]/page.tsx, tests/movements-queries.test.ts</files>
  <read_first>
    db/queries/movements.ts (IssuedDeviceView type lines 34-39; listIssuedByEmployee lines 389-429 — the select gains ONE field; ruSortKey ordering stays)
    app/(app)/(card)/devices/[id]/page.tsx (FieldRow/Value recipe lines 136-153; the «Гарантия до» FieldRow at lines 298-301 with its phase-3 "no coloring yet" comment to replace; module-level UTC dateFormat lines 38-42)
    app/(app)/(card)/employees/[id]/page.tsx (IssuedSection lines 39-41 + issued line-2 segments lines 74-82)
    lib/warranty-date.tsx (the card variant from Task 1)
    05-PATTERNS.md sections: movements.ts extension, WarrantyDate card site
    05-UI-SPEC.md Screens rows: /devices/[id] «Гарантия до»; Employee card «Техника» rows
  </read_first>
  <action>
    1. db/queries/movements.ts — widen `IssuedDeviceView` with `warrantyUntil: Date | null` (lines 34-39) and add `warrantyUntil: devices.warrantyUntil` to the listIssuedByEmployee select (~line 389). One field; no join changes; the two-step issued+latest structure untouched. Do NOT widen with any-typing (Pitfall 9 — the compiler error at the component is the signal the wiring is complete).
    2. app/(app)/(card)/devices/[id]/page.tsx — swap the «Гарантия до» FieldRow value for <WarrantyDate value={device.warrantyUntil} today={displayTodayUtc()} variant="card" /> (16/400 local card-value role; null → «—» via the component); remove the superseded phase-3 "no coloring yet" comment; import displayTodayUtc from '@/lib/warranty' and WarrantyDate from '@/lib/warranty-date'.
    3. app/(app)/(card)/employees/[id]/page.tsx — in the issued list line 2, append the warranty segment after «выдано {дата}»: <WarrantyDate value={device.warrantyUntil} today={displayTodayUtc()} variant="list" />; the segment (including its ' · ' separator) disappears entirely when warrantyUntil is null. Line 2 becomes «{серийник mono} · выдано {dd.mm.yyyy} · гар. до {dd.mm.yyyy}» per the UI-SPEC Screens row; the serial stays mono, the warranty segment is not mono.
    4. tests/movements-queries.test.ts — extend (existing harness): seed an assigned device with warrantyUntil set and one with null; assert listIssuedByEmployee returns warrantyUntil as a Date for the first and null for the second (the select-widening contract plan WAR-01 site 3 depends on).
  </action>
  <verify>
    <automated>npx vitest run tests/movements-queries.test.ts && grep -n "variant=\"card\"" "app/(app)/(card)/devices/[id]/page.tsx" && grep -n "variant=\"list\"" "app/(app)/(card)/employees/[id]/page.tsx" && npx vitest run</automated>
  </verify>
  <done>the device card colors the «Гарантия до» value (or «—» when null) and the employee card's issued rows carry the colored «гар. до …» segment (omitted when null); listIssuedByEmployee returns warrantyUntil with the type widened — all three D-16 sites now render from the same WarrantyDate + warrantyState; full suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored device fields (model/serial/notes) → colored render sites | operator-entered data rendered inside new spans |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Tampering (stored XSS via new spans) | WarrantyDate render sites | low | accept | WarrantyDate renders operator-entered VALUES only through React text nodes (auto-escaped) and static copy; no dangerouslySetInnerHTML anywhere in the component; accepted — no additional surface beyond the existing phase-3/4 row rendering |
| T-05-08 | Repudiation (misread state) | color semantics divergence filter vs color | medium | mitigate | single warrantyState()/WARRANTY_WARN_DAYS source shared by filter (plan 02) and color (this plan) — divergence is unrepresentable by construction; frozen-clock tests pin the boundary |
| T-05-SC | Tampering | npm installs | none | accept | zero new packages this phase — no supply-chain surface |
</threat_model>

<verification>
- npx vitest run tests/movements-queries.test.ts — select-widening contract
- npx vitest run — whole suite green
- grep proves both tokens, the listDevices select field, and both card-site variant usages; the #34C759 hex appears nowhere in globals.css
- Visual color/truncation checks are documented manual-only items for end-of-phase human-check (05-VALIDATION.md) — long department names and row-2 overflow per UI-SPEC backstop are covered by the title/truncate classes this plan sets
</verification>

<success_criteria>
- Every list row, the device card and the employee card show the warranty state in the correct color — green #248A3D beyond 60 days, orange #FF9500 within the inclusive 60-day window, red #D70015 expired, nothing when «без гарантии»
- One component and one calculation serve all three sites; a filter hit can never render green
</success_criteria>

## Artifacts this phase produces

- `--color-warranty-ok` / `--color-warranty-warn` tokens in `app/globals.css`
- `WarrantyDate` server component in `lib/warranty-date.tsx` (variants list/card)
- `warrantyUntil` in the `listDevices` row select (`db/queries/devices.ts`) and in `listIssuedByEmployee` + `IssuedDeviceView` (`db/queries/movements.ts`)
- extended `tests/movements-queries.test.ts`

<output>
Create `.planning/phases/05-search-filters/05-03-SUMMARY.md` when done
</output>
