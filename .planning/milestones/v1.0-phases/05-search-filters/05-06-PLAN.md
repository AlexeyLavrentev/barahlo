---
phase: 05-search-filters
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - "app/(app)/devices/search-box.tsx"
autonomous: true
gap_closure: true
gap_ids: [G-5-1]
requirements: [FIND-01]
must_haves:
  truths:
    - "Быстрый ввод/удаление символов в поиске не теряет keystrokes и не откатывает ввод — ни в окне взвод→эхо, ни пока навигация летит (truth G-5-1)"
    - "Внешние изменения q («Сбросить фильтры», Back/Forward) по-прежнему принимаются в инпут, когда локальных правок нет (UAT Test 2 сохранён)"
    - "Серверный trim-эхо по-прежнему нормализует инпут (push «abc » → инпут «abc») и не порождает навигационную петлю (CR-01)"
    - "Enter коммитит немедленно; эхо Enter-пуша не откатывает текст, набранный в полёте"
  artifacts:
    - "app/(app)/devices/search-box.tsx — эффект реконсиляции с локальным приоритетом: внешняя адаптация только при value === lastSynced.current && q !== lastSynced.current; собственные эхо абсорбируются через inFlight-реф без перезаписи свежего ввода; lastSynced штампуется в момент пуша, а не при взводе таймера"
    - ".planning/phases/05-search-filters/05-06-SUMMARY.md — с инструкцией ручного ретеста UAT Test 1"
  key_links:
    - "Инвариант веток эффекта: ни один ранний return не оставляет value !== q без взведённого таймера (именно это старая adopt-ветка ломала — текст никогда не уходил пушем)"
    - "inFlight-реф ↔ классификация эха: q, совпавший с неисключенным собственным пушем (или его trim), абсорбируется молча — без setValue поверх более новых правок"
    - "commitNow использует тот же inFlight: эхо немедленного Enter-пуша классифицируется как своё, а не как внешнее"
---

<objective>
Close gap G-5-1 (major, UAT Test 1): быстрый ввод/удаление символов в поиске /devices терял keystrokes или откатывал удаления. Корневая причина подтверждена debug-сессией (.planning/debug/search-input-keystroke-loss.md): CR-01-эффект реконсиляции в search-box.tsx принимает ЛЮБУЮ разницу q-пропа от lastSynced за внешнюю навигацию, но lastSynced штампуется при взводе дебаунса (строка 57) — до прихода эха. Всё окно взвод→эхо (≥300 мс по дизайну) каждое нажатие выглядит внешним изменением: adopt-ветка (47–51) затирает свежий ввод setValue(q) и выходит без перевзвода таймера (66–73) — текст не только откатился, он больше не уйдёт пушем. commitNow (83) имеет то же окно в полёте.

Направление фикса от дебаггера, проверенное против кода при планировании: инвертировать приоритет — локальные правки побеждают URL. Адаптировать q ТОЛЬКО когда инпут чист (value === lastSynced.current && q !== lastSynced.current); при грязном инпуте — никогда не адаптировать, перевзводить дебаунс.

Адаптация против «голого» clean-check (обоснование в action Task 1): сам по себе он не закрывает truth «не откатывается к старому q из серверного эха, пока летит навигация» — при штампе на взводе более новое нажатие перештамбовывает lastSynced, инпут выглядит чистым, и эхо СТАРГО пуша адаптируется поверх свежего текста (трасса: «a» → push → «b» @320 → эхо «a» @350 → откат «ab»→«a»). Поэтому lastSynced штампуется в момент пуша (внутри setTimeout-колбэка / commitNow), а собственные эхо классифицируются через inFlight-реф и абсорбируются без записи в инпут.

Purpose: FIND-01 «мгновенный точный ответ» не работает, если поле поиска ест символы; это единственный провал UAT фазы 5 (5/6 зелёные).
Output: исправленный search-box.tsx (единственный файл продакшн-кода), SUMMARY с инструкцией ручного ретеста UAT Test 1; все 278 тестов зелёные.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-search-filters/05-UAT.md
@.planning/debug/search-input-keystroke-loss.md
@app/(app)/devices/search-box.tsx
@.planning/phases/05-search-filters/05-UI-SPEC.md

Контракт SearchBox (05-UI-SPEC.md, строка 127, реализует D-01/D-02): controlled input ВНЕ `<form action>` (React 19 reset), 300 мс дебаунс → router.replace(buildDevicesQuery(...), { scroll: false }) внутри startTransition; Enter коммитит немедленно; повторный ввод отменяет взведённый таймер; value === q guard против no-op пушей; фокус/value переживают серверную подмену. ВСЁ это сохраняется — фикс меняет только логику реконсиляции внутри эффекта.

Референс регрессии: 05-RESEARCH.md Pattern 3 (строки ~293–323) — исходная форма без lastSynced (`if (value === q) return` → arm): печатает корректно, но не адаптирует внешние сбросы. CR-01-фикс be80413 добавил lastSynced и сломал грязный инпут. Новый код объединяет оба поведения.

AGENTS.md: новых Next API не используется — router.replace/startTransition уже работают в этом файле; в node_modules/next/dist/docs/ заглядывать не нужно, если не меняешь навигационную семантику (не меняй).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Реконсиляция SearchBox с локальным приоритетом — adopt только при чистом инпуте, собственные эхо абсорбируются</name>
  <files>app/(app)/devices/search-box.tsx</files>
  <action>
Переписать эффект реконсиляции (сейчас строки 38–74) и commitNow (77–89), сохранив props-контракт, 300 мс дебаунс, router.replace(scroll:false) в startTransition, buildDevicesQuery, maxLength, плейсхолдер и разметку без изменений. Изменения — только в хуковой логике и комментариях (стиль комментарий файла — английский, поясняющий; обнови комментарии строк 31–34 и 44–57 под новую семантику, CR-01-ссылки сохрани).

Состояние: к существующему рефу lastSynced (якорь «что сервер уже держит») добавь реф inFlight типа string[] — значения наших пушей, эхо которых ещё не вернулось. Refs мутируй только внутри эффекта/колбэков, в рендере не читай.

Ветки эффекта СТРОГО в этом порядке (deps остаются [value, q, current, router]):

1. Mount guard — без изменений: первый запуск ставит mounted и lastSynced = q, return.
2. Собственное эхо: найди индекс в inFlight значения p, для которого q === p ИЛИ q === p.trim() (сервер тримит q — query-params.ts; maxLength 100 делает cap недостижимым из инпута). Если найдено: удали p из inFlight, поставь lastSynced = q, и вызывай setValue(q) ТОЛЬКО если value === p — инпут не трогали с момента пуша; это сохраняет Test-2 trim-эхо (push «abc » → инпут становится «abc»). Если после этого value === q — return; иначе проваливайся вниз на взвод (инвариант из must_haves: return с value !== q без таймера запрещён — именно так старая adopt-ветка теряла текст навсегда).
3. Внешняя адаптация (CR-01, UAT missing item 1): ТОЛЬКО если value === lastSynced.current && q !== lastSynced.current — инпут чист, локальных правок нет, значит q изменился извне («Сбросить фильтры», Back/Forward): lastSynced = q, setValue(q), return (после этого value === q, таймер не нужен).
   При value !== lastSynced.current (грязный инпут) — НИКОГДА не адаптировать (UAT missing item 2): проваливайся вниз, более новый текст уйдёт пушем. Продуктовое решение подтверждено планированием: пользователь, печатающий в момент чужой навигации, сохраняет свой текст; current в пуша берётся свежий из пропса, так что сброс OTHER фильтров не откатывается.
4. No-op guard — без изменений: value === q → return (Pitfall 6).
5. Взвод дебаунса: setTimeout 300 мс, колбэк делает ровно то, что сейчас, ПЛЮС штамп в момент пуша, а не при взводе: внутри колбэка перед startTransition — добавь value в inFlight (после добавления, если длина больше 4, удали старейший shift-ом — защита от зависших записей) и поставь lastSynced.current = value. Перенос штамба из строки 57 внутрь колбэка — ключевая часть фикса: при штамбе на взводе эхо старшего пуша выглядит «внешним» (см. обоснование в objective). Cleanup (сейчас 66–73) — без изменений.

commitNow (Enter, UAT missing item 3): тело сохранить (очистка таймера, value === q guard, lastSynced = value, startTransition replace), но перед штамбом lastSynced добавь value в inFlight тем же способом (с тем же cap). Тогда эхо Enter-пуша классифицируется веткой 2 и не откатит текст, набранный в полёте.

Чего НЕ делать (scope-запреты): не менять интервал 300 мс, не переводить инпут в `<form action>`/server action, не добавлять пакеты (T-05-SC), не трогать query-params.ts/db-слой/разметку, не вводить счётчики переходов или очередь навигаций — inFlight-реф значений достаточен (все сценарии трассы ниже покрывает).

Контрольные трассы для самопроверки логики (все обязаны сходиться; источник — сценарии A–D debug-сессии):
- Быстрое «a»+«b» @80 мс: ни одного adopt (q === lastSynced на каждом ране), один push «ab» — символы не теряются (сценарий A, раньше финал был «» без единой навигации).
- Быстрый delete из «abc»: adopt не срабатывает (q === lastSynced), удаления не откатываются (сценарий C, раньше откат к «abc»).
- Печать в полёте: push «a» @300, «b» @320 (грязный → перевзвод), эхо «a» @350 — своё (матчит inFlight), setValue НЕ вызывается, инпут остаётся «ab», перевзвод толкает «ab» (сценарий D, раньше финал «a»).
- Trim-эхо: push «abc » → эхо «abc» матчит trim → инпут «abc», без повторного пуша (Test 2, CR-01 петля мертва).
- Сброс при чистом инпуте: q="" не матчит inFlight (пуст), value === lastSynced → adopt, инпут пустеет (Test 2).
  </action>
  <verify>
    <automated>cd /Users/aleksey/projects/barahlo && set -o pipefail && npx vitest run 2>&1 | tail -3 && npm run lint && grep -q "value === lastSynced.current" "app/(app)/devices/search-box.tsx" && grep -q "q === p.trim()" "app/(app)/devices/search-box.tsx" && [ "$(grep -c inFlight "app/(app)/devices/search-box.tsx")" -ge 4 ] && echo STRUCTURE-OK</automated>
  </verify>
  <done>Сьюта 278 тестов / 18 файлов зелёная, lint чист; в search-box.tsx есть clean-check адаптации (value === lastSynced.current), trim-матч собственных эхо и реф inFlight, используемый и в эффекте, и в commitNow; трассы A–D из action сходятся при чтении кода.</done>
  <reversibility rating="reversible">Один файл, изолированная клиентская логика; git revert возвращает CR-01-поведение be80413 без затрагивания остальных 5/6 зелёных пунктов UAT.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Регрессионный прогон + SUMMARY с инструкцией ручного ретеста UAT Test 1</name>
  <files>.planning/phases/05-search-filters/05-06-SUMMARY.md</files>
  <action>
Прогони полный чек-лист регрессии и зафиксируй результат в SUMMARY (по шаблону summary.md):

1. `npx vitest run` — все 278 тестов зелёные (18 файлов); отдельно прогони tests/device-search.test.ts и tests/normalize.test.ts — FIND-01/FIND-04 матрица не задета (фикс не трогал db/query-params слои, но прогон обязателен).
2. `npm run lint` — чисто.
3. Коммит фикса: `fix(05-06): search-box reconciliation — local edits win, own echoes absorbed (G-5-1)` (код + SUMMARY одним коммитом допустимо).
4. В SUMMARY перечисли: gap G-5-1 → корневая причина (штамб lastSynced на взводе + adopt-ветка без различения своих эхо), форма фикса (три пункта UAT missing: clean-check адаптация; перевзвод при грязном инпуте; защита commitNow — плюс адаптация планирования: штамб в момент пуша и inFlight-абсорбция), ключевые трассы A–D, и раздел «Manual retest (UAT Test 1)» с точными шагами ниже — их выполнит владелец при end-of-phase верификации.

Шаги ручного ретеста для SUMMARY (копия UAT Test 1 с добавленными проверками отката): на /devices с сид-базой — (а) быстрыми нажатиями (без пауз) набрать фрагмент серийника с первого символа: каждый символ виден, откатов нет, результаты обновляются ~300 мс после остановки, без skeleton-вспышек; (б) так же быстро стереть часть: удаления не откатываются; (в) начать перечитывать до срабатывания дебаунса: пуш уходит один, с финальным текстом; (г) нажать Enter, и в полёте навигации продолжить печатать: эхо не откатывает ввод; (д) регресс Test 2: «Сбросить фильтры» опустошает инпут, Back/Forward восстанавливают, push запроса с хвостовым пробелом нормализует инпут до тримнутого, навигационной петли нет.
  </action>
  <verify>
    <automated>cd /Users/aleksey/projects/barahlo && set -o pipefail && npx vitest run 2>&1 | grep -E "Test Files|Tests " && test -f .planning/phases/05-search-filters/05-06-SUMMARY.md && grep -q "Manual retest" .planning/phases/05-search-filters/05-06-SUMMARY.md && echo SUMMARY-OK</automated>
    <human-check>Владелец выполняет «Manual retest (UAT Test 1)» из 05-06-SUMMARY.md на dev-сервере (пункты а–д; пункт д — обязательный регресс Test 2). До end-of-phase верификации фазы этот план не считается закрытым по truth G-5-1.</human-check>
  </verify>
  <done>Сьюта зелёная, lint чист, 05-06-SUMMARY.md существует и содержит корень причины, форму фикса и полный скрипт ручного ретеста; пользователь может закрыть G-5-1 повторным прогоном UAT Test 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keystrokes → controlled input | Пользовательский ввод живёт только в клиентском state острова; в URL попадает через buildDevicesQuery (существующий, экранирование/trim не меняются) |
| URL q → RSC props | q проходит серверный parseDevicesSearchParams (trim, cap 100, LIKE-escape) — слой вне диффа этого плана, не затронут |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Tampering | query-params.ts разбор q из URL | low | accept | Враждебный q остаётся инертным существующей серверной валидацией (trim/cap/escape, T-05-02); фикс меняет только клиентскую реконсиляцию и не добавляет поверхность |
| T-05-08 | Denial of Service | search-box.tsx навигационный ритм | low | accept | Фикс не создаёт пуш-петлю: собственное эхо абсорбируется (ветка 2), внешняя адаптация завершается value === q (ветка 3), дебаунс остаётся одиночным; сценарии трасс в Task 1 это утверждают |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | Ноль новых пакетов в плане (legitimacy-аудит фазы: none); запрещено action-ом Task 1 |
</threat_model>

<verification>
- `npx vitest run` — 278 тестов / 18 файлов зелёные (сьюта не деградировала)
- `npm run lint` — чисто
- Структурные гейты Task 1: clean-check `value === lastSynced.current`, trim-матч `q === p.trim()`, реф inFlight (≥4 упоминаний: объявление, findIndex, push в колбэке, push в commitNow)
- Контракт UI-SPEC:127 сохранён (D-01): 300 мс дебаунс, replace scroll:false в startTransition, Enter-коммит, отмена таймера перечитыванием, value===q no-op guard
- UAT Test 2 (реконсиляция CR-01) — регресс по шагам (д) ручного ретеста
</verification>

<success_criteria>
- G-5-1 закрыт: быстрый ввод/удаление не теряет keystrokes и не откатывает ввод ни в одном из сценариев A–D debug-сессии
- UAT Test 2 не регрессировал: reset/Back/Forward/trim-эхо работают как до фикса
- Единственный изменённый файл продакшн-кода — app/(app)/devices/search-box.tsx
- Сьюта 278/278 зелёная, lint чист
- 05-06-SUMMARY.md содержит скрипт ручного ретеста; финальное закрытие гэпа — за человеком (end-of-phase)
</success_criteria>

<output>
Create `.planning/phases/05-search-filters/05-06-SUMMARY.md` when done
</output>
