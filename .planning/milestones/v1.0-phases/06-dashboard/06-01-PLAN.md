---
phase: 06-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "db/queries/devices.ts"
  - "db/queries/movements.ts"
  - "app/(app)/page.tsx"
  - "app/(app)/nav.tsx"
  - "scripts/smoke-devices.mjs"
autonomous: true
requirements:
  - DASH-01
  - DASH-03
must_haves:
  truths:
    - "Открытие / под сессией показывает живой дашборд: «Дашборд» (20/600), «Всего: {pluralDevices(total)}» (не ссылка — D-03), одна сетка из 8 тайлов (4 типа, затем 4 статуса), блок «Последние перемещения» (D-01, D-02; DASH-01)"
    - "(Спец-край 1, DASH-01) Каждый тайл показывает число из агрегата над теми же данными, что список /devices; отсутствующие бакеты GROUP BY рендерятся как 0 из keystone-дефолтов DEVICE_TYPES/DEVICE_STATUS_KEYS (Map.get ?? 0) — все 8 тайлов существуют всегда, включая свежую установку; parity-пин против listDevices доставляет план 06-02"
    - "(Спец-край 5, DASH-03) Лента = 10 последних движений, orderBy desc(occurredAt) + desc(id) (tiebreaker обязателен); строки — сегментированные ссылки: контейнер ряда без собственного href, модель ведёт на карточку устройства /devices/{id}, имена сотрудников — отдельные ссылки на /employees/{id} (id в SELECT), вложенные якоря запрещены; null from/to заменяется «склад» только в assigned/returned/transferred, received/from_repair/disposed — без маршрута (таблица UI-SPEC)"
    - "Тайл/счётчик-ссылки собраны ТОЛЬКО buildDevicesQuery с полным объектом DeviceFilters (D-03) — ручная конкатенация filter-URL в странице отсутствует"
    - "(Спец-край 3, частично; DASH-03) Свежая БД: все 8 тайлов «0», лента показывает «Перемещений пока нет» + «Здесь появятся выдачи, возвраты и передачи техники.» — честная норма (createDevice не пишет movement-событие)"
    - "(D-06) Страница — чистый server component: ноль новых клиентских островов (остаётся только существующий nav), ноль кэша/автообновления; каждый заход перезапускает queries"
    - "Таб браузера на / показывает «Дашборд» (export const metadata title), не корневой «Учёт техники» (research Pitfall 9)"
    - "(UI Considerations → covered) Списанное учитывается: тайл «Списано» нейтральный (навигация, не бейдж), пилюля «Списание» в ленте — destructive-тинт bg-destructive/10 text-destructive; «Всего» склоняется через pluralDevices; длинные имена/маршруты truncate + title"
  artifacts:
    - "db/queries/devices.ts: deviceCountByType() → TypeCount[], deviceCountByStatus(), totalDeviceCount() — GROUP BY/count рядом с deviceWhere/warrantyPredicate (co-location = D-04)"
    - "db/queries/movements.ts: listRecentMovements(limit = 10) → RecentMovementView[] (id, eventType, occurredAt, deviceId, model, serialNumber, fromId, fromName, toId, toName) — alias double-join listTimeline + innerJoin devices"
    - "app/(app)/page.tsx: переписан (стаб-форвардер удалён) — metadata «Дашборд», requireSession первой строкой, тайлы + лента + пустые состояния"
    - "app/(app)/nav.tsx: NAV_ITEMS с первым элементом { href: '/', label: 'Дашборд' }, active-правило не изменено"
    - "scripts/smoke-devices.mjs: шаг 8 переписан во всех трёх местах (header-список шагов, ассерт-блок, summary) — тот же коммит, что переписывает page.tsx"
  key_links:
    - "page.tsx → buildDevicesQuery (query-params.ts): единственный строитель URL всех 8 тайлов — сентинелы-omission и D-08 coupling в билдере"
    - "page.tsx → DEVICE_TYPES/DEVICE_STATUS_KEYS (lib/device-schema.ts): zero-default итерация keystone, не SQL-строки"
    - "feed rows → RecentMovementView.fromId/toId: наличие id в SELECT — то, что делает имена ссылками (D-05); timeline.tsx (names-as-text) — анти-аналог, не копировать"
    - "smoke-devices.mjs шаг 8 ↔ page.tsx: синхронно в одной задаче, иначе фазовый гейт краснеет (research Pitfall 7)"
  prohibitions:
    - requirement_id: DASH-01
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "Дашборд НЕ должен становиться вторым источником истины: ни один счётчик страницы не имеет собственной параллельной реализации предиката/фильтра — только агрегаты из db/queries/devices.ts, составленные из тех же операторов, что deviceWhere/warrantyPredicate (D-04)"
    - requirement_id: DASH-03
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "Лента НЕ должна фальсифицировать историю: маршрутная строка только по таблице событий UI-SPEC («склад» — только подлинные складские перемещения assigned/returned/transferred, «в ремонт» не изображается складским движением), события append-only читаются SELECT-ом — на дашборде не появляется никакая мутация"
  backstops:
    - statement: "Ошибка рендера дашборда не создаёт новой error-поверхности: агрегаты — тотальные функции (GROUP BY возвращает []), залоггиненный сбой всплывает в существующую границу; запрос / без сессии → переадресация на /login, никогда не 500"
      verification: backstop
    - statement: "Переполнение строк ленты: длинная модель обрезается до shrink-0 пилюли/серийника, длинный маршрут — до shrink-0 даты; title-атрибуты несут полный текст (held-out визуальная проверка на verify-work)"
      verification: backstop
---

<objective>
Tracer-срез фазы 6: `/` перестаёт быть переадресацией и становится живым дашбордом — сводные тайлы «сколько техники и какой» (DASH-01) и лента «что двигалось» (DASH-03) поверх существующего ядра, без единой мутации и без новых клиентских островов (D-06).

Purpose: тонкий end-to-end путь через ВСЕ слои фазы (queries-агрегаты → pure-RSC страница → nav → smoke) доказывает архитектуру read-модели до достройки гарантийного блока; попутно снимается единственный острый край — старый smoke-ассерт переадресации корня.
Output: рабочий дашборд с тайлами и лентой, обновлённый smoke, ноль красных гейтов.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard/06-PATTERNS.md
@.planning/phases/06-dashboard/06-UI-SPEC.md

# Phase-5 surface this plan extends and consumes:
@app/(app)/devices/query-params.ts
@app/(app)/devices/type-filter.tsx
@lib/device-schema.ts
@lib/movement-schema.ts
@lib/ru.ts
@lib/warranty.ts
@app/globals.css
</context>

<artifacts_section>
## Artifacts this phase produces (phase-wide; «этот план» = 06-01)

**Query functions**
- `deviceCountByType(): TypeCount[]`, `deviceCountByStatus()`, `totalDeviceCount(): number` — этот план (06-01)
- `listRecentMovements(limit = 10): RecentMovementView[]` + тип `RecentMovementView` — этот план (06-01)
- `warrantyPresetCounts(today?)`, `nearestExpiringWarranties(limit?, today?)` + типы `WarrantyPresetCounts`/`NearestExpiringWarranty` — план 06-02

**Page / nav**
- `app/(app)/page.tsx` — полная переписка: 06-01 (голова, 8 тайлов, блок ленты, пустые состояния) + 06-02 (блок «Гарантия»)
- `app/(app)/nav.tsx` — пункт «Дашборд» первым — 06-01

**Tests / smoke**
- `scripts/smoke-devices.mjs` шаг 8 (три места) — этот план (06-01)
- `tests/dashboard-queries.test.ts` (parity, TZ-граница, zero-default, лента) — план 06-02
- `scripts/smoke-dashboard.mjs` (периметр + render + deep-links) — план 06-02
</artifacts_section>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — «/»: живые счётчики по типам/статусам и лента 10 движений end-to-end</name>
  <files>db/queries/devices.ts, db/queries/movements.ts, app/(app)/page.tsx, app/(app)/nav.tsx, scripts/smoke-devices.mjs</files>
  <read_first>
    - .planning/phases/06-dashboard/06-PATTERNS.md — выкопировки file:line (весь план строится на них)
    - db/queries/devices.ts :1-24 (импорты: count, asc, gte, isNotNull, lt, lte уже на месте; контракт чистых sync-функций), :191-203 (warrantyPredicate), :213-244 (deviceWhere), :246-293 (listDevices + count-блок :268-273)
    - db/queries/movements.ts :2 (alias), :6-8 (чистый модуль), :23-30 (MovementEventView — прецедент view-типа), :365-387 (listTimeline — join-паттерн и комментарий про backdated-порядок)
    - app/(app)/devices/page.tsx :1-93 (скелет RSC-страницы, imports+metadata, guard-first, today-once), :108-151 (пустые состояния-карточки), :166-229 (анатомия двухстрочного ряда с title), :242-264 (href только через билдер)
    - app/(app)/devices/query-params.ts :23-30 (DeviceFilters), :94-104 (buildDevicesQuery)
    - app/(app)/devices/type-filter.tsx :24-30 (FILTER_ITEMS — байт-точные множественные подписи типов)
    - app/(app)/nav.tsx :10-13 (NAV_ITEMS), :20-21 (active-правило)
    - lib/device-schema.ts :16, :82-87, :115-126 (DeviceTypeKey, DEVICE_TYPES, deviceStatusLabel, DEVICE_STATUS_KEYS)
    - lib/movement-schema.ts :18-35 (movementEventLabel — 7 лейблов + raw-fallback; НОВОЙ карты не создавать)
    - lib/ru.ts :24-37 (pluralDevices), :52-59 (occurredAtFormat, DISPLAY_TZ)
    - lib/warranty.ts (displayTodayUtc — вычисляется один раз за рендер)
    - scripts/smoke-devices.mjs :1-30 (header-список шагов — needle №1), :115-130 (шаг 5 — replacement-паттерн 200+probe; mint-cookie), :155-170 (needle №2 — блок ассерта), :270-280 (needle №3 — summary)
    - app/globals.css :61-69 (токены существуют — файл НЕ трогать, zero new tokens)
    - /Users/aleksey/.zcode/skills/vercel-react-best-practices/SKILL.md — правила server-serialization (ничего нового через RSC-границу), server-hoist-static-io (Intl-форматтеры уже module-level), rendering-conditional-render (тернарники пустых состояний)
    - /Users/aleksey/.zcode/skills/apple-design/SKILL.md — раздел 16 (тишина/воздух) и 15 (типографика) как обоснование quiet-тайлов UI-SPEC
    - .planning/phases/06-dashboard/06-UI-SPEC.md — Visual Details (Tile, Event pill, Feed row), Copywriting Contract, Screens & Component Inventory
    - .planning/phases/06-dashboard/06-RESEARCH.md — Patterns 1/4/5, Pitfalls 2/3/4/5/7/8/9
  </read_first>
  <action>
    1. **db/queries/devices.ts** — добавь три экспортированных агрегата РЯДОМ с deviceWhere/warrantyPredicate (co-location и есть D-04-инвариант; НЕ создавай отдельный dashboard-модуль и не экспортируй сам предикат — research Pattern 1). Сигнатуры по probe-верифицированным shape'ам RESEARCH Pattern 1: deviceCountByType возвращает массив { typeKey, n } с typeKey: DeviceTypeKey; deviceCountByStatus — { status: DeviceStatusKey; n }; totalDeviceCount — число через count()-запрос с .get()!.value по образцу count-блока listDevices (:268-273, без join — тайловые термы employees не трогают). GROUP BY через .groupBy(devices.typeKey) / .groupBy(devices.status). Только типизированные drizzle-операторы: raw sql-шаблон не биндит Date (probe: TypeError) — single-pass CASE агрегация запрещена (orchestrator-резолюция 1). Комментарий к блоку: счётчики дашборда обязаны делить предикатный код с фильтрами фазы 5.
    2. **db/queries/movements.ts** — добавь listRecentMovements(limit = 10): RecentMovementView[] — ОДИН join-запрос, не N+1 (resolved discretion). Копия паттерна listTimeline (:365-387): alias(employees, 'from_emp') и alias(employees, 'to_emp'), leftJoin по fromEmployeeId/toEmployeeId, ПЛЮС innerJoin(devices, eq(movements.deviceId, devices.id)) — inner безопасен (FK NOT NULL + onDelete restrict; списанные устройства не удаляются). SELECT несёт: id, eventType, occurredAt, deviceId, model, serialNumber, fromId: fromEmp.id, fromName: fromEmp.name, toId: toEmp.id, toName: toEmp.name — id ОБЯЗАТЕЛЬНЫ, D-05 превращает имена в ссылки, и ссылке нужен id (в отличие от MovementEventView; timeline.tsx names-as-text — АНТИ-аналог, orchestrator-резолюция 4). Порядок: orderBy(desc(movements.occurredAt), desc(movements.id)) — tiebreaker обязателен, backdated-события реальны (Pitfall 5). Только чтение: ни одного UPDATE/DELETE (append-only триггеры). Экспортируй тип RecentMovementView по RESEARCH Pattern 4.
    3. **app/(app)/page.tsx** — перепиши файл ЦЕЛИКОМ: текущий 8-строчный стаб, форвардящий корень на список устройств, удаляется (D-01; orchestrator-резолюция 3: smoke чинится в этой же задаче — см. п.5). Скелет по devices/page.tsx: export const metadata с title «Дашборд» (Pitfall 9); первой исполняемой строкой await requireSession() (defense-in-depth, периметр proxy и так покрывает /); затем sync-вызовы: const today = displayTodayUtc() — ОДИН раз за рендер (orchestrator-резолюция 6; в 06-02 этот же today получат гарантийные счётчики и WarrantyDate), totalDeviceCount(), deviceCountByType() → Map по ключу, deviceCountByStatus() → Map, listRecentMovements(10). Рендер по 06-UI-SPEC (Screens & Component Inventory + Visual Details, копи-контракт дословный):
       - Голова: «Дашборд» (text-xl font-semibold tracking-tight text-ink); под ним на mt-1 «Всего: {pluralDevices(total)}» (text-sm text-ink-secondary) — НЕ ссылка (D-03).
       - Тайлы: ОДНА сетка `grid grid-cols-2 gap-3 sm:grid-cols-4` с mt-6 — 8 Link-тайлов, сначала 4 типа, затем 4 статуса. Стиль тайла per UI-SPEC: rounded-2xl bg-white p-4 shadow-sm ring-1 ring-hairline hover:bg-page active:scale-[0.97] focus-visible:ring-2 ring-accent/30; контент — число (text-[28px] font-semibold tracking-[-0.02em] leading-[1.2] text-ink) над подписью (text-sm text-ink-secondary truncate, mt-1). БЕЗ chevron/иконки/пилюли. Подписи типов — page-local const из ЧЕТЫРЁХ множественных подписей байт-точно как FILTER_ITEMS острова type-filter («Ноутбуки», «Мониторы», «Док-станции», «Периферия» — НЕ deviceTypeName-единственные; orchestrator-резолюция 5; RSC не импортирует константу клиентского острова), с комментом-ссылкой на type-filter.tsx FILTER_ITEMS как источник истины. Подписи статусов — deviceStatusLabel, порядок D-02: assigned, in_stock, repair, disposed (все четыре тайла в идентичной нейтральной стилистике — «Списано» навигация, не бейдж). Zero-default (Pitfall 3): тайлы рендерятся итерацией DEVICE_TYPES / DEVICE_STATUS_KEYS с Map.get(key) ?? 0 — GROUP BY молчит по отсутствующим бакетам; фантомный тайл от неизвестного статуса в данных невозможен, т.к. итерация идёт по keystone.
       - Href каждого тайла строит ТОЛЬКО buildDevicesQuery с ПОЛНЫМ объектом DeviceFilters: для типа { q: '', type: key, status: 'all', departmentId: null, warranty: 'all', ramNoUpgrade: false }, для статуса зеркально { q: '', type: 'all', status: key, ... }. Ручная конкатенация filter-URL запрещена (анти-паттерн RESEARCH: сентинелы-omission и D-08 coupling — работа билдера; orchestrator-резолюция 7).
       - Блок «Последние перемещения» (DASH-03, D-05): заголовок 20/600 + белая карточка `mt-3 rounded-2xl bg-white shadow-sm ring-1 ring-hairline divide-y divide-hairline`. Ряд = listRecentMovements(10): контейнер `min-h-11 px-4 py-2 hover:bg-page` БЕЗ собственного href — вложенные якоря невалидны и ломают гидрацию (Pitfall 4; orchestrator-резолюция 4). Строка 1: пилюля события movementEventLabel(eventType) `rounded-full bg-black/5 px-2 py-1 text-sm text-ink-secondary` shrink-0 (для «Списание» — `bg-destructive/10 text-destructive`, единственный tinted-лейбл), затем Link на `/devices/${deviceId}`: span модели (text-base text-accent truncate; hover:underline underline-offset-2 — первый текст-линк приложения per UI-SPEC) + span серийника `font-mono text-sm text-ink-secondary shrink-0`; полный текст строки — в title атрибуте. Строка 2 (`mt-1 flex items-center justify-between gap-2`): маршрут (text-sm text-ink-secondary truncate) + дата `occurredAtFormat.format(occurredAt)` (text-sm text-ink-secondary shrink-0). Маршрут по таблице событий UI-SPEC (D-05; orchestrator-резолюция 4): assigned — «склад» → ссылка toName; transferred — ссылка fromName → ссылка toName; returned — ссылка fromName → «склад»; to_repair — «от {ссылка fromName}» (если держатель был); received / from_repair / disposed — маршрута нет (только дата). Null-слот = «склад» ТОЛЬКО в трёх складских перемещениях; archived-сотрудники ссылаются как активные (карточка сама показывает состояние). НОВОЙ карты событий и нового форматтера не создавать — movementEventLabel и occurredAtFormat уже существуют и протестированы (keystone-правило).
       - Пустая лента — НОРМА свежей БД (Pitfall 8: createDevice не пишет movement): quiet-строки в карточке «Перемещений пока нет» (text-sm text-ink) и «Здесь появятся выдачи, возвраты и передачи техники.» (text-sm text-ink-secondary).
       - Дисциплина vercel-react-best-practices: server-serialization — на страницу ничего не сериализуется (островов нет); rendering-conditional-render — пустые состояния тернарниками; Intl-форматтеры остаются module-level (server-hoist-static-io).
       - loading.tsx для / НЕ создавать (orchestrator-резолюция 2): граница на уровне (app) сидит НАД группой (card) и ре-breaks 404-матрицу фазы 2 (research Pattern 6/Pitfall 6); ~8 sync-запросов рендерятся за единицы мс. Никаких error.tsx, островов, форм, диалогов.
    4. **app/(app)/nav.tsx** — NAV_ITEMS пополняется ПЕРВЫМ элементом { href: '/', label: 'Дашборд' } (D-01). Active-правило НЕ менять: pathname === item.href матчит «/» точно, startsWith(`//`) никогда не true (верифицировано по коду :20-21). Остальное острова не трогать (D-06: единственный клиентский JS на странице остаётся этот остров).
    5. **scripts/smoke-devices.mjs** — шаг 8 меняется в ТРЁХ местах В ЭТОЙ ЖЕ ЗАДАЧЕ (Pitfall 7; orchestrator-резолюция 3): (а) header-список шагов в шапке файла (needle «GET / WITH cookie → 307 …»); (б) ассерт-блок ~:159-167; (в) финальная summary-строка ~:275. Новый ассерт — по образцу шага 5 (:115-123): GET / с minted cookie ожидает 200 (не переадресацию) и HTML-маркеры дашборда: слово «Дашборд», наличие в разметке href на список с фильтром типа и с фильтром статуса, пункт навигации «Дашборд». На свежей temp-БД лента ПУСТА — ожидай empty-подпись «Перемещений пока нет», а не строки (Pitfall 8). Периметр (шаги без cookie → 307 /login) не трогать. Проверь, что в файле не осталось ни одного старого needle о переадресации корня.
  </action>
  <verify>
    <automated>npm run build && node scripts/smoke-devices.mjs && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    <!-- planner-discipline-allow: buildDevicesQuery -->
    - `npx vitest run` зелёный целиком (базовая линия ~278 не сломана)
    - `npm run build && node scripts/smoke-devices.mjs` — exit 0: периметр без cookie → 307 на /login; с cookie → 200 + маркеры дашборда в HTML
    - `grep -c "next/navigation" app/(app)/page.tsx` == 0 (стаб удалён целиком; action не использует этот литерал)
    - `grep -c "Дашборд" app/(app)/nav.tsx` >= 1 и первый элемент NAV_ITEMS имеет href «/»
    - `grep -c "export function deviceCountByType" db/queries/devices.ts` == 1; `grep -c "export function deviceCountByStatus" db/queries/devices.ts` == 1; `grep -c "export function totalDeviceCount" db/queries/devices.ts` == 1
    - `grep -c "groupBy" db/queries/devices.ts` >= 2 (typeKey и status)
    - `grep -c "export function listRecentMovements" db/queries/movements.ts` == 1; `grep -c "fromId: fromEmp.id" db/queries/movements.ts` == 1; `grep -c "toId: toEmp.id" db/queries/movements.ts` == 1; `grep -c "desc(movements.id)" db/queries/movements.ts` >= 1
    - `grep -c "buildDevicesQuery" app/(app)/page.tsx` >= 1; `grep -c '"/devices?' app/(app)/page.tsx` == 0 (ни одной ручной filter-URL-строки; шаблонные ссылки карточек на литерал не попадают)
    - `grep -c "DEVICE_TYPES" app/(app)/page.tsx` >= 1; `grep -c "DEVICE_STATUS_KEYS" app/(app)/page.tsx` >= 1 (zero-default из keystone)
    - `grep -c "movementEventLabel" app/(app)/page.tsx` >= 1; `grep -c "occurredAtFormat" app/(app)/page.tsx` >= 1 (keystone-переиспользование, без параллельных карт)
    - `grep -c "Дашборд" app/(app)/page.tsx` >= 2 (заголовок + metadata title)
    - в корне фазы не появился ни один loading-файл для группы (app): `ls "app/(app)/loading.tsx" 2>/dev/null` пуст
  </acceptance_criteria>
  <done>Оператор открывает / и видит: «Дашборд», «Всего: N устройств», 8 кликабельных нейтральных тайлов с живыми числами (все 8 существуют даже на пустой базе — по нулю), и ленту 10 последних движений с пилюлями событий, ссылками на карточки устройств и сотрудников, «склад» для складских перемещений и датой в DISPLAY_TZ; на пустой базе лента честно пуста. Smoke зелёный, полная vitest-басline зелёная, build проходит.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → / (RSC) | Аутентифицированный GET без параметров; страница не читает searchParams вообще |
| / → /devices (deep links) | Tile hrefs — статические внутренние filter-URL из buildDevicesQuery; целевая страница серверно ревалидирует каждый параметр |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | `/` (RSC page) | high | mitigate | Двойной существующий слой: proxy default-deny покрывает «/» (матчер верифицирован) + await requireSession() первой строкой страницы; smoke ассертит 307 /login без cookie (canon-безопасность — breadcrumb: /gsd-secure-phase, не minted-запрет) |
| T-06-02 | Tampering (XSS) | feed rows / tile labels | medium | mitigate | React auto-escaping, без dangerouslySetInnerHTML; hrefs — шаблонные литералы над внутренними id и вывод buildDevicesQuery, никогда пользовательские строки; имена сотрудников рендерятся текстом внутри Link |
| T-06-03 | Tampering | ручная правка query `/` | low | accept | Страница не парсит ни одного параметра (V5 n/a); параметры игнорируются, ссылки — константы билдера |
| T-06-04 | Information Disclosure | IDOR-зондаж ссылок ленты (/devices/{id}, /employees/{id}) | low | accept | Вне скоупа фазы: карточные маршруты уже guarding (requireSession + zod id + notFound); лента не открывает новых id сверх существующих списков; тот же auth-периметр |
| T-06-SC | Tampering | npm-инсталлы | — | n/a | Ноль новых пакетов (Package Legitimacy Audit RESEARCH: none) — supply-chain чекпоинт не требуется |
</threat_model>

<verification>
- `npm run build` проходит; `node scripts/smoke-devices.mjs` exit 0 (новый шаг 8 + нетронутый периметр); `npx vitest run` зелёный
- Страница / отдаёт 200 под сессией и 307 → /login без неё; ни одного нового клиентского острова; `app/(app)/loading.tsx` не существует
- Ветвление волн: план не трогает файлы вне files_modified
</verification>

<success_criteria>
- DASH-01 (рендер-половина): 8 тайлов с живыми числами по типам/статусам, zero-default от keystone, каждый — deep-link фильтра /devices через buildDevicesQuery; «Всего: N» — некликабельный заголовок
- DASH-03: лента 10 последних движений, стабильный порядок (occurredAt DESC, id DESC), сегментированные ссылки без вложенных якорей, «склад» по таблице событий, id сотрудников в SELECT
- D-01/D-02/D-05/D-06 соблюдены; smoke-needle переадресации снят синхронно с переписыванием страницы; parity-пин счётчиков доставляет 06-02
</success_criteria>

<output>
Create `.planning/phases/06-dashboard/06-01-SUMMARY.md` when done
</output>
