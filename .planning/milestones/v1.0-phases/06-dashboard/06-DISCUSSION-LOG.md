# Phase 6: Dashboard - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-14
**Phase:** 6-dashboard
**Mode:** --auto (auto-chain после закрытия фазы 5; все решения — recommended-дефолты)
**Areas discussed (auto-selected):** Адрес и навигация, Иерархия экрана, Кликабельность тайлов, Гарантийный блок, Лента перемещений, Данные и свежесть

---

## Адрес и навигация

| Option | Description | Selected |
|--------|-------------|----------|
| `/` = дашборд (Recommended) | redirect убирается, nav: Дашборд · Устройства · Сотрудники | ✓ |
| Отдельный `/dashboard` | главная остаётся redirect на /devices | |

**Auto-selected:** `/` = дашборд — открыл приложение → видишь картину; сводка = витрина read-модели.

---

## Иерархия экрана

| Option | Description | Selected |
|--------|-------------|----------|
| Тайлы типов + статусов, ниже Гарантия + Лента (Recommended) | два ряда тайлов, двухблочная зона под ними | ✓ |
| Единый список виджетов | проще, но теряется сводка-с-одного-взгляда | |

**Auto-selected:** тайлы сверху, гарантия/лента ниже; сетка — UI-SPEC.

---

## Кликабельность тайлов

| Option | Description | Selected |
|--------|-------------|----------|
| Deep-link в фильтры фазы 5 (Recommended) | `?type=laptop`, `?status=repair` — ноль новой выборки | ✓ |
| Статичные цифры | сводка без навигации | |

**Auto-selected:** все тайлы кликабельны в готовые URL-фильтры.

---

## Гарантийный блок

| Option | Description | Selected |
|--------|-------------|----------|
| Счётчики-пресеты + топ-5 ближайших (Recommended) | ссылки w30/w60/expired; WarrantyDate переиспользуется | ✓ |
| Один объединённый пресет «истекает+истекла» | потребовал бы расширения парсера фазы 5 | |
| Только счётчики без списка | клик и так ведёт в полный список | |

**Auto-selected:** три счётчика (готовые пресеты) + 5 ближайших живых; расширять фазу-5 парсер не нужно.

---

## Лента перемещений

| Option | Description | Selected |
|--------|-------------|----------|
| 10 последних, клик в карточку устройства (Recommended) | пилюля события · модель · от→кому · дата DISPLAY_TZ | ✓ |
| 5 последних | компактнее, но лента редко длиннее экрана | |
| С фильтром по типу события | YAGNI для одного оператора | |

**Auto-selected:** 10 событий, строка → /devices/[id], имена — ссылки в сотрудников.

---

## Данные и свежесть

| Option | Description | Selected |
|--------|-------------|----------|
| Чистый RSC, живые данные на каждый заход (Recommended) | GROUP BY-агрегаты, без кэша и островов | ✓ |
| Клиентский виджет с автообновлением | polling/SSE — YAGNI на LAN для одного оператора | |

**Auto-selected:** server component без кэша/обновлений.

---

## Claude's Discretion

- Форма queries-агрегатов (GROUP BY / отдельный модуль)
- Состав полей ленты в SQL (join vs N+1)
- Пустые состояния блоков, копи тайлов
- loading.tsx для `/`

## Deferred Ideas

- ⌘K / поиск сотрудников — V2-02
- Сохранённые фильтры — V2-01
- Автообновление дашборда — при появлении второго пользователя
- Графики закупок — вне v1
